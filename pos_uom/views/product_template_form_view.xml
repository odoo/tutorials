<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="product_template_form_view" model="ir.ui.view">
        <field name="name">product.template.form.inherit.pos_uom</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="point_of_sale.product_template_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='pos']/group/group[@name='pos']" position="inside">
                <field name="second_uom_id" domain="[('category_id', '=', uom_category_id)]" invisible="not uom_id" options="{'no_create': True}"/>
            </xpath>
        </field>
    </record>
</odoo>
