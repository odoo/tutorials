<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_order_form_inherit_kit" model="ir.ui.view">
        <field name="name">sale.order.view.form.inherit</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">

            <field name="product_template_id" position="after">
                <field name="is_kit" invisible="true" string=""/>
                <button name="%(kit.kit_sub_product_wizard_action)d" type="action" string="Sub Products" invisible="not is_kit or state != 'draft'" />
            </field>
            
            <xpath expr="//group[@name='order_details']" position="inside">
                <field name="print_sub_products_in_report"/>
            </xpath>
        </field>
    </record>
</odoo>
