<odoo>
    <template id="sale_order_portal_content" inherit_id="sale.sale_order_portal_content">
        <xpath expr="//tbody[@class='sale_tbody']/t/tr" position="attributes">
            <attribute name="t-if">(not line.parent_line_id) or (line.parent_line_id and sale_order.print_in_report)</attribute>
        </xpath>
    </template>
</odoo>
