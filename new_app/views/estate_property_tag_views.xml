<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Form View For Estate Property Tag Model -->
    <record id="view_estate_property_tag_form" model="ir.ui.view">
        <field name="name">estate.property.tag.form</field>
        <field name="model">estate.property.tag</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <field name="name"/>
                    <!-- <field name="color"/> -->
                </sheet>
            </form>
        </field>
    </record>

    <!-- List View  -->
    <record id="view_estate_property_tag_list" model="ir.ui.view">
        <field name="name">estate.property.tag.list</field>
        <field name="model">estate.property.tag</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <!-- <field name="color"/> -->
            </list>
        </field>
    </record>

    <record id="estate_property_tag_action" model="ir.actions.act_window">
        <field name="name">Property Tags</field>
        <field name="res_model">estate.property.tag</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem id="menu_estate_property_tag" name="Property Tags" parent="new_app.menu_properties" sequence="4" action="estate_property_tag_action" />

</odoo>
