<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <record forcecreate="True" id="ir_cron_reminder_action" model="ir.cron">
        <field name="name">Digest Emails</field>
        <field name="model_id" ref="model_estate_property_visit"/>
        <field name="state">code</field>
        <field name="code">model._cron_send_reminder()</field>
        <field name="user_id" ref="base.user_root"/>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="nextcall" eval="(DateTime.now() + timedelta(hours=2)).strftime('%Y-%m-%d %H:%M:%S')"/>
    </record>

    <record forcecreate="True" id="ir_cron_refuse_offer" model="ir.cron">
        <field name="name">Refuse Offer</field>
        <field name="model_id" ref="model_estate_property_offer"/>
        <field name="state">code</field>
        <field name="code">model._cron_refuse_offer()</field>
        <field name="user_id" ref="base.user_root"/>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="nextcall" eval="(DateTime.now() + timedelta(hours=2)).strftime('%Y-%m-%d %H:%M:%S')"/>
    </record>
</odoo>