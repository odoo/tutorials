<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="estate_type_action" model="ir.actions.act_window">
        <field name="name">Estate Type</field>
        <field name="res_model">estate.property.type</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- List view -->
    <record id="estate_property_type_view_list" model="ir.ui.view">
        <field name="name">estate.property.type.list</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <list string="Properties">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
            </list>
        </field>
    </record>

    <!-- Form view -->
    <record id="estate_property_type_view_form" model="ir.ui.view">
        <field name="name">estate.property.type.form</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <form string="Estate Property Type">
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button type="action" name="%(estate_property_offers_action)d" class="oe_stat_button" invisible="not offer_count" icon="fa-money">
                            <field name="offer_count" string="Offers" widget="statinfo"/>
                        </button>
                    </div>
                    <h1>
                        <field name="name" placeholder="e.g. House"/>
                    </h1>
                    <notebook>
                        <page string="Properties" name="properties">
                            <field name="property_ids">
                                <list>
                                    <field name="name"/>
                                    <field name="expected_price"/>
                                    <field name="state"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

</odoo>
