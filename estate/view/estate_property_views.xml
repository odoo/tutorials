<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form,kanban</field>
        <field name="context">{'search_default_available': True}</field>
    </record>

    <!-- List view -->
    <record id="estate_property_view_list" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="Properties" decoration-success="state in ['offer_received','offer_accepted']" decoration-muted="state=='sold'" decoration-it="state=='offer_accepted'">
                <field name="name"/>
                <field name="postcode"/>
                <field name="bedroom"/>
                <field name="living_area"/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="expected_date" optional="hide"/>
            </list>
        </field>
    </record>

    <!-- Kanban view -->
    <record id="estate_property_view_kanban" model="ir.ui.view">
        <field name="name">estate.property.kanban</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <kanban default_group_by="property_type_id">
                <field name="state"/>
                <field name="property_type_id"/>
                <templates>
                    <t t-name="card" class="flex-row">
                        <main class="ms-2">
                            <field name="name" class="fw-bold"/>
                            <div>
                                <field name="expected_price"/>
                            </div>
                            <t t-if="record.state.raw_value == 'offer_received'">
                                <div>
                                    <field name="best_price" />
                                </div>
                            </t>
                            <t t-if="record.state.raw_value == 'offer_accepted'">
                                <div>
                                    <field name="selling_price" />
                                </div>
                            </t>
                            <div>
                                <field name="property_tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                            </div>
                        </main>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Form view -->
    <record id="estate_property_view_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="New Estate">
                <header>
                    <button name="action_sold_adv" type="object" string="Sold" invisible="state in ['sold', 'cancelled']"/>
                    <button name="action_cancel_adv" type="object" string="Cancel" invisible="state in ['sold', 'cancelled']"/>
                    <field name="state" widget="statusbar" statusbar_visible="new,offer_received,offer_accepted,sold"/>
                </header>
                <sheet>
                    <h1>
                        <field name="name" placeholder="e.g New House"/>
                    </h1>
                    <field name="property_tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                    <group>
                        <group>
                            <field name="postcode"/>
                            <field name="expected_date" string="Available From"/>
                        </group>
                        <group>
                            <field name="expected_price"/>
                            <field name="best_price"/>
                            <field name="selling_price"/>
                        </group>
                        <group>
                            <field name="property_type_id"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <field name="description" string="Description"/>
                            <group>
                                <group>
                                    <field name="bedroom"/>
                                    <field name="living_area" string="Living Area (sqm)"/>
                                    <field name="facades" string="Facades"/>
                                    <field name="garage"/>
                                    <field name="garden"/>
                                    <field name="garden_area" string="Garden Area (sqm)" invisible="garden"/>
                                    <field name="garden_orientation" invisible="garden"/>
                                    <field name="total_area"/>
                                </group>
                            </group>
                        </page>
                        <page string="Offers">
                            <field name="proterty_offer_ids" readonly="state in ['sold','cancelled','offer_accepted']">
                                <list editable="bottom" decoration-success="status=='accepted'" decoration-danger="status=='refused'" >
                                    <field name="price"/>
                                    <field name="partner_id"/>
                                    <field name="validity"/>
                                    <field name="date_deadline"/>
                                    <button name="action_accept_offer" type="object" icon="fa-check" string="accepte" invisible="status != 'new'"/>
                                    <button name="action_refuse_offer" type="object" icon="fa-times" string="refuse" invisible="status != 'new'"/>
                                </list>
                            </field>
                        </page>
                        <page string="Other">
                            <group>
                                <group>
                                    <field name="salesman_id"/>
                                    <field name="buyer_id"/>
                                </group>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Search -->
    <record id="estate_property_search" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Estate Type">
                <field name="name" string="Estate" />
                <field name="postcode"/>
                <field name="expected_price"/>
                <field name="bedroom"/>
                <field name="living_area" filter_domain="[('living_area', '>=', self)]"/>
                <field name="facades"/>
                <separator/>
                <filter string="Available" name="available" domain="[('state', 'in', ['new', 'offer_received'])]"/>
                <group>
                    <filter string="Postcode" name="postcode" context="{'group_by':'postcode'}"/>
                </group>
            </search>
        </field>
    </record>

</odoo>
