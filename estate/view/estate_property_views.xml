<?xml version="1.0"?>
<odoo>
    <!-- list view -->
    <record id="estate_model_list_view" model="ir.ui.view">
        <field name="name">estate_property_view</field>
        <field name="model">estate_model</field>
        <field name="arch" type="xml">
            <list string="list_view" decoration-success="state in ['offer_received', 'offer_accepted']" decoration-muted="state == 'sold'" decoration-bf="state == 'offer_accepted'">
                <field name="name"/>
                <field name="postcode"/>
                <field name="bedrooms"/>
                <field name="living_area"/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="date_availability" invisible="1" optional="1"/>
                <field name="property_type_id"/>
                <field name="tag_ids"/>
            </list>
        </field>
    </record>
    <!-- search view -->
    <!-- <record id="search_view" model="ir.ui.view">
            <field name="name">estate_search_view</field>
            <field name="model">estate_model</field>
            <field name="arch" type="xml">
                <search string="search view on field">
                    <field name="name"/>
                    <field name="postcode"/>
                    <field name="expected_price"/>
                    <field name="bedrooms"/>
                    <field name="living_area"/>
                    <field name="facades"/>
                    <field name="property_type_id"/>
                    <filter string="Available Properties" name="available_properties" domain="['|', ('state', '=', 'new'), ('state', '=', 'offer_received')]"/>
                    <filter name="living_area" string="Living Area (>=)" domain="[('living_area', '>=', context.get('living_area_value', 0))]" />
                    <filter name="available" string="Available" domain="['|', ('state', '=', 'new'), ('state', '=', 'offer_received')]"/>
                    <group string="Group By">
                        <filter string="Postcode" name="postcode" context="{'group_by':'postcode'}"/>
                    </group>
                </search>
            </field>
    </record> -->
    <record id="search_view" model="ir.ui.view">
        <field name="name">estate_search_view</field>
        <field name="model">estate_model</field>
        <field name="arch" type="xml">
            <search string="Search View">
                <!-- Fields for searching -->
                <field name="name"/>
                <field name="postcode"/>
                <field name="expected_price"/>
                <field name="bedrooms"/>
                <!-- <field name="living_area" filter_domain="[('living_area', '>=', self)]"/> -->
                <field name="living_area" string="Living Area (sqm)" filter_domain="[('living_area', '>=', self)]" />
                <field name="facades"/>
                <field name="property_type_id"/>

                <!-- Available Properties filter -->
                <filter string="Available Properties" name="available_properties" domain="['|', ('state', '=', 'new'), ('state', '=', 'offer_received')]"/>

                <!-- Living Area filter (>=) -->
                <!-- <filter string="Living Area (>=)" name="living_area" domain="[('living_area', '>=', self)]"/> -->

                <!-- Available filter (same as Available Properties filter) -->
                <filter name="available" string="Available" domain="['|', ('state', '=', 'new'), ('state', '=', 'offer_received')]"/>

                <!-- Group By filter -->
                <group string="Group By">
                    <filter string="Postcode" name="postcode" context="{'group_by':'postcode'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- action model -->
    <record id="estate_model_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate_model</field>
        <field name="view_mode">list,form,kanban,graph</field>
        <field name="context">{'search_default_available': 1}</field>
        <field name="view_id" ref="estate_model_list_view"/>
    </record>
    <!-- form view -->
    <record id="estate_model_form_view" model="ir.ui.view">
        <field name="name">estate_property_form_view</field>
        <field name="model">estate_model</field>
        <field name="arch" type="xml">
            <form string="Property Form">
                <header>
                    <button name="sold_action" type="object" string="Sold" invisible="state in ['sold', 'cancelled']"/>
                    <button name="cancle_action" type="object" string="Cancle" invisible="state in ['sold', 'cancelled']"/>
                    <field name="state" widget="statusbar" statusbar_visible="new, offer_received, offer_accepted, sold"/>
                </header>
                <sheet>
                    <group>
                        <h2>Name: </h2>
                        <h1>
                            <field name="name"/>
                        </h1>
                        <field name="state"/>
                        <field name="tag_ids" widget="many2many_tags" options="{'color_field':'Color'}"/>
                    </group>
                    <group>
                        <group>
                            <field name="property_type_id" options="{'no_create': True}"/>
                            <field name="postcode"/>
                            <field name="date_availability"/>
                        </group>
                        <group>
                            <field name="expected_price"/>
                            <field name="selling_price"/>
                            <field name="best_price"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description"/>
                                <field name="property_type_id"/>
                                <field name="bedrooms"/>
                                <field name="living_area"/>
                                <field name="facades"/>
                                <field name="garage"/>
                                <field name="garden"/>
                                <field name="garden_area" invisible="garden == False"/>
                                <field name="garden_orientation" invisible="garden == False"/>
                                <field name="total_area"/>
                            </group>
                        </page>
                        <page string="Offers">
                            <field name="offer_ids" widget="one2many_list" readonly="state in ['offer_accepted', 'sold', 'cancelled']">
                                <list editable="top" decoration-success="status == 'accepted'" decoration-danger="status == 'refused'">
                                    <field name="price"/>
                                    <field name="partner_id"/>
                                    <field name="validity"/>
                                    <field name="date_deadline"/>
                                    <field name="status"/>
                                    <button name="action_accept" string="Confirm" type="object" icon="fa-check" invisible="status == 'accepted'"/>
                                    <button name="action_refuse" string="Refuse" type="object" icon="fa-times" invisible="status == 'accepted'"/>
                                </list>
                            </field>
                        </page>

                        <page string="Other Info">
                            <group>
                                <field name="salesman"/>
                                <field name="buyer"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>
</odoo>