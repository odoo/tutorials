<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_estate_add_offer_wizard_form" model="ir.ui.view">
        <field name="name">estate.add.offer.wizard.form</field>
        <field name="model">estate.add.offer.wizard</field>
        <field name="arch" type="xml">
            <form string="Add Offer">
                <group>
                    <field name="property_ids" widget="many2many_tags" readonly="1"/>
                    <field name="price"/>
                    <field name="status"/>
                    <field name="buyer_id"/>
                    <field name="validity"/>
                </group>
                <footer>
                    <button name="action_make_offer" string="Make an Offer" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_estate_add_offer_wizard" model="ir.actions.act_window">
        <field name="name">Add Offer</field>
        <field name="res_model">estate.add.offer.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>
