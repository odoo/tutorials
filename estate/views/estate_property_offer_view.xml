<odoo>
    <record id="estate_property_offer_view_list" model="ir.ui.view">
        <field name="name">estate.property.offer.view.list</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <list editable="bottom" decoration-danger="status == 'refused'" decoration-success="status == 'accepted'">
                <field name="price" width="50px"/>
                <field name="partner_id" string="Partner" width="50px"/>
                <button name="offer_accepted" string="Accept" type="object" icon="fa-check" width="50px" invisible="status" groups="estate.group_estate_manager,base.group_system"/>
                <button name="offer_refused" string="Refuse" type="object" icon="fa-times" width="50px" invisible="status" groups="estate.group_estate_manager,base.group_system"/>
                <field name="status" width="50px"/>
                <field name="validity" string="Validity (days)" width="50px"/>
                <field name="deadline" width="50px" options="{'min_date': 'today'}"/>
            </list>
        </field>
    </record>

    <record id="estate_property_offer_view_form" model="ir.ui.view">
        <field name="name">estate.property.offer.view.form</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <form>
                <group>
                    <field name="price"/>
                    <field name="deadline" options="{'min_date': 'today'}"/>
                </group>
                <!-- <group>
                    <field name="status"/>
                    <field name="property_id"/>
                </group> -->
                <group>
                    <field name="partner_id"/>
                    <field name="validity"/>
                </group>
            </form>
        </field>
    </record>

    <record id="estate_property_offer_action" model="ir.actions.act_window">
        <field name="name">Offer</field>
        <field name="res_model">estate.property.offer</field>
        <field name="view_mode">list,form</field>
    </record>

    <record id="estate_property_send_offer_server_action" model="ir.actions.server">
        <field name="name">Create Offers NEWW</field>
        <field name="model_id" ref="estate.model_estate_property_offer_wizard"/>
        <field name="binding_model_id" ref="estate.model_estate_property"/>
        <field name="binding_view_types">list,form</field>
        <field name="state">code</field>
        <field name="code">
            action = {
                'name': 'Create Offer',
                'type': 'ir.actions.act_window',
                'res_model': 'estate.property.offer.wizard',
                'view_mode': 'form',
                'target': 'new',
                'context': env.context,
            }
        </field>
    </record>
</odoo>
