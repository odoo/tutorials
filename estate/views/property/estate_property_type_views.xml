<odoo>
    <!-- List View -->
    <record id="view_estate_property_type_list" model="ir.ui.view">
        <field name="name">Properties Type</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <list string="Properties Type" editable="bottom">
                <field name="name"/>
                <field name="description"/>
                <field name="status"/>
            </list>
        </field>
    </record>

    <!-- Search View -->
    <record id="view_estate_property_search" model="ir.ui.view">
        <field name="name">Properties Type Search</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <search string="Search Properties">
                <field name="name"/>
                <field name="status"/>
            </search>
        </field>
    </record>
</odoo>