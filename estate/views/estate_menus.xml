<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Root Menu -->
    <menuitem 
        id="estate_menu_root" 
        name="Real Estate"/>

    <!-- First Level -->
    <menuitem 
        id="estate_menu_properties"
        name="Properties"
        parent="estate_menu_root"/>

    <!-- Action Menu -->
    <menuitem 
        id="estate_menu_properties_action"
        parent="estate_menu_properties"
        action="estate_property_action"/>


    <!-- Property Type Menu -->
    <menuitem 
        id="estate_menu_type"
        name="Property Type"
        parent="estate_menu_root"/>

    <!-- Property Type Action -->
    <record id="estate_property_type_action" model="ir.actions.act_window">
        <field name="name">Property Type</field>
        <field name="res_model">estate.property.type</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem
        id="estate_menu_type_action"
        parent="estate_menu_type"
        action="estate_property_type_action"/>


    <!-- Property Tags Menu -->
    <menuitem 
        id="estate_menu_tags"
        name="Property Tags"
        parent="estate_menu_root"/>

    <!-- Property Tags Action -->
    <record id="estate_property_tag_action" model="ir.actions.act_window">
        <field name="name">Property Tags</field>
        <field name="res_model">estate.property.tag</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem
        id="estate_menu_tags_action"
        parent="estate_menu_tags"
        action="estate_property_tag_action"/>
</odoo>

