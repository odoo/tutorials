<?xml version="1.0"?>
<odoo>
 <record id="estate_property_view_search" model="ir.ui.view">
    <field name="name">estate.property.view.search</field>
    <field name="model">estate.property</field>
    <field name="type">search</field>
    <field name="arch" type="xml">

        <search string="Tests">
            <field name="name"/>
            <field name="property_type_id"/>
            <field name="bedrooms" />
            <field name="description"/>
            <field name ="postcode"/>
            <field name ="date_availability"/>
            <field name ="expected_price"/>
            <field name="buyer_id"/>
            <field name="total_area" string="living_area bigger than" filter_domain="[('living_area', '&gt;', self)]"/>
            <field name="salesperson_id"/>
            <field name ="selling_price"/>
            <field name ="living_area"/>
            <group expand="1" string="Group By">
            <filter string="BedRooms" name="bedrooms" context="{'group_by':'bedrooms', 'residual_visible':True,'bedrooms':''}"/>
            </group>
            <filter string="Postcode" name="bedrooms" context="{'group_by':'postcode', 'residual_visible':True}"/>
            <filter string="Buyer" name="bedrooms" context="{'group_by':'buyer_id', 'residual_visible':True}"/>
            <filter string="With Garden" name="garden" domain="[('garden', '=', True)]"/>
            <filter string="Available" name="Available" domain="[('state', 'in',['New','Offer Received'] )]"/>


        </search>
    </field>
 </record>


</odoo>
