<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="estate_property_type_action" model="ir.actions.act_window">
        <field name="name">Property Type</field>
        <field name="res_model">estate.property.type</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Define new property type
            </p>
            <p>
                Use options specified in form to list property type
            </p>
        </field>
    </record>

    <!-- list view -->
    <record id="estate_property_list_view" model="ir.ui.view">
        <field name="name">estate.property.type.list</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <list string="Channel">
                <field name="sequence" widget="handle"></field>
                <field name="name" string="Estate Type Name"></field>
            </list>
        </field>
    </record>


    <!-- Form View -->
    <record id="estate_property_type_form_view" model="ir.ui.view">
        <field name="name">estate.property.type.form</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <form string="Estate Property Type">
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="estate.estate_property_offer_action" type="action"
                            class="oe_stat_button" icon="fa-money">
                            <field name="offer_count" string="Offers Made" widget="statinfo" />
                        </button>
                    </div>
                    <group>
                        <field name="name" />
                    </group>

                    <notebook>
                        <page name="Properties">
                            <field name="property_ids">
                                <list string="Associated Properties">
                                    <field name="sequence" widget="handle" />
                                    <field name="name" string="Property Name" />
                                    <field name="expected_price" string="Expected Price" />
                                    <field name="state" string="Status" />
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

</odoo>