<?xml version='1.0' encoding='UTF-8'?>
<odoo>

    <!-- Form view -->
    <record id="estate_property_type_view_form" model="ir.ui.view">
        <field name="name">estate.property.type.view.form</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <h1>
                        <field name="name"/>
                    </h1>
                    <div name="button_box" class="oe_button_box">
                        <button type="action" name="%(estate_property_offer_action)d" string="Offers" icon="fa-money">
                            <field name="offer_count" string="Offers" widget='statinfo'/>
                        </button>
                    </div>
                    <notebook>
                        <page string="Properties">
                            <field name="property_ids">
                                <list default_order="name desc" editable="top">
                                    <field name="name" open_form_view="true"/>
                                    <field name="expected_price"/>
                                    <field name="state"/>
                                    <control>
                                        <create string="Add a Properites"/>
                                    </control>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- List View -->
    <record id="estate_property_type_view_list" model="ir.ui.view">
        <field name="name">estate.property.type.view.list</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <list editable='bottom' open_form_view="True">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
            </list>
        </field>
    </record>

    <!-- Search View -->
    <record id="estate_property_type_view_search" model="ir.ui.view">
        <field name="name">estate.property.type.view.search</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <separator/>
                <filter string="Type" name="name"/>
                <group>
                    <filter string="Name" name="name" context="{'group_by':'name'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="estate_property_type_action" model="ir.actions.act_window">
        <field name="name">Property Types</field>
        <field name="res_model">estate.property.type</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
