<odoo>
    <record id="estate_property_type_model_action" model="ir.actions.act_window">
        <field name="name">Property Type</field>
        <field name="res_model">estate.property.type</field>
        <field name="view_mode">list,form</field>
        <field name="context">{'search_default_active_filter': True}</field>
    </record>

    <record id="view_estate_property_type_list" model="ir.ui.view">
        <field name="name">estate.property.type.list</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <list string="Properties Type" default_order="sequence">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="description"/>
                <field name="offer_ids" string="Properties"/>
                <field name="offer_count" string="Number of offers"/>
                <field name="status"/>
            </list>
        </field>
    </record>

    <record id="view_estate_property_type_form" model="ir.ui.view">
        <field name="name">estate.property.type.form</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <form string="Property Type">
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button type="action" name="%(action_estate_property_offer)d" class="oe_stat_button" icon="fa-envelope">
                            <field name="offer_count" widget="statinfo" string="Offers"/>
                        </button>
                    </div>
                    <group>
                        <field name="name"/>
                        <field name="description"/>
                        <field name="status"/>
                    </group>
                    <notebook>
                        <page string="Properties">
                            <field name="property_ids">
                                <list string="Properties">
                                    <field name="name"/>
                                    <field name="expected_price"/>
                                    <field name="state"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_estate_property_search" model="ir.ui.view">
        <field name="name">estate.property.type.search</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <search string="Search Properties">
                <field name="name"/>
                <filter string="Active" name="active_filter" domain="[('status','=','active')]"/>
            </search>
        </field>
    </record>
</odoo>