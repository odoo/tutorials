<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Estate property type list view -->
    <record id="view_estate_property_type_list" model="ir.ui.view">
        <field name="name">view.estate.property.type.list</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <list string="List Properties">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
            </list>
        </field>
    </record>

    <!-- Estate property type from view -->
    <record id="view_estate_property_type_form" model="ir.ui.view">
        <field name="name">view.estate.property.type.form</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <form string="Form property type">
                <sheet>
                    <group>
                        <h1>
                            <field name="name"/>
                        </h1>
                    </group>
                    <div name="button_box" class="oe_button_box" position="inside">
                            <button name="%(estate_property_offer_action)d" type="action"
                                    class="oe_stat_button" string="Offers" icon="fa-money">
                                <field name="offer_count" widget="statinfo"/>
                            </button> 
                        </div>
                    <notebook>
                        <page string="Properties" name="estate_property_type_properties">
                            <field name="property_ids">
                                <list>
                                    <field name="name"/>
                                    <field name="expected_price"/>
                                    <field name="state"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Estate property type action -->
    <record id="estate_property_type_action" model="ir.actions.act_window">
        <field name="name">Property Type</field>
        <field name="res_model">estate.property.type</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
