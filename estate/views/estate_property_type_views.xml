<?xml version="1.0"?>
<odoo>
    <record id="estate_property_type_view_search" model="ir.ui.view">
        <field name="name">estate.property.type.view.search</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <search string="Search Property Types">
                <field name="name" />
                <separator />
                <filter string="Livable" name="livable" domain="[('livable', '=', 'True')]" />
                <filter string="Not Livable" name="not_livable" domain="[('livable', '=', 'False')]" />
            </search>
        </field>
    </record>

    <record id="estate_property_type_view_form" model="ir.ui.view">
        <field name="name">estate.property.type.form</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <form string="Property Type">
                <sheet>
                    <div name="button_box" position="attributes">
                        <button class="oe_stat_button"
                            icon="fa-money"
                            name="%(estate.estate_property_type_offer_action)d"
                            type="action">
                            <field name="offer_count" widget="statinfo" string="Offers"/>
                        </button>
                    </div>
                    <div class="oe_title">
                        <h1 class="mb16">
                            <field name="name" placeholder="Apartment.." />
                        </h1>
                    </div>
                    <group>
                        <field name="livable" />
                    </group>
                    <notebook>
                        <page string="Properties">
                            <field name="property_ids">
                                <list string="Properties" sample="1">
                                    <field name="name" />
                                    <field name="expecting_price" options="{'currency_field': 'currency_id'}" />
                                    <field name="selling_price" options="{'currency_field': 'currency_id'}" invisible="stage != 'sold'" />
                                    <field name="stage"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="estate_property_type_view_tree" model="ir.ui.view">
        <field name="name">estate.property.type.view.list</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <list string="Property Types" sample="1">
                <field name="name" />
                <field name="livable" />
            </list>
        </field>
    </record>

    <record id="estate_property_type_action" model="ir.actions.act_window">
        <field name="name">Property Types</field>
        <field name="res_model">estate.property.type</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
