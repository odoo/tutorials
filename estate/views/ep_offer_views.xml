<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="ep_offer_view_list" model="ir.ui.view">
        <field name="name">ep_offer.view.list</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <list string="Property offer">
                <field name="partner_id" string="Buyer"/>
                <field name="price" string="Offered price"/>
                <field name="state" string="Status"/>
                <field name="validity" string="Validity"/>
                <field name="date_deadline" string="Deadline"/>
            </list>
        </field>
    </record>
    <record id="ep_offer_view_form" model="ir.ui.view">
        <field name="name">ep_offer.view.form</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <form string="Property offer">
                <sheet>
                    <group>
                        <!--field name="property_id" string="Property"/-->
                        <field name="partner_id" string="Buyer"/>
                        <field name="price" string="Offered price"/>
                        <field name="validity" string="Validity"/>
                        <field name="date_deadline" string="Deadline"/>
                        <field name="state" string="Status"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
</odoo>