<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Property</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form</field> <!-- By default list and form view mode is given even if we remove this view mode from the code we will still get the list and form view -->
            <field name="view_ids" eval="[
                (5, 0, 0),
                (0, 0, {'view_mode': 'list'}),
                (0, 0, {'view_mode': 'form', 'view_id': ref('estate_property_view_form')})
                ]"
            />
    </record>


    <record id="estate_property_action2" model="ir.actions.act_window">
        <field name="name">Properties2</field>
        <field name="res_model">estate.property</field>

        <field name="view_id" ref="estate_property_wizard_views"/>
        <field name="view_mode">form</field> 
        <field name="target">new</field>
    </record>
</odoo>
