<odoo>
    <record id="estate_property_type_view_form" model="ir.ui.view">
        <field name="name">estate.property.type.view.form</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <!-- <button name="action_see_offers" type="object" string="Sell" class="oe_stat_button" icon="fa-money">
                        <field name="offer_count" string="Offers" widget="statinfo"/>
                    </button> -->
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="estate.estate_property_offer_action" type="action" class="oe_stat_button" icon="fa-money" invisible="offer_count == 0">
                            <field name="offer_count" widget="statinfo" string="Offers Full"/>
                        </button>
                        <button name="action_see_offers" type="object" class="oe_stat_button" icon="fa-money" invisible="offer_count == 0">
                            <field name="offer_count" string="Offers" widget="statinfo"/>
                        </button>
                        <button name="action_pricey_property" type="object" class="oe_stat_button" icon="fa-star" invisible="pricey_count == 0">
                            <field name="pricey_count" string="Pricey" widget="statinfo"/>
                        </button>
                        <button name="action_offer_alert" type="object" class="oe_stat_button" icon="fa-warning" invisible="offer_alert == 0">
                            <field name="offer_alert" string="Uh-0h!" widget="statinfo"/>
                        </button>
                    </div>
                    <h1>
                        <field name="type" placeholder="Add type name"/>
                    </h1>
                        <notebook>
                            <page string="Properties">
                                <field name="property_ids">
                                    <list default_order="expected_price desc">
                                        <field name="name"/>
                                        <field name="expected_price"/>
                                        <field name="state"/>
                                    </list>
                                </field>
                            </page>
                        </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="estate_property_type_action" model="ir.actions.act_window">
        <field name="name">Property Type</field>
        <field name="res_model">estate.property.type</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
