<?xml version="1.0"?>
<odoo>

    <record id="estate_property_offers_action" model="ir.actions.act_window">
        <field name="name">Offers</field>
        <field name="res_model">estate.property.offer</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('property_type_id', '=', active_id)]</field>
    </record>

    <!-- LIST VIEW -->

    <record id="estate_property_offer_list_view" model="ir.ui.view">
        <field name="model">estate.property.offer</field>

        <field name="arch" type="xml">
            <list string="Property" editable="bottom">
                <field name="price"/>
                <field name="partner_id"/>
                <field name="validity"/>
                <field name="property_type_id"/>
                <field name="date_deadline"/>
                <button name="action_accept_offer" type="object" title="Accept" icon="fa-check" invisible="property_id.state in ('sold')"/>
                <button name="action_refuse_offer" type="object" title="Refuse" icon="fa-times" invisible="property_id.state in ('sold')"/>
                <field name="status"/>
            </list>
        </field>
    </record>

    <!-- FORM VIEW -->

    <record id="estate_property_offer_form_view" model="ir.ui.view">
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <form string="Property">
                <sheet>
                    <group>
                        <field name="price"/>
                        <field name="partner_id"/>
                        <field name="property_id"/>
                        <field name="create_date"/>
                        <field name="validity"/>
                        <field name="date_deadline"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
</odoo>
