<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Action For Property Offer -->
    <record id="property_offer_action" model="ir.actions.act_window">
        <field name="name">Property Offers</field>
        <field name="res_model">estate.property.offer</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- List View For Propert Offer -->
    <record id="estate_property_offer_list" model="ir.ui.view">
        <field name="name">estate.property.offer.list</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <list
                string="Property_Offer"
                editable="bottom"
                decoration-success="status == 'accepted'"
                decoration-danger="status == 'refused'"
            >
                <field name="price"/>
                <field name="partner_id"/>
                <field name="validity"/>
                <field name="date_deadline"/>
                <button name="action_accepted" title="Accept" type="object" icon="fa-check" invisible="status in ['accepted', 'refused']" class="o_icon_button"/>
                <button name="action_refused" title="Refuse" type="object" icon="fa-times" invisible="status in ['accepted', 'refused']" class="o_icon_button"/>
            </list>
        </field>
    </record>

    <!-- Form View For Property Offer -->
    <record id="estate_property_offer_form" model="ir.ui.view">
        <field name="name">estate.property.offer.form</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <form string="Property_Offer">
                <sheet>
                    <group>
                        <field name="price"/>
                        <field name="partner_id"/>
                        <field name="status"/>
                        <field name="validity"/>
                        <field name="date_deadline"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
</odoo>
