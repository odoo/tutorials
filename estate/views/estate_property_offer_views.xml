<odoo>
    <data>
        <record id="view_estate_property_offer_list" model="ir.ui.view">
            <field name="name">estate.property.offer.list</field>
            <field name="model">estate.property.offer</field>
            <field name="arch" type="xml">
                <list editable="bottom" decoration-success="status == 'accepted'" decoration-danger="status == 'refused'">
                    <field name="price"/>
                    <field name="partner_id"/>
                    <field name="valid_until"/>
                    <field name="status"/>
                    <button name="action_accept_offer" type="object" icon="fa-check" title="Accept Offer"
                            invisible="partner_id.state in ('sold', 'canceled', 'offer_accepted') or status == 'accepted'"/>
                    <button name="action_refuse_offer" type="object" icon="fa-times" title="Refuse Offer"
                            invisible="partner_id.state in ('sold', 'canceled', 'offer_accepted') or status == 'refused'"/>
                </list>
            </field>
        </record>

        <record id="view_estate_property_offer_form" model="ir.ui.view">
            <field name="name">estate.property.offer.form</field>
            <field name="model">estate.property.offer</field>
            <field name="arch" type="xml">
                <form>
                    <sheet>
                        <group>
                            <field name="property_id" options="{'no_create': True, 'no_open': True}"/>
                            <field name="partner_id" domain="[('is_company','=',False)]"/>
                            <field name="price"/>
                            <field name="valid_until"/>
                            <field name="status"/>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>
    </data>
</odoo>
