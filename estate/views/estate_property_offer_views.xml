<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="estate_property_offer_action" model="ir.actions.act_window">
        <field name="name">Property Offers</field>
        <field name="res_model">estate.property.offer</field>
        <field name="view_mode">list,form,kanban</field>
    </record>

    <record id ="estate_property_offer_list" model="ir.ui.view">
        <field name="name">estate.property.offer.list</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <list string ="offers" editable="bottom" >
                <field name="price"/>
                <field name="status"/>
                <field name="partner_id"/>
                <field name="property_id"/>
                <field name="validity"/>
                <field name="date_deadline"/>
                <!-- <button name="action_accept" string="Confirm" type="object" icon="fa-check"/>
                <button name="action_refuse" string="Cancel" type="object" icon="fa-times"/> -->
            </list>
        </field>
    </record>

    <record id = "estate_property_offer_form" model="ir.ui.view">
        <field name="name">estate.property.offer.form</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <group>
                            <field name="partner_id"/>
                            <field name="price"/>
                            <field name="status"/>
                        </group>
                        <group>
                            <field name="date_deadline"/>
                            <field name="validity"/>
                            <field name="property_id"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
    <record id = "estate_property_offer_kanban" model="ir.ui.view">
        <field name="name">estate.property.offer.kanban</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <kanban>
                <templates>
                    <t t-name="card" >
                        <div t-attf-class="#{!selection_mode ? 'text-centre' : ''}">
                            <i class="fa fa-building" role="img" aria-label="Enterprise"></i>
                            <field class="fw-bold fs-5" name="property_id"/>
                        </div>
                        <hr class="mt-50 mb-50"/>
                        <div class="row" t-if="!selection_mode">
                            <div t-if="record.partner_id.value" class="col-6 text-centre">
                                <strong>Partner: </strong>
                                <field name="partner_id"/>
                            </div>
                            <div t-if="record.price.value" class="col-6 text-centre">
                                <strong>Price: </strong>
                                <field name="price"/>
                            </div>
                            <div t-if="record.status.value" class="col-6 text-centre">
                                <strong>Status: </strong>
                                <field name="status"/>
                            </div>
                            <div t-if="record.validity.value" class="col-6 text-centre">
                                <strong>validity: </strong>
                                <field name="validity"/>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

</odoo>
