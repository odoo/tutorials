<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="estate_offers_view_list" model="ir.ui.view">
        <field name="name">estate.property.offer.tree</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <list editable="bottom"
                decoration-danger="status == 'refused'"
                decoration-success="status == 'accepted'">
                <field name="price" />
                <field name="partner_id" />
                <field name="status" invisible="1" />
                <field name="property_type_id" />
                <button name="action_accept_offer" string="Confirm" type="object" icon="fa-check"
                     />
                <button name="action_refuse_offer" string="Cancel" type="object" icon="fa-times"
                     />
            </list>
        </field>
    </record>

    <record id="estate_property_offer_action" model="ir.actions.act_window">
        <field name="name">Offers</field>
        <field name="res_model">estate.property.offer</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('property_type_id', '=', context.get('default_property_type_id'))]</field>
        <field name="context">{}</field>
        <field name="views" eval="[(ref('estate_offers_view_list'), 'list'), (False, 'form')]" />
    </record>
</odoo>