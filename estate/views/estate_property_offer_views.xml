<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="estate_property_offer_list" model="ir.ui.view">
        <field name="name">estate.property.offer.list</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <list string="Offers" editable="bottom" decoration-success="status == 'accepted'" decoration-danger="status == 'refused'">
                <field name="price" width="180" />
                <field name="partner_id" width="180" />
                <field name="validity" width="180" />
                <field name="date_deadline" width="180" />
                <button name="action_accept" title="Accept" type="object" icon="fa-check" invisible="(status == 'accepted' or status == 'refused')"  />
                <button name="action_reject" title="Reject" type="object" icon="fa-times" invisible="(status == 'accepted' or status == 'refused')"    />
                <field name="status" width="180" />
            </list>
        </field>
    </record>
    <record id="estate_property_offer_form" model="ir.ui.view">
        <field name="name">estate.property.offer.form</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <form string="Offers">
                <sheet>
                    <group>
                        <field name="price" />
                        <field name="partner_id" />
                        <field name="validity" />
                        <field name="date_deadline" />
                    </group>
                </sheet>
            </form>
        </field>
    </record>
    <record id="estate_property_offer_action" model="ir.actions.act_window">
        <field name="name">Offers</field>
        <field name="res_model">estate.property.offer</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('property_type_id', '=', active_id)]</field>
        <field name="context">{'default_property_type_id': active_id}</field>
    </record>
</odoo>