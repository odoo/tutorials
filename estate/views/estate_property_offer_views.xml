<?xml version = "1.0"?>
<odoo>

    <record id="action_estate_property_offer" model="ir.actions.act_window">
        <field name="name">Property Offers</field>
        <field name="res_model">estate.property.offer</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('property_type_id', '=', active_id)]</field>
    </record>

    <!-- List View -->
    <record id="estate_property_offer_view_list" model="ir.ui.view">
        <field name="name">Offers</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <list editable="top" decoration-success='status == "accepted"' decoration-danger='status == "refused"'>
                <field name="price" />
                <field name="partner_id" />
                <field name="validity" />
                <button name="estate_property_offer_action_accept" type="object" icon="fa-check" title="Accept" invisible='status in ["accepted" , "refused"]' />
                <button name="estate_property_offer_action_refuse" type="object" icon="fa-times" title="Refuse" invisible='status in ["accepted" , "refused"]'/>
                <field name="date_deadline"/>
                <field name="status" />
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="estate_property_offer_view_form" model="ir.ui.view">
        <field name="name">Offers</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="price">Price</field>
                        <field name="partner_id">Partner</field>
                        <field name="status">Status</field>
                        <field name="validity">Validity (days)</field>
                        <field name="date_deadline">Deadline</field>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
</odoo>
