<odoo>
    <record id='estate_property_offer_list_view' model='ir.ui.view'>
        <field name="name">estate.property.offer.list</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <list string="Property Offer List" editable="bottom" decoration-danger="status == 'refused'" decoration-success="status == 'accepted'">
                <field name="price"/>
                <field name="partner_id"/>
                <field name="validity"/>
                <field name="date_deadline"/>
                <button name="action_offer_accept_btn" type="object" icon="fa-check" help="Accept Offer" invisible="status"/>
                <button name="action_offer_reject_btn" type="object" icon="fa-times" help="Reject Offer" invisible="status"/>
            </list>
        </field>
    </record>

    <record id="estate_property_offer_form_view" model="ir.ui.view">
        <field name="name">estate.property.offer.form</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <form string="Property Offer Form">
                <sheet>
                    <group>
                        <field name="price"/>
                        <field name="partner_id" />
                        <field name="validity"/>
                        <field name="date_deadline"/>
                        <field name="status" />
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_view_offer" model="ir.actions.act_window">
        <field name="name">Property Type Offers</field>
        <field name="res_model">estate.property.offer</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[("property_id.property_type_id", "=", active_id)]</field>
    </record>
</odoo>
