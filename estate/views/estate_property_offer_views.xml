<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="estate_property_offer_view_list" model="ir.ui.view">
        <field name="name">estate.property.offer.view.list</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <list editable="bottom" decoration-success="status == 'accepted'"
                decoration-danger="status == 'refused'">
                <field name="price" />
                <field name="partner_id" />
                <field name="validity" />
                <field name="date_deadline" />
                <field name="currency_id" invisible="1" /> <!-- required by monetary widget (price) -->
                <button name="action_accept_offer" type="object" title="Accept Offer"
                    icon="fa-check" invisible="status" />
                <button name="action_refuse_offer" type="object" title="Refuse Offer"
                    icon="fa-times" invisible="status" />
                <field name="status" readonly="True" />
            </list>
        </field>
    </record>

</odoo>
