<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="estate_property_offer_listview" model="ir.ui.view">
        <field name="name">esate.property.listview</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <list string="List" editable="bottom" decoration-success = "status == 'accepted'" decoration-danger = "status == 'refused'" >
                <field name="price" />
                <field name="partner_id" />
                <field name="validity" />
                <field name="date_deadline" />
                <button name="action_accept" title="Accept" type="object" icon="fa-check" invisible="status in ['accepted', 'refused'] or property_status in ['offer_accepted', 'sold', 'cancelled']"/>
                <button name="action_reject" title="Refuse" type="object" icon="fa-times" invisible="status in ['accepted', 'refused'] or property_status in ['offer_accepted', 'sold', 'cancelled']"/>
                <field name="status" />
            </list>
        </field>
    </record>

    <record id="estate_property_offer_formview" model="ir.ui.view">
        <field name="name">estate.property.fromview</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <form string="Form">
                <group>
                    <field name="price" />
                    <field name="partner_id" />
                    <field name="validity" />
                    <field name="date_deadline" />
                    <field name="status" />
                </group>
            </form>
        </field>
    </record>

    <record id="action_estate_property_offer" model="ir.actions.act_window">
        <field name="name">Offers</field>
        <field name="res_model">estate.property.offer</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('property_type_id', '=', active_id)]</field>
    </record>
</odoo>
