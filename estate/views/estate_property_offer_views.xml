<?xml version="1.0" encoding="utf-8"?>

<odoo>

    <!-- Estate/Property/Offers List-->
    <record id="estate_property_offer_view_list" model="ir.ui.view">
        <field name="name">estate.property.offer.list</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <list string="Offers list" editable="top" decoration-success="status=='accepted'" decoration-danger="status=='refused'">
                <field name="price" string="Price"/>
                <field name="partner_id" string="Partner" widget="many2one"/>
                <field name="validity" widget="text"/>
                <field name="date_deadline"/>
                <button name="accept_offer" type="object" icon="fa-check" title="Accept Offer" invisible="status in ('refused', 'accepted')"/>
                <button name="reject_offer" type="object" icon="fa-times" title="Reject Offer" invisible="status in ('refused', 'accepted')"/>
                <field name="status" string="Status" widget="text" optional="hide"/>
            </list>
        </field>
    </record>

    <!-- Estate/Property/Offers Form-->
    <record id="estate_property_offer_view_form" model="ir.ui.view">
        <field name="name">estate.property.offer.form</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <form string="Offers">
                <group>
                    <field name="price"/>
                    <field name="partner_id"/>
                    <field name="validity"/>
                    <field name="date_deadline"/>
                    <field name="status"/>
                </group>
            </form>
        </field>
    </record>

    <!-- Configuration/Estate/Property/Offers Action-->
    <record id="estate_property_offer_view_action" model="ir.actions.act_window">
    <field name="name">estate.property.offer.action</field>
    <field name="res_model">estate.property.offer</field>
    <field name="view_mode">list,form</field>
    <field name="domain">[('property_type_id', '=', active_id)]</field>
    </record>

</odoo>