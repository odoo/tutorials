<?xml version='1.0' encoding='UTF-8'?>
<odoo>
    <data noupdate="1">

        <!-- List View -->
        <record id="estate_property_offer_view_list" model="ir.ui.view">
            <field name="name">estate.property.offer.view.list</field>
            <field name="model">estate.property.offer</field>
            <field name="arch" type="xml">
                <list editable='bottom' decoration-success="status == 'accepted'" decoration-danger="status == 'refused'">
                    <field name="price"/>
                    <field name="partner_id"/>
                    <field name="validity"/>
                    <button name="action_accept" type="object" title="Accepted" icon="fa-check" invisible="status == 'accepted' or id.state == 'offer_accepted'"/>
                    <button name="action_refuse" type="object" title="Refused" icon="fa-close" invisible="status == 'accepted'or id.state == 'offer_accepted'"/>
                    <field name="date_deadline"/>
                    <field name="status"/>
                </list>
            </field>
        </record>
    </data>

    <!-- Form View -->
    <record id="estate_property_offer_view_form" model="ir.ui.view">
        <field name="name">estate.property.offer.view.form</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="price"/>
                        <field name="partner_id"/>
                        <field name="validity"/>
                        <field name="date_deadline"/>
                        <field name="property_type_id"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action window -->
    <record id="estate_property_offer_action" model="ir.actions.act_window">
        <field name="name">Property offer</field>
        <field name="res_model">estate.property.offer</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('property_type_id', '=', active_id)]</field>
    </record>
</odoo>
