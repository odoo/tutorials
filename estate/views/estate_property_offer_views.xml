<?xml version="1.0"?>
<odoo>
    <record model="ir.actions.act_window" id="estate_property_offer_action_from_property_type">
        <field name="name">Offer</field>
        <field name="res_model">estate.property.offer</field>
        <field name="view_mode">list</field>
        <field name="domain">[('property_type_id', '=', active_id)]</field>
    </record>

    <record model="ir.ui.view" id="estate_property_offer_view_list">
        <field name="name">Property offer</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <list string="Offers" editable="bottom"
            decoration-success="status=='accepted'"
            decoration-warning="status=='refused'">
                <field name="price"/>
                <field name="partner_id"/>
                <field name="validity"/>
                <field name="date_deadline"/>
                <field name="property_type_id"/>
                <button name="accept_offer" string="Confirm" type="object" icon="fa-check" invisible="status"/>
                <button name="refuse_offer" string="Refuse" type="object" icon="fa-times" invisible="status"/>
                <field name="status" invisible="1"/>
            </list>
        </field>
    </record>

    <record model="ir.ui.view" id="estate_property_offer_view_search">
        <field name="name">estate.property.offer.search</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <search >
                <field name="price"/>
                <field name="partner_id"/>
                <field name="status"/>
            </search>
        </field>
    </record>

    <record model="ir.ui.view" id="estate_property_offer_view_form">
        <field name="name">Property offer</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <form string="Property_form" >
                <sheet>
                    <group>
                        <field name="price"/>
                        <field name="partner_id"/>
                        <field name="validity"/>
                        <field name="date_deadline"/>        
                        <field name="status"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
</odoo>
