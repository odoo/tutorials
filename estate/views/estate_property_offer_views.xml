<?xml version="1.0" encoding="UTF-8"?>

<odoo>

    <!-- ====================================
                Action
    ==================================== -->
    <record id="estate_model_property_offer_view" model="ir.actions.act_window">
        <field name="name">Estate Property Offer</field>
        <field name="res_model">estate.property.offer</field>
        <field name="domain">[('property_type_id', '=', active_id)]</field>
        <field name="view_mode">list,form</field>
        
    </record>

    <!-- ====================================
                List View
    ==================================== -->
    <record id="estate_property_offer_list_view" model="ir.ui.view">
        <field name="name">estate.property.offer.list</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <list string="Offers" editable="bottom" decoration-danger="status == 'refused'" decoration-success="status == 'accepted'">
                <field name="price"/>
                <field name="partner_id"/>
                <field name="validity"/>
                <field name="date_deadline"/>
                <button name="action_property_offer_accept" type="object" string="Accept" icon="fa-check" invisible="status in ('accepted', 'refused')"></button> 
                <button name="action_property_offer_refused" type="object" string="Refuse" icon="fa-times" invisible="status in ('accepted', 'refused')"></button>

                <!-- ============ Remove this fied due to color indecation ===============-->
                <!-- <field name="status" invisible="status in ('accepted', 'refused')"/> -->                   
            </list>
        </field>
    </record>

    <!-- ====================================
                Form View
    ==================================== -->
    <record id="estate_model_property_offer_form_view" model="ir.ui.view">
        <field name="name">estate.property.offer.form</field> 
        <field name="model">estate.property.offer</field> 
        <field name="arch" type="xml">
            <form>
                <group>
                    <field name="price"/>
                    <field name="partner_id"/>
                    <field name="validity"/>
                    <field name="date_deadline"/>
                    <field name="status"/>
                </group>
            </form>
        </field> 
    </record>
    

</odoo>
