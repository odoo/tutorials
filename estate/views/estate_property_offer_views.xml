<odoo>

    <record id="estate_property_offer_view" model="ir.actions.act_window">
        <field name="name">Estate Property Offer</field>
        <field name="res_model">estate.property.offer</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('property_type_id', '=', active_id)]</field>
    </record>

    <record id="estate_property_offer_list_view" model="ir.ui.view">
        <field name="name">estate.property.offer.list</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <list editable="bottom" decoration-danger="status == 'refused'"
                decoration-success="status == 'accepted'">
                <field name="property_id" string="Property"/>
                <field name="property_type_id" />
                <field name="price" />
                <field name="partner_id" />
                <field name="validity" />
                <field name="date_deadline" />
                <!-- <field name="status" invisible='1'/> -->
                <button name="property_action_accept" type="object"
                    string="Accept"
                    icon="fa-check"
                    invisible="status == 'accepted' or status == 'refused'" />
                <button name="property_action_refuse" type="object"
                    string="Refuse"
                    icon="fa-times"
                    invisible="status == 'accepted' or status == 'refused'" />
            </list>
        </field>
    </record>

    <record id="estate_property_offer_form_view" model="ir.ui.view">
        <field name="name">estate_property_offer.view.form</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <form string="Estate Property Offer"
                help="Use options specified in form to list property offer">
                <sheet>
                    <group>
                        <field name="partner_id" string="Buyer" />
                        <field name="validity" string="Validity" />
                        <field name="price" string="Price" />
                        <field name="date_deadline" string="Deadline" />
                        <field name="status" string="Status" class="oe_background_grey" readonly="1" />
                    </group>
                </sheet>
            </form>
        </field>
    </record>

</odoo>
