<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Inherit the Users form view -->
    <record id="view_users_form_inherit" model="ir.ui.view">
        <field name="name">res.users.form.inherit.properties</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <!-- <xpath expr="//notebook" position="inside"> -->
            <page name="preferences" position="after">
                <page string="Properties">

                    <field name="property_ids"/>
                    <!-- <field name="property_ids" widget="one2many_list" mode="tree,form">
                        <tree editable="bottom" decoration-success="state == 'sold'">
                            <field name="name" string="Property Name"/>
                            <field name="expected_price" string="Expected Price"/>
                            <field name="state" string="State"/>
                        </tree>
                    </field> -->
                </page>
            </page>
            <!-- </xpath> -->
        </field>
    </record>
</odoo>
