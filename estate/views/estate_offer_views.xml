<?xml version="1.0"?>
<odoo>
    <record id="estate_offer_action" model="ir.actions.act_window">
        <field name="name">Offers</field>
        <field name="res_model">estate.offer</field>
        <field name="view_mode">list</field>
        <field name="domain">[('property_type_id', '=', active_id)]</field>
    </record>

    <record id="estate_offer_list" model="ir.ui.view">
        <field name="name">Offers</field>
        <field name="model">estate.offer</field>
        <field name="arch" type="xml">
            <list editable="top" decoration-success="status == 'accepted'" decoration-danger="status == 'refused'">
                <field name="price" />
                <field name="partner_id" />
                <field name="validity" />
                <field name="date_deadline" />
                <button type="object" string="Accept" name="set_status_accepted" icon="fa-check" invisible="status != ''" />
                <button type="object" string="Refuse" name="set_status_refused" icon="fa-close" invisible="status != ''" />
                <field name="status" />
            </list>
        </field>
    </record>
</odoo>
