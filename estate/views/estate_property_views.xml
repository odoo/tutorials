<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="estate_property_search_view" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Search Properties">
                <field name="name"/>
                <field name="postcode"/>
                <field name="expected_price"/>
                <field name="bedrooms"/>
                <field name="living_area"
                    filter_domain="[('living_area', '>=', self)]"/>
                <field name="squared_area"
                    filter_domain="[('squared_area', '>', 500)]"/>
                <field name="facades"/>
                <separator/>
                <filter name="available" string="Available" domain="[
                        '|',
                        ('state', '=', 'new'),
                        ('state', '=', 'offer_received')
                    ]" />
                <separator/>
                <filter name="square" string="available" domain="
                    [('squared_area', '>', '500')]"/>
                <filter name="group_by_postcode" string="Postcode" context="{'group_by': 'postcode'}"/>
            </search>
        </field>
    </record>
    <record id="estate_property_list_view" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="Properties"
                decoration-success="state == 'offer_received'"
                decoration-bf="state == 'offer_accepted'"
                decoration-danger="state == 'cancelled'"
                decoration-muted="state == 'sold'">
                <field name="name"/>
                <field name="postcode"/>
                <field name="squared_area"/>
                <field name="tag_ids" widget="many2many_tags"/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="living_area"/>
                <field name="bedrooms"/>
                <field name="date_availability" optional="hide"/>
            </list>
        </field>
    </record>
    <record id="estate_property_kanban_view" model="ir.ui.view">
        <field name="name">estate.property.kanban</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <kanban default_group_by="property_type_id" records_draggable="false">
                <field name="name"/>
                <field name="state"/>
                <field name="expected_price"/>
                <field name="best_price"/>
                <field name="selling_price"/>
                <field name="tag_ids"/>
                <templates>
                    <t t-name="card">
                        <div class="oe_kanban_card">
                            <strong>
                                <field name="name"/>
                            </strong>

                            <p>
                                <strong>Expected Price:</strong>
                                <field name="expected_price"/>
                            </p>
                            <p t-if="record.state.raw_value == 'offer_received'">
                                <strong>Best Price:</strong>
                                <field name="best_price"/>
                            </p>

                            <p t-if="record.state.raw_value == 'offer_accepted'">
                                <strong>Selling Price:</strong>
                                <field name="selling_price"/>
                            </p>

                            <p>
                                <field name="tag_ids" widget="many2many_tags"/>
                            </p>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>
    <record id="estate_property_form_view" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Property">
                    <button
                            name="%(estate_property_visit_action_property)d"
                            type="action"
                            class="oe_stat_button"
                            icon="fa-calendar">

                            <field name="visit_count" widget="statinfo" string="Visits"/>

                    </button>
                    <header>
                        <button name="action_sold"
                                type="object"
                                string="Sold"
                                invisible="state in ['sold', 'cancelled']"/>
                        <button name="action_cancel"
                                type="object"
                                string="Cancel"
                                invisible="state in ['sold', 'cancelled']"/>
                        <button name="best_accept"
                                type="object"
                                string="best"/>
                        <button type="action"
                                name="%(estate_property_request_action_property)d"
                                class="oe_stat_button"
                                icon="fa-wrench">
                            <field name="request_count" widget="statinfo" string="Requests"/>
                        </button>
                        <field name="state"
                            widget="statusbar"
                            statusbar_visible="new, offer_received, offer_accepted, sold, cancelled"/>
                        
                    </header>
                <sheet>
                    
                    <h1>
                        <field name="name"/>
                    </h1>
                    <group>
                        <group>
                            <field name="postcode"/>
                            <field name="property_type_id"
                            options="{'no_create': True, 'no_edit': True}"/>
                            <field name="date_availability"/>
                        </group>
                        <group>
                            <field name="expected_price"/>
                            <field name="selling_price"/>
                            <field name="best_price" readonly="1"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description"/>
                            </group>
                            <group>
                                <field name="tag_ids"
                                    widget="many2many_tags"
                                    options="{'color_field': 'color'}"/>
                            </group>
                            <group>
                                <field name="bedrooms"/>
                                <field name="living_area"/>
                                <field name="facades"/>
                                <field name="garage"/>
                                <field name="garden"/>
                                <field name="garden_area"
                                    invisible="not garden"/>
                                <field name="total_area" readonly="1"/>
                                <field name="garden_orientation"
                                    invisible="not garden"/>
                                <field name="squared_area" />
                            </group>
                        </page>
                        <page string="Offers">
                            <field name="offer_ids" readonly="state in ('offer_accepted', 'sold', 'cancelled')">
                                <list editable="bottom"
                                    decoration-danger="status == 'refused'"
                                    decoration-success="status == 'accepted'">
                                    <field name="price"/>
                                    <field name="partner_id"/>
                                    <field name="validity"/>
                                    <field name="date_deadline"/>   
                                    <field name="status" 
                                        column_invisible="parent.state != 'offer_accepted'"/>
                                    <button name="action_accept"
                                            type="object"
                                            icon="fa-check"
                                            class="btn-success"
                                            title="Accept Offer"
                                            invisible="status in ['accepted','refused'] or parent.state == 'offer_accepted'"/>
                                    <button name="action_refuse"
                                            type="object"
                                            icon="fa-times"
                                            class="btn-danger"
                                            title="Refuse Offer"
                                            invisible="status in ['accepted', 'refused'] or parent.state == 'offer_accepted'"/>
                                </list>
                            </field>
                        </page>
                        <page string="Requests">
                            <field name="request_ids">
                                <list editable="bottom">
                                    <field name="name"/>
                                    <field name="technician_id"/>
                                    <field name="priority"/>
                                    <field name="estimated_price"/>
                                    <field name="actual_price"/>
                                    <field name="request_date"/>
                                    <field name="state"/>
                                    <button name="action_cancel" type="object" icon="fa-times" class="btn-link" title="Cancel"/>
                                </list>
                            </field>
                        </page>
                        <page string="Sales">
                            <group>
                                <field name="buyer_id"/>
                                <field name="user_id"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>
    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form,kanban</field>
        <field name="search_view_id" ref="estate_property_search_view"/>
        <field name="context">{'search_default_available': 1}</field>
    </record>
</odoo>
