<?xml version="1.0"?>
<odoo>
  <record id="estate_property_action" model="ir.actions.act_window">
    <field name="name">Property</field>
    <field name="res_model">estate.property</field>
    <field name="view_mode">tree,form,kanban</field>
    <field name="context">{'search_default_available_filter': True}</field>
    <field name="help" type="html">
      <p class="o_view_nocontent_smiling_face">
        Create New Property
      </p>
    </field>
  </record>

  <record id="view_estate_property_tree" model="ir.ui.view">
    <field name="name">estate.property.tree</field>
    <field name="model">estate.property</field>
    <field name="arch" type="xml">
      <tree string="Properties"
        decoration-success="state == 'offer received' or state == 'offer accepted'"
        decoration-bf="state == 'offer accepted'" decoration-muted="state == 'sold'">
        <header>
          <button name="%(estate_property_offer_wizard_button)d" type="action" string="Add Offer" />
        </header>
        <field name="name" />
        <field name="postcode" />
        <field name="property_type_id" />
        <field name="date_availability" optional="True" />
        <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}" />
        <field name="bedrooms" />
        <field name="expected_price" />
        <field name="selling_price" />
        <field name="state" column_invisible='1' />
      </tree>
    </field>
  </record>

  <record id="estate_property_view_form" model="ir.ui.view">
    <field name="name">estate.property.form</field>
    <field name="model">estate.property</field>
    <field name="arch" type="xml">
      <form string="Property">
        <header>
          <button name="action_sold_button" type="object" string="Sold"
            invisible="state == 'sold' or state == 'cancelled'" />
          <button name="action_cancel_button" type="object" string="Cancelled"
            invisible="state == 'sold' or state == 'cancelled'" />
          <field name="state" widget="statusbar"
            statusbar_visible="new,offer received,offer accepted,sold" />
        </header>
        <div name="button_box">
          <button name="%(offers_print_report_action)d" type="action" class="oe_highlight"
            string="Print" />
        </div>
        <sheet>
          <group>
            <h1>
              <group>
                <field name="name" />
              </group>
            </h1>
            <group></group>
            <group>
              <field name="state" />
              <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}" />
              <field name="property_type_id" options="{'no_create': True, 'no_open': True}" />
              <field name="postcode" />
              <field name="date_availability" />
            </group>
            <group>
              <field name="expected_price" />
              <field name="selling_price" />
              <field name="best_price" />
            </group>
          </group>
          <notebook>
            <page string="Description">
              <group>
                <field string="Bedrooms" name="bedrooms" />
                <field name="living_area" />
                <field name="facades" />
                <field name="garage" />
                <field name="garden" />
                <field name="garden_area" invisible="not garden" />
                <field name="garden_orientation" invisible="not garden" />
                <field name="state" />
                <field name='total_area' />
              </group>
            </page>
            <page string="Offers">
              <group>
                <field name="offer_ids"
                  readonly="state == 'offer accepted' or state == 'sold' or state == 'cancelled'">
                  <tree string="Offers" editable="bottom" decoration-success="status == 'accepted'"
                    decoration-danger="status == 'refused'">
                    <field name="price" />
                    <field name="partner_id" />
                    <field name="status" optional="status == 'accepted' or status == 'refused'" />
                    <button name="action_accept_button" type="object" string="Accept"
                      icon="fa-check" invisible="status == 'accepted' or status == 'refused'" />
                    <button name="action_refuse_button" type="object" string="Refuse"
                      icon="fa-level-down" invisible="status == 'accepted' or status == 'refused'" />
                    <field name="validity" />
                    <field name="date_deadline" />
                  </tree>
                </field>
              </group>
            </page>
            <page string="Other Info">
              <group>
                <field name="seller_id" />
                <field name="buyer_id" />
              </group>
            </page>
          </notebook>
        </sheet>
      </form>
    </field>
  </record>

  <record id="view_estate_property_kanban" model="ir.ui.view">
    <field name="name">estate.property.kanban</field>
    <field name="model">estate.property</field>
    <field name="arch" type="xml">
      <kanban default_group_by="property_type_id" groups_draggable="False" records_draggable="False">
        <field name="name" />
        <field name="tag_ids" />
        <field name="best_price" />
        <field name="selling_price" />
        <field name="state" />
        <templates>
          <t t-name="kanban-box">
            <div class="oe_kanban_global_click">
              <div>
                <strong>
                  <field name="name" />
                </strong>
              </div>
              <div>
                <field name="tag_ids" />
              </div>
              <div>Expected Price: <field name="expected_price" /></div>
              <div t-if="record.state.raw_value == 'offer received'"> Best Offer: <field
                  name="best_price"></field>
              </div>
              <div t-if="record.state.raw_value == 'sold'"> Selling Price: <field
                  name="selling_price"></field>
              </div>
            </div>
          </t>
        </templates>
      </kanban>
    </field>
  </record>

  <record id="estate_property_search" model="ir.ui.view">
    <field name="name">estate.property.search</field>
    <field name="model">estate.property</field>
    <field name="arch" type="xml">
      <search string="Bedroom">
        <field name="name" string="Name" />
        <field name="postcode" string="Postcodes" />
        <field name="expected_price" string="Expected Price" />
        <field name="bedrooms" string="Bedrooms" />
        <field name="living_area" string="Living Area" filter_domain="[('living_area', '>=', self)]" />
        <field name="facades" string="Facades" />
        <separator />
        <filter string="Available" name="available_filter"
          domain="[('state', 'in', ('new', 'offer received'))]" />
        <filter string="Accepted" name="accepted_filter"
          domain="[('state', '=', ('offer accepted'))]" />
        <group expand="0" string="Group By">
          <filter string="Postcode" name="group_by_postcode" context="{'group_by': 'postcode'}" />
          <filter string="Bedrooms" name="group_by_bedrooms" context="{'group_by': 'bedrooms'}" />
        </group>
      </search>
    </field>
  </record>
</odoo>