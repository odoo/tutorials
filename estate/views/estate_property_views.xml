<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Custom List View -->
    <record id="view_estate_property_list" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="Properties">
                <field name="name" width="20%"/>
                <field name="postcode" width="10%"/>
                <field name="bedrooms" width="10%"/>
                <field name="living_area" width="15%"/>
                <field name="expected_price" width="15%"/>
                <field name="selling_price" width="15%"/>
                <field name="date_availability" width="15%"/>
            </list>
        </field>
    </record>

    <!-- Custom Form View -->
    <record id="view_estate_property_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Estate Property">
                <header>
                    <button invisible="state == 'sold' or state == 'cancelled'" name="action_sold" type="object" string="Sold"/>
                    <button invisible="state == 'sold' or state == 'cancelled'" name="action_cancelled" type="object" string="Cancelled"/>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <group>
                        <h1>
                            <field name="name"/>
                        </h1>
                        <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                    </group>
                    <group>
                        <group>
                            <field name="property_type_id" options="{'no_create': true}"/>
                            <field name="postcode"/>
                            <field name="date_availability"/>
                        </group>
                        <group>
                            <field name="expected_price"/>
                            <field name="selling_price"/>
                            <field name="best_offer"/>
                        </group>
                    </group>
                    <notebook>
                        <page name="description" string="Description">
                            <group>
                                <field name="description"
                                    placeholder="When duplicated, status and date are not copied"/>
                                <field name="bedrooms"/>
                                <field name="living_area"/>
                                <field name="facades"/>
                                <field name="garage"/>
                                <field name="garden"/>
                                <field name="garden_area" invisible="garden == False"/>
                                <field name="total_area"/>
                                <field name="garden_orientation" invisible="garden == False"/>
                            </group>
                        </page>

                        <page name="offers" string="Offers">
                            <group>
                                <field name="offer_ids"/>
                            </group>
                        </page>

                        <page name="other_info" string="Other Info">
                            <group>
                                <field name="seller_id"/>
                                <field name="buyer_id"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Custom Search View -->
    <record id="view_estate_property_search" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Tests">
                <field name="name"/>
                <field name="postcode"/>
                <field name="expected_price"/>
                <field name="bedrooms"/>
                <field name="living_area"/>
                <field name="facades"/>
                <filter string="Available" name="available"
                    domain="[('state', 'in', ['new', 'offer received'])]"/>
                <group expand="1" string="Group By">
                    <filter string="Postcode" name="group_by_postcode"
                        context="{'group_by': 'postcode'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Actions -->
    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Real Estate</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">form,list</field>
    </record>
</odoo>
