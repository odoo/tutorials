<?xml version="1.0" encoding="UTF-8"?>
<!-- the root elements of the data file -->
<odoo>
    <record id="estate_advertisement_property_action" model="ir.actions.act_window">
        <field name="name">Property</field>
        <field name="res_model">realestate.properties</field>
        <field name="view_mode">list,form</field>
    </record>

    <record id="estate_property_custom_view" model="ir.ui.view">
        <field name="name">realestate.property.form</field>
        <field name="model">realestate.properties</field>
        <field name="arch" type="xml">
            <form string="Custom description form">
                <sheet>
                    <h1 class="oe_title mb32">
                        <field name="name" />
                    </h1>
                    <group>
                        <field name="property_tag_ids" widget="many2many_tags" class="mb16" />
                        <group>
                            <field name="postcode" />
                            <field name="date_availability" />
                        </group>
                        <group>
                            <field name="expected_price" />
                            <field name="best_offer" />
                            <field name="selling_price" />
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description" />
                                <field name="living_area" />
                                <field name="facades" />
                                <field name="garage" />
                                <field name="garden" />
                                <field name="garden_area" />
                                <field name="garden_orientation" />
                                <field name="property_tag_ids" />
                                <field name="total_area" />
                            </group>
                        </page>
                        <page string="Offers">
                            <field name="offer_ids" />
                        </page>
                        <page string="Other info">
                            <group>
                                <field name="buyer_id" />
                                <field name="sale_rep_id" />
                            </group>
                        </page>
                    </notebook>

                </sheet>
            </form>
        </field>

    </record>

    <record id="estate_property_views_list" model="ir.ui.view">
        <field name="name">realestate.property.list</field>
        <field name="model">realestate.properties</field>
        <field name="arch" type="xml">
            <list string="Channel">
                <field name="name" />
                <field name="postcode" />
                <field name="bedrooms" />
                <field name="living_area" />
                <field name="expected_price" />
                <field name="selling_price" />
                <field name="date_availability" />
            </list>
        </field>
    </record>

    <record id="estate_property_views_filter" model="ir.ui.view">
        <field name="name">realestate.property.filter</field>
        <field name="model">realestate.properties</field>
        <field name="arch" type="xml">
            <search string="Filter string">
                <field name="name" />
                <field name="bedrooms" />
                <field name="facades" />
                <field name="postcode" />
                <field name="living_area" />
                <field name="expected_price" />
                <separator />
                <filter string="Available properties" name="available"
                    domain="['|', ('state', '=', 'new'), ('state', '=', 'offer received')]" />
                <group>
                    <filter string="Group by postcode" name="postcode"
                        context="{'group_by': 'postcode'}" />
                </group>
            </search>
        </field>
    </record>

</odoo>