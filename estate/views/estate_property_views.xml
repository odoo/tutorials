<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ===================== -->
    <!-- PROPERTY KANBAN VIEW -->
    <!-- ===================== -->
    <record id="estate_property_view_kanban" model="ir.ui.view">
        <field name="name">estate.property.kanban</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">

            <kanban default_group_by="property_type_id"
                    records_per_row="4"
                    quick_create="0"
                    draggable="0">

                <field name="name"/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="state"/>
                <field name="property_type_id"/>
                <field name="best_price"/>
                <field name="tag_ids"/>

                <templates>
                    <t t-name="card">
                        <div class="oe_kanban_card oe_kanban_global_click">

                            <!-- Title -->
                            <strong>
                                <field name="name"/>
                            </strong>

                            <!-- Property Type -->
                            <div>
                                <field name="property_type_id"/>
                            </div>

                            <!-- Tags -->
                            <div>
                                <field name="tag_ids" widget="many2many_tags"/>
                            </div>

                            <!-- Expected Price -->
                            <div>
                                Expected: <field name="expected_price"/>
                            </div>

                            <!-- Best Offer -->
                            <div>
                                Best Offer:
                                <span t-if="record.best_price.raw_value &gt; 0 and record.state.raw_value != 'sold'">
                                    <field name="best_price"/>
                                </span>
                                <span t-else="">-</span>
                            </div>

                            <!-- Selling Price -->
                            <div t-if="record.state.raw_value == 'sold'">
                                Selling Price: <field name="selling_price"/>
                            </div>

                            <!-- State -->
                            <div>
                                <span t-if="record.state.raw_value == 'sold'">Sold</span>
                                <span t-elif="record.state.raw_value == 'offer_accepted'">Accepted</span>
                                <span t-elif="record.state.raw_value == 'offer_received'">Offers</span>
                                <span t-elif="record.state.raw_value == 'cancelled'">Cancelled</span>
                                <span t-else="">New</span>
                            </div>

                        </div>
                    </t>
                </templates>

            </kanban>

        </field>
    </record>

    <!-- ===================== -->
    <!-- PROPERTY LIST VIEW -->
    <!-- ===================== -->
    <record id="estate_property_view_list" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list
                decoration-success="state in ('offer_received','offer_accepted')"
                decoration-bf="state == 'offer_accepted'"
                decoration-muted="state == 'sold'"
            >
                <field name="name"/>
                <field name="postcode"/>
                <field name="property_type_id"/>
                <field name="expected_price"/>
                <field name="best_price"/>
                <field name="total_area"/>
                <field name="selling_price"/>
                <field name="buyer_id"/>
                <field name="state"/>
            </list>
        </field>
    </record>

    <!-- ===================== -->
    <!-- PROPERTY FORM VIEW -->
    <!-- ===================== -->
    <record id="estate_property_view_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form>

                <header>
                    <button name="action_sold"
                            type="object"
                            string="Mark as Sold"
                            class="btn-primary"
                            invisible="state != 'offer_accepted'"/>

                    <button name="action_cancel"
                            type="object"
                            string="Cancel"
                            class="btn-secondary"
                            invisible="state in ['sold', 'cancelled']"/>

                    <field name="state"
                           widget="statusbar"
                           statusbar_visible="new,offer_received,offer_accepted,sold,cancelled"/>
                </header>

                <sheet>

                    <div class="alert alert-info"
                         invisible="state != 'sold'">
                        <strong>This property has been SOLD</strong>
                    </div>

                    <div class="alert alert-warning"
                         invisible="state != 'cancelled'">
                        <strong>This property listing has been CANCELLED</strong>
                    </div>

                    <h1>
                        <field name="name"/>
                    </h1>

                    <group>
                        <group>
                            <field name="postcode"/>
                            <field name="date_availability"/>
                        </group>

                        <group>
                            <field name="expected_price"/>
                            <field name="selling_price"/>
                        </group>
                    </group>

                    <group>
                        <field name="property_type_id"
                               options="{'no_create': True, 'no_open': True}"/>
                        <field name="tag_ids"
                               widget="many2many_tags"
                               options="{'color_field': 'color'}"/>
                    </group>

                    <notebook>

                        <page string="Description">
                            <field name="description"/>
                        </page>

                        <page string="Details">
                            <group>
                                <field name="bedrooms"/>
                                <field name="living_area"/>
                                <field name="facades"/>
                                <field name="garage"/>
                                <field name="garden"/>
                                <field name="garden_area" invisible="not garden"/>
                                <field name="garden_orientation" invisible="not garden"/>
                                <field name="total_area"/>
                                <field name="price_per_sqm"/>
                                <field name="best_price"/>
                            </group>
                        </page>

                        <page string="Sales">
                            <group>
                                <field name="seller_id"/>
                                <field name="buyer_id"/>
                            </group>
                        </page>

                        <page string="Offers">
                            <field name="offer_ids"
                                   readonly="state in ['offer_accepted', 'sold', 'cancelled']">

                                <list editable="bottom"
                                      decoration-danger="status == 'refused'"
                                      decoration-success="status == 'accepted'">

                                    <field name="price"/>
                                    <field name="partner_id"/>
                                    <field name="status" invisible="1"/>
                                    <field name="validity"/>
                                    <field name="date_deadline"/>

                                </list>

                            </field>
                        </page>

                    </notebook>

                </sheet>
            </form>
        </field>
    </record>

    <!-- ===================== -->
    <!-- SEARCH VIEW -->
    <!-- ===================== -->
    <record id="estate_property_view_search" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="postcode"/>
                <field name="property_type_id"/>
                <field name="buyer_id"/>

                <field name="living_area"
                       filter_domain="[('living_area', '>=', self)]"/>

                <filter string="Available"
                        name="available"
                        domain="[('state','in',['new','offer_received'])]"/>
            </search>
        </field>
    </record>

    <!-- ===================== -->
    <!-- ACTION -->
    <!-- ===================== -->
    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form,kanban</field>
        <field name="context">{'search_default_available': 1}</field>
    </record>

</odoo>