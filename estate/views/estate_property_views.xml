<?xml version="1.0"?>

<odoo>

    <record id="estate_property_views_action" model="ir.actions.act_window">
        <field name="name">Estate Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form,kanban</field>
        <field name="domain">[]</field>
        <field name = "context">{'search_default_availible_property': 1}</field>

    </record>

    <record id="estate_property_views_list" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="Properties" decoration-success="state == 'offer_received' or state == 'offer_accepted'" decoration-bf="state == 'offer_accepted'" decoration-muted="state == 'sold'">
                <field name="name" />
                <field name="property_type_id" options="{'no_create': true, 'no_edit': true, 'no_open': true}"/>
                <field name="postcode"/>
                <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                <field name="bedrooms"/>
                <field name="living_area"/>
                <field name="expected_price"/>
                <field name="selling_price"/>
            </list>
        </field>
    </record>

    <record id="estate_property_view_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Estate Property">
                <header>
                    <button name="action_mark_property_sold" class="btn btn-success" type="object" string="Sold" invisible="state == 'cancelled' or state == 'sold'" />
                    <button name="action_mark_property_cancelled" class="btn btn-danger" type="object" string="Cancel" invisible="state == 'sold' or state == 'cancelled'" />
                    <field name="state" type ="object" widget="statusbar" statusbar_visible="new,offer_received,offer_accepted,sold,cancelled"/>

                </header>
                <sheet>
                    <group>
                        <group>
                            <h1>
                                <lable>Name</lable>
                            </h1>
                            <div>
                                <h1>
                                    <field name="name" string="Name" />
                                </h1>
                            </div>
                            <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                            <field name="state" />
                            <field name="postcode" />
                            <field name="date_availability" invisible="true" />
                        </group>
                        <group>
                            <field name="expected_price" />
                            <field name="selling_price" />
                            <field name="property_type_id" string="Property Type" options="{'no_create': true}"/>
                            <field name="best_price" />
                        </group>

                    </group>
                    <notebook>
                        <page string="Description">
                            <group>
                                <group>
                                    <field name="description" />
                                    <field name="company_id"/>
                                    <field name="bedrooms" />
                                    <field name="garage" />
                                    <field name="garden" />
                                    <field name="facades" />
                                    <field name="active" />
                                    <field name="living_area" />
                                    <field name="total_area" />
                                </group>
                                <group>
                                    <group invisible="not garden">
                                        <field name="garden_area" />
                                        <field name="garden_orientation" />
                                    </group>
                                </group>
                            </group>
                        </page>

                        <page string="Offers">
                            <field name="offer_ids" string="Offers" class="mb16" readonly="state in ('offer_accepted','cancelled','sold') "/>
                        </page>

                        <page string="Other Info">
                            <group>
                                <field name="buyer_id" />
                                <field name="salesperson_id" />
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="estate_property_search" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Estate Property">
                <field name="name" />
                <field name="postcode" />
                <field name="expected_price" />
                <field name="bedrooms" />
                <field name="living_area" />
                <field name="facades" />
                <field string="Living Area (sqm)" name="living_area" filter_domain="['>=', ('area'), (self)]"/>
                <filter name="availible_property" string="Avalible" domain="[('state', 'in', ('offer_received','new'))]"/>
                <filter string="With Garden" name="garden" domain="[('garden', '=', True)]" />
                <filter string="Not Active" name="garden" domain="[('active', '=', False)]" />
                <filter string="State" name="state" context="{'group_by':'state'}"/>
                <filter string="Postcode" name="postcode" context="{'group_by':'postcode'}" />
                <filter string="Tags" name="tag_ids" context="{'group_by':'tag_ids'}" />
            </search>
        </field>
    </record>

    <record id="estate_property_view_kanban" model="ir.ui.view">
        <field name="name">estate.property.kanban</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <kanban default_group_by="property_type_id" records_draggable="false">
                <field name="state" />
                <templates>
                    <t t-name="card">
                        <div>
                            <field name="name" />
                        </div>
                        <div class="mb-16">
                            Expected Price:
                            <field name="expected_price" />
                        </div>
                        <div t-if="record.state.raw_value == 'offer_received'">
                            Best Price:
                            <field name="best_price" />
                        </div>
                        <div t-if="record.state.raw_value == 'offer_accepted'">
                            Selling Price:
                            <field name="selling_price" />
                        </div>
                        <div>
                            <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}" />
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>
</odoo>
