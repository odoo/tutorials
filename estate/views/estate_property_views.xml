<?xml version="1.0"?>
<odoo>

    <record id="estate_model_action" model="ir.actions.act_window">
        <field name="name">Estate Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form,kanban</field>
        <field name="context">{'search_default_active': True, 'search_default_current': True}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                    No Properties to Display.
            </p>
            <p>
                    Please Click on New tab to create an opportunity.
            </p>
        </field>
    </record>

    <record id="estate_property_action_add_offer" model="ir.actions.server">
        <field name="name">Add Offer</field>
        <field name="model_id" ref="model_estate_property"/>
        <field name="binding_model_id" ref="model_estate_property"/>
        <field name="state">code</field>
        <field name="code">
            action = model.action_add_offer()
        </field>
    </record>

    <record id="estate_property_view_tree" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list decoration-muted="state=='sold'" decoration-success="(state=='offer_received' or state=='offer_accepted' ) 
            and state!='sold'" decoration-bf="state=='offer_accepted' and state!='sold'" string="Channel">
                <field name="name" string="Title"/>
                <field name="postcode"/>
                <field name="bedrooms"/>
                <field name="living_area"/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="date_availability" invisible="1" string="Available From"/>
            </list>
        </field>
    </record>

    <record id="estate_property_view_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Estate Property Details">
                <group>
                    <group>
                        <button invisible="state=='sold' or state=='cancelled'" name="action_change_state" type="object" string="Sold" context="{'param_name': 'sold'}"/>
                        <button invisible="state=='sold' or state=='cancelled'" name="action_change_state" type="object" string="Cancel" context="{'param_name': 'cancelled'}"/>
                    </group>
                    <group>
                        <field name="state" widget="statusbar" statusbar_visible="new,offer_received,offer_accepted,sold" string=" "/>
                    </group>
                </group>
                <sheet>
                    <group>
                        <h1 class="mb32">
                            <field name="name" class="mb16"/>
                        </h1>
                        <field editable="bottom" name="tag_ids" widget="many2many_tags" string=" " options="{'color_field': 'color'}"/>
                    </group>

                    <group>
                        <group>
                            <field name="postcode"/>
                            <field name="property_type_id" options="{'no_create_edit':true}"/>
                            <field name="date_availability"/>
                        </group>
                        <group>
                            <field name="expected_price"/>
                            <field name="selling_price"/>
                            <field name="best_price"></field>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description"/>
                                <field name="bedrooms"/>
                                <field name="living_area"/>
                                <field name="facades"/>
                                <field name="garage"/>
                                <field name="garden"/>
                                <field invisible="not garden" name="garden_area"/>
                                <field invisible="not garden" name="garden_orientation"/>
                                <field name="total_area"></field>
                            </group>
                        </page>
                        <page string="Offers">
                            <group>
                                <field name="offer_ids" string=" " readonly="state in ['offer_accepted', 'sold', 'cancelled']">
                                    <list decoration-danger="status=='refused'" decoration-success="status=='accepted'" string="Channel" editable="bottom">
                                        <field name="price"/>
                                        <field name="partner_id"/>
                                        <button invisible="status" name="action_change_offer_state" type="object" title="Accept Offer" icon="fa-check" context="{'param_name': 'accepted'}"/>
                                        <button invisible="status" name="action_change_offer_state" type="object" title="Refuse Offer" icon="fa-times" context="{'param_name': 'refused'}"/>
                                        <field name="validity"/>
                                        <field name="date_deadline"/>
                                    </list>
                                </field>

                            </group>
                        </page>
                        <page string="Other Info">
                            <group>
                                <field name="user_id"/>
                                <field name="buyer_id"/>
                            </group>
                        </page>

                    </notebook>
                </sheet>
                <chatter reload_on_post="True"/>
            </form>
        </field>
    </record>

    <record id="estate_property_view_kanban" model="ir.ui.view">
        <field name="name">estate.property.kanban</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <kanban default_group_by="property_type_id" records_draggable="false">
                <templates>
                    <t t-name="card">
                        <div>
                            <field name="name"/>
                            <field name="state" invisible="1"/>
                            <div>
                                <strong>Expected Price:</strong>
                                <field name="expected_price"/>
                            </div>
                            <t t-debug="pdb"/>
                            <div t-if="record.state.raw_value == 'offer_received'">
                                <strong>Best Price:</strong>
                                <field name="best_price"/>
                            </div>
                            <div t-if="record.state.raw_value == 'offer_accepted' or record.state.raw_value == 'sold'">
                                <strong>Selling Price:</strong>
                                <field name="selling_price"/>
                            </div>
                            <div>
                                <field name="tag_ids" widget="many2many_tags"/>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>
    
    <record id="view_delivery_carrier_search" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Estate Property Search">
                <field name="name" string="Title"/>
                <field name="postcode"/>
                <field name="expected_price"/>
                <field name="bedrooms"/>
                <field name="living_area" filter_domain="[('living_area', '>=', self)]"/>
                <field name="facades"/>
                <separator/>
                <filter name="active" string="Available" domain="[('active', '=', True)]"/>

                <filter string="Property Status" name="state" domain="['|',('state', '=', 'new'), ('state', '=', 'offer_received')]"/>
                <group expand="1" string="Group By">
                    <filter string="Property State" name="state" context="{'group_by':'state'}"/>
                    <filter string="Postcode" name="postcode" context="{'group_by':'postcode'}"/>
                </group>
            </search>
        </field>
    </record>
</odoo>
