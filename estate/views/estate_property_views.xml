<?xml version="1.0"?>
<odoo>
    <record id="estate_default_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form</field>
    </record>

    <record id="estate_list_view" model="ir.ui.view">
        <field name="name">Estate List</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list>
                <field name="name" string="Title"/>
                <field name="property_type_id" string="Type"/>
                <field name="postcode" string="Postcode"/>
                <field name="bedrooms" string="Bedrooms"/>
                <field name="living_area" string="Living Area (sqm)"/>
                <field name="expected_price" string="Expected Price"/>
                <field name="selling_price" string="Selling Price"/>
                <field name="date_availability" string="Available From"/>
            </list>
        </field>
    </record>

    <record id="estate_form_view" model="ir.ui.view">
        <field name="name">Properties</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="New House">
                <sheet>
                    <h1>
                        <field name="name" placeholder="New House"/>
                    </h1>
                    <div class="mb-3">
                        <field name="tag_ids" widget="many2many_tags"/>
                    </div>
                    <div class="row">
                        <div class="col">
                            <group>
                                <field name="postcode" string="Postcode"/>
                                <field name="date_availability" string="Available From"/>
                            </group>
                        </div>
                        <div class="col">
                            <group>
                                <field name="expected_price" string="Expected Price"/>
                                <field name="selling_price" string="Selling Price"/>
                            </group>
                        </div>
                    </div>

                    <notebook>
                        <page string="Description">
                            <div class="row w-50">
                                <div class="col">
                                    <group>
                                        <field name="description" string="Description"/>
                                        <field name="property_type_id" string="Type"/>
                                        <field name="bedrooms" string="Bedrooms"/>
                                        <field name="living_area" string="Living Area (sqm)"/>
                                        <field name="garage" string="Garage"/>
                                        <field name="garden" string="Garden"/>
                                        <field name="garden_area" string="Garden Area (sqm)"/>
                                        <field name="garden_orientation" string="Garden Orientation"/>
                                        <field name="state" string="State"/>
                                    </group>
                                </div>
                            </div>
                        </page>
                        <page string="Offers">
                            <list>
                                <field name="offer_ids" string="Offer"/>
                            </list>
                        </page>
                        <page string="Other info">
                            <div class="row w-50">
                                <div class="col">
                                    <group>
                                        <field name="salesperson_id" string="Salesman"/>
                                        <field name="buyer_id" string="Buyer"/>
                                    </group>
                                </div>
                            </div>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="estate_search" model="ir.ui.view">
        <field name="name">Properties</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search>
                <field name="name" string="Title"/>
                <field name="postcode"/>
                <field name="expected_price"/>
                <field name="bedrooms"/>
                <field name="living_area" string="Living Area (sqm)"/>
                <field name="facades"/>
                <filter string="Available properties" name="state" domain="['|',('state','=','new'),('state','=','offer_recieved')]"/>
                <filter string="Postcode" name="postcode" context="{'group_by':'postcode'}"/>
            </search>
        </field>
    </record>
</odoo>