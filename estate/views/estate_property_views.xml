<?xml version="1.0"?>
<odoo>
    <record id="estate_property_view_tree" model="ir.ui.view">
        <field name="name">estate.property.view.tree</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="Tests" decoration-bf="state=='offert accepted'" decoration-success="state=='offer accepted' or state=='offer received'" decoration-muted="state=='sold'">
                <field name="name" string="Title"/>
                <field name="postcode"/>
                <field name="bedrooms"/>
                <field name="living_area" string="Living Area (sqm)"/>
                <field name="expected_price" string="Expected Price"/>
                <field name="selling_price" string="Selling Price"/>
                <field name="date_availability" string="Available From" optional="True"/>
                <field name="property_type_id" string="Type"/>
                <field name="state" column_invisible="1"/>
            </list>
        </field>
    </record>

    <record id="estate_property_view_kanban" model="ir.ui.view">
        <field name="name">estate.property.view.kanban</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <kanban records_draggable='False' default_group_by="property_type_id">
                <field name="state"/>
                <templates>
                    <t t-name="card">
                        <div>
                            <p>
                                <field name="name" class="fw-bold"/>
                            </p>
                            <p>
                                Exprected Price:
                                <field name="expected_price"/>
                            </p>
                            <p>
                                <div t-if="record.state.raw_value == 'offer received'">
                                    Best Offer:
                                    <field name="best_price"/>
                                </div>
                            </p>
                            <p>
                                <div t-if="record.state.raw_value == 'offer accepted'">
                                    Selling Price:
                                    <field name="selling_price"/>
                                </div>
                            </p>
                            <p>
                                <field name="tag_ids" widget="many2many_tags" string="" options="{'color_field': 'color'}"/>
                            </p>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="estate_property_search" model="ir.ui.view">
        <field name="name">estate.property.seach</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Test">
                <field name="name" string="Title"/>
                <field name="postcode"/>
                <field name="expected_price" string="Expected Price"/>
                <field name="bedrooms"/>
                <field name="living_area" string="Living Area (sqm)" filter_domain="[('living_area', '>=', self)]"/>
                <field name="facedes"/>
                <field name="property_type_id" string="Type"/>

                <filter name="filter_state_available" string="Available" 
                        domain="[
                            '|', 
                                ('state', '=', 'new'), ('state', '=', 'offer received')]"/>

                <filter string="Postcode" name="groupby_postcode" context="{'group_by':'postcode'}"/>

            </search>
        </field>
    </record>

    <record id="estate_property_view_form" model="ir.ui.view">
        <field name="name">estate.property.view.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_sold" type="object" string="Sold" invisible="state == 'sold' or state == 'cancelled'"/>
                    <button name="action_cancel" type="object" string="Cancel" invisible="state == 'sold' or state == 'cancelled'"/>
                    <field name="state" widget="statusbar" statusbar_visible="new,offer received,offer accepted,sold" options="{'clickable': 1}"/> 
                </header>
                <sheet>
                    <group>
                        <div class="oe_title">
                            <h1><field name="name"/></h1>
                        </div>
                        <field name="tag_ids" widget="many2many_tags" string="" options="{'color_field': 'color'}"/>
                    </group>
                     <separator/>
                    <group>
                        <group>
                            <field name="state"/>
                            <field name="property_type_id" string="Property Type" options="{'no_create':true}"/>
                            <field name="postcode"/>
                            <field name="expected_price" string="Expected Price"/>
                        </group>
                        <group>
                            <field name="date_availability" string="Available From"/>
                            <field name="best_price" string="Best Offer"/>
                            <field name="selling_price" string="Selling Price"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description"/>
                                <field name="bedrooms"/>
                                <field name="living_area" string="Living Area (sqm)"/>
                                <field name="garage" />
                                <field name="garden" />
                                <field name="garden_area" string="Garden Area (sqm)" invisible="not garden"/>
                                <field name="garden_orientation" string="Garden Orientation" invisible="not garden"/>
                                <field name="total_area" string="Total Area"/>
                            </group>
                        </page>
                        <page string="Offers">
                            <group>
                                <field name="offer_ids" string="" readonly="state=='sold' or state=='cancelled' or state=='offer accepted'"/>
                            </group>
                        </page>
                        <page string="Other Info">
                            <group>
                                <field name="sale_id" string="Salesman"/>
                                <field name="buyer_id" string="Buyer"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Estate Property</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="context"> {'search_default_filter_state_available': True}</field>
    </record>
</odoo>