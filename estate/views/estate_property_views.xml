<?xml version="1.0"?>
<odoo>
    <record id="estate_property_view_kanban" model="ir.ui.view">
        <field name="name">estate.property.kanban</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <kanban records_draggable="False" default_group_by="property_type_id" edit="False">
                <templates>
                    <t t-name="card">
                        <div class="col g-0">
                            <field name='name' class="fw-bolder"/>
                            <field name="expected_price" invisible="1"/>
                            <div>
                                Expected Price: <field name="expected_price"/>
                            </div>
                            <div t-if="record.best_price.value > 0">
                                Best Offer: <field name="best_price"/>
                            </div>
                            <div t-if="record.selling_price.value > 0">
                                Selling Price: <field name="selling_price"/>
                            </div>
                            <field name="tag_ids"/>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="estate_property_view_search" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Properties Search">
                <field name="property_type_id" string="Property Type"/>
                <field name="name" string="Property Name"/>
                <field name="post_code" string="Post Code"/>
                <field name="expected_price" string="Expected Price"/>
                <field name="bedrooms" string="Bedrooms"/>
                <field name="facades" string="Facades"/>
                <field name="living_area" string="Living Area" filter_domain="[('living_area', '>=', self)]"/>
                <separator/>
                <filter name="availability" string="Availability" domain="[('date_availability', '&gt;', context_today())]"/>
                <group expand="2" string="Group By">
                    <filter name="post_code" string="Post Code" context="{'group_by':'post_code'}"/>
                    <filter name="property_type_id" string="Property Type" context="{'group_by':'property_type_id'}"/>
                </group> 
            </search>
        </field>
    </record>

    <record id="estate_property_view_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Property Form">
                <sheet>
                    <header>
                        <button name="action_set_sold" type="object" string="Sold" class="btn-primary" invisible="not (state in ('new', 'offer received', 'offer accepted'))"/>
                        <button name="action_set_cancel" type="object" string="Cancel" class="btn-secondary" invisible="not (state in ('new', 'offer received'))"/>
                        <field name="state" widget="statusbar" string="State" statusbar_visible="new,offer received,offer accepted,sold"/>
                    </header>
                    <div>
                        <h1>
                            <field name="name" string="Title"/>
                        </h1>
                    </div>
                    <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                    <group>
                        <group>
                            <field name="property_type_id" string="Property Type" options="{'no_create': true}"/>
                            <field name="post_code" string="Post Code"/>
                            <field name="date_availability" string="Available From"/>
                        </group>
                        <group>
                            <field name="best_price" string="Best Price"/>
                            <field name="expected_price" string="Expected Price"/>
                            <field name="selling_price" string="Selling Price"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description" string="Description"/>
                                <field name="bedrooms" string="Bedrooms"/>
                                <field name="living_area" string="Living Area (sqm)"/>
                                <field name="facades" string="Facades"/>
                                <field name="garage" string="Garage"/>
                                <field name="garden" string="Garden"/>
                                <field name="garden_area" string="Garden Area (sqm)" invisible="not garden"/>
                                <field name="garden_orientation" string="Garden Orientation" invisible="not garden"/>
                                <field name="total_area" string="Total Area"/>
                            </group>
                        </page>
                        <page string="Offers">
                            <field name="offer_ids" editable="bottom" readonly="state in ('sold', 'canceled', 'offer accepted')">
                                <list editable="bottom" decoration-success="status == 'accepted'" decoration-danger="status == 'refused'">
                                    <field name="price"/>
                                    <field name="validity"/>
                                    <field name="date_deadline"/>
                                    <field name="partner_id"/>
                                    <field name="property_type_id"/>
                                    <button name="action_set_accept" type="object" icon="fa-check" title="accept" invisible="status in ('accepted', 'refused')"/>
                                    <button name="action_set_refuse" type="object" icon="fa-times" title="refuse" invisible="status in ('accepted', 'refused')"/>
                                </list>
                            </field>
                        </page>
                        <page string="Other Info">
                            <group>
                                <field name="buyer_id" string="Buyer"/>
                                <field name="salesperson_id" string="Salesperson"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="estate_property_view_list" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="Properties" decoration-success="state in ['offer received', 'offer accepted']" decoration-bf="state == 'offer accepted'" decoration-muted="state == 'sold'">
                <field name="name" string="Title"/>
                <field name="post_code" string="Postcode"/>
                <field name="bedrooms" string="Bedrooms"/>
                <field name="living_area" string="Living Area (sqm)"/>
                <field name="expected_price" string="Expected Price"/>
                <field name="selling_price" string="Selling Price"/>
                <field name="date_availability" string="Available From" optional="True"/>
                <field name="property_type_id" string="Property Type"/>
                <field name="state" string="State"/>
            </list>
        </field>
    </record>

    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="context">{'search_default_availability': True}</field>
        <field name="view_mode">list,form,kanban</field>
    </record>
</odoo>
