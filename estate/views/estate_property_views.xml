<?xml version='1.0' encoding='UTF-8'?>
<odoo>
    <!-- LIST View-->
    <record id="estate_view_list" model="ir.ui.view">
        <field name="name">estate.view.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list decoration-success="state == 'offer_received' or state == 'offer_accepted'" decoration-bf="state == 'offer_accepted'" decoration-muted="state == 'sold'">
                <field name="best_price" />
                <field name="name" />
                <field name="postcode"/>
                <field name="tags_ids"/>
                <field name="date_availability" optional='{"hide":true}'/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="bedrooms"/>
                <field name="living_area"/>
                <field name="garden_orientation"/>
                <field name="create_date"/>
                <field name="write_date"/>
            </list>
        </field>
    </record>

    <!-- FORM View-->
    <record id="estate_view_form" model="ir.ui.view">
        <field name="name">estate.view.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button string="Sold" name="action_sold" type="object"/>
                    <button string="Cancel" name="action_cancel" type="object"/>
                    <field name="state" widget="statusbar" statusbar_visible="new,offer_received,offer_accepted,sold" decoration-success="state == 'offer_received'"/>
                    <!-- <button type="action" name="%(estate.estate_property_type_action)d" string="Property Types" icon="fa-magic"/> -->
                </header>
                <sheet>
                    <h1>
                        <field name="name"/>
                    </h1>
                    <group>
                        <group>
                            <field name="tags_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                            <field name="property_type_id" options="{'no_create_edit':true}"/>
                            <field name="postcode"/>
                            <field name="date_availability"/>
                        </group>
                        <group>
                            <field name="expected_price"/>
                            <field name="best_price"/>
                            <field name="selling_price"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description"/>
                                <field name="bedrooms"/>
                                <field name="living_area"/>
                                <field name="facades"/>
                                <field name="garage"/>
                                <field name="garden"/>
                                <field name="garden_area" invisible="not garden"/>
                                <field name="garden_orientation" invisible="not garden"/>
                                <field name="total_area" decoration-success="total_area == 100"/>
                            </group>
                        </page>
                        <page string="Offers">
                            <group>
                                <field name="offer_ids" nolabel="1" readonly= "state in 'offer_accepted', 'sold', 'cancelled'"/>
                            </group>
                        </page>
                        <page string="Other Info">
                            <group>
                                <field name="seller_id"/>
                                <field name="buyer_ids"/>
                            </group>
                        </page>
                        <page string="Maintenance Requests">
                            <group>
                                <field name="property_maintenance_requests" nolabel="1">
                                    <list editable="bottom">
                                        <field name="title"/>
                                        <field name="cost"/>
                                        <field name="status"/>
                                    </list>
                                </field>
                            </group>
                            <group>
                                <field name="total_maintenance_cost"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Search View -->
    <record id="estate_view_search" model="ir.ui.view">
        <field name="name">estate.view.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="postcode"/>
                <field name="expected_price"/>
                <field name="bedrooms"/>
                <field name="living_area" string="living_area" filter_domain="[('living_area', '&gt;=', self)]"/>
                <field name="facades"/>
                <separator/>
                <filter string="New / Offer Received" name="state" domain="[('state', 'in', ['new', 'offer received'])]"/>
                <separator/>
                <filter string="Best 50" name='best_price' domain="[('best_price', '&gt;', 50)]"/>
                <filter name='archive' domain="[('active', '=', 'False')]"/>
                <group>
                    <filter string="Postcode" name="postcode" context="{'group_by':'postcode'}"/>
                </group>
            </search>
        </field>
    </record>


    <record id="estate_model_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form</field>
        <field name="context">{'search_default_best_price': 0, 'search_default_state': 0}</field>
    </record>
</odoo>
