<?xml version="1.0" encoding="utf-8"?>
<odoo>


    <!-- ESTATE PROPERTY MAIN ACTION RECORD-->
    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Estate Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form,kanban</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
        Create a Property
            </p>
            <p>
        Click the "NEW" button on the left corner to get started creating your first propety.
            </p>
        </field>
        <field name="context"> 
        {'search_default_available': True }
        </field>
    </record>



    <!-- ESTATE PROPERTY KANBAN VIEW  -->
    <record id="estate_property_view_kanban" model="ir.ui.view">
        <field name="name">estate.property.view.kanban</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <kanban default_group_by="property_type_id" records_draggable='False'>
                <field name="state"/>
                <templates>
                    <t t-name="card">
                        <field name="name" class="fw-bold"></field>
                        <div class="flex-row">
                            <text> Expected Price : </text>

                            <field name="expected_price"></field>
                        </div>
                        <div class="flex-row" t-if="record.state.raw_value == 'offer_received'">
                            <text> Best Price : </text>
                            <field name="best_price"></field>
                        </div>
                        <div class="flex-row" t-if="record.state.raw_value == 'offer_accepted'">
                            <text> Selling Price : </text>
                            <field name="selling_price"></field>
                        </div>
                        <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>


    <!-- ESTATE PROPERTY LIST VIEW -->
    <record id="estate_property_view_list" model="ir.ui.view">
        <field name="name">estate.property.view.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list sample="1" decoration-success="state=='offer_accepted' or state=='offer_received'" decoration-bf="state=='offer_accepted'" decoration-muted ="state=='sold'">
                <field name="name" string="Title"/>
                <field name="property_type_id" />
                <field name="postcode" />
                <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'} " />
                <field name="bedrooms" />
                <field name="living_area" />
                <field name="expected_price"/>
                <field name="selling_price" />
                <field name="date_availability" optional="hide"/>
            </list>
        </field>
    </record>


    <!-- ESTATE PROPERTY FORM VIEW -->
    <record id="estate_property_view_form" model="ir.ui.view">
        <field name="name">estate.property.view.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="estate_property_form">
                <header>
                    <button class= "btn btn-primary" name="action_sell" type="object" string="SELL" invisible=" state == 'sold' or state=='cancelled'"/>
                    <button class= "btn btn-primary" name="action_cancel" type="object" string="CANCEL" invisible=" state == 'sold' or state=='cancelled'"/>
                    <field name="state" widget="statusbar" statusbar_visible="new,offer_received,offer_accepted,sold"/>
                </header>

                <sheet>
                    <group>
                        <group>
                            <h1>
                                <field name="name" placeholder="Property Name"></field>
                            </h1>
                            <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>

                        </group>
                        <group>
                            <field name="property_image" widget='image' options="{ 'size' :[100,100]}" />
                        </group>
                    </group>
                    <group>
                        <group>
                            <field name="property_type_id" options="{'no_create':True}"></field>
                            <field name="postcode"></field>
                            <field name="date_availability"></field>
                        </group>
                        <group>
                            <field name="expected_price"></field>
                            <field name="best_price"></field>
                            <field name="selling_price"></field>
                        </group>
                    </group>

                    <notebook>
                        <page name="description_page" string="Description">
                            <group name="Features">
                                <field name="description"></field>
                                <field name="bedrooms"></field>
                                <field name="living_area"></field>
                                <field name="facades"></field>
                                <field name="garage"></field>
                                <field name="garden"></field>
                                <field name="garden_area" invisible="not garden"></field>
                                <field name="garden_orientation" invisible="not garden"></field>
                                <field name="state"></field>
                                <field name="total_area"></field>
                            </group>
                        </page>
                        <page name="offers_page" string="Offers">
                            <group>
                                <field name="offer_ids" nolabel="1" readonly ="state=='offer_accepted' or state=='cancelled' or state=='sold' "></field>
                            </group>
                        </page>
                        <page name="other_info_page" string="Other Info">
                            <group name="Users">
                                <field name="user_id"></field>
                                <field name="partner_id" readonly='1'></field>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- ESTATE PROPERTY SEARCH VIEW -->
    <record id="estate_property_view_search" model="ir.ui.view">
        <field name="name">estate.property.view.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Search Estate Property">
                <field name="name" string="Title" />
                <field name="postcode" string="Postcode" />
                <field name="expected_price" string="Expected Price" />
                <field name="bedrooms" string="Bedrooms " />
                <field name="living_area" string="Living Area (sqm)" filter_domain="[ ('living_area', '>=', self )]"/>
                <field name="facades" />
                <filter string="Available" name="available" domain="['|', ('state', '=', 'new'), ('state', '=', 'offer_received')]"/>
                <group expand="0" string="Group By">
                    <filter string="Postcode" name="postcode" context="{'group_by':'postcode'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_make_bulk_offer" model="ir.actions.server">
        <field name="name">Add Offer</field>
        <field name="model_id" ref="estate.model_estate_property"/>
        <field name="binding_model_id" ref="estate.model_estate_property"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">action = records.action_make_offer()</field>
    </record>

</odoo>
