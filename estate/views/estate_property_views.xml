<odoo>
    <record id="action_estate_property" model="ir.actions.act_window">
        <field name="name">Estate Property</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form</field>
        <field name="context">{'search_default_available_properties': True}</field>
    </record>

    <record id="view_state_property_list" model="ir.ui.view">
        <field name="name">state_property_list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list decoration-muted="state == 'sold'" decoration-success="state in ('received', 'accepted')"
                  decoration-bf="state=='accepted'">
                <field name="name" string="Title"/>
                <field name="postcode"/>
                <field name="bedrooms"/>
                <field name="living_area" string="Living Area (sqm)"/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="date_availability" string="Available From" optional="hide"/>
            </list>
        </field>
    </record>

    <record id="view_state_property_form" model="ir.ui.view">
        <field name="name">state_property_form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="set_to_cancel" type="object" string="Cancel"/>
                    <button name="set_to_sold" type="object" string="Sold"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,received,accepted,sold"/>
                </header>
                <sheet>
                    <h1>
                        <field name="name"/>
                    </h1>
                    <div class="mb-1">
                        <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                    </div>
                    <group>
                        <group>
                            <field name="property_type_id" options="{'no_edit': True, 'no_create': True}"/>
                            <field name="postcode"/>
                            <field name="date_availability" string="Available From"/>
                        </group>
                        <group>
                            <field name="expected_price"/>
                            <field name="best_price" string="Best Offer"/>
                            <field name="selling_price"/>
                        </group>
                    </group>
                    <notebook>

                        <page name="description" string="Description">
                            <group>
                                <field name="description"/>
                                <field name="bedrooms"/>
                                <field name="facades"/>
                                <field name="garage"/>
                                <field name="garden"/>
                                <field name="garden_area" invisible="not garden"/>
                                <field name="garden_orientation" invisible="not garden"/>
                                <field name="living_area"/>
                                <field name="total_area"/>
                            </group>
                        </page>

                        <page name="offers" string="Offers">
                            <field name="offer_ids" readonly="state in ('accepted', 'sold', 'cancel')">
                                <list editable="bottom" decoration-success="status == 'accepted'"
                                      decoration-danger="status == 'refused'">
                                    <field name="price"/>
                                    <field name="partner_id"/>
                                    <field name="status"/>
                                    <button name="accept_offer" title="Accept" type="object" icon="fa-check" invisible="status"/>
                                    <button name="refuse_offer" title="Refuse" type="object" icon="fa-times-circle" invisible="status"/>
                                </list>
                            </field>
                        </page>


                        <page name="otherinfo" string="Other Info">
                            <group>
                                <field name="buyer_id"/>
                                <field name="salesperson_id"/>
                            </group>
                        </page>

                    </notebook>
                </sheet>
            </form>
        </field>
    </record>


    <record id="view_state_property_search" model="ir.ui.view">
        <field name="name">state_property_search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="postcode"/>
                <field name="expected_price"/>
                <field name="bedrooms"/>
                <field name="living_area" filter_domain="[('living_area', '&gt;=', self)]"/>
                <field name="facades"/>
                <filter string="Available Properties" name="available_properties"
                        domain="[('state', 'in', ('draft', 'received'))]"/>
                <filter name="group_postcode" context="{'group_by': 'postcode'}"/>
            </search>
        </field>
    </record>

</odoo>
