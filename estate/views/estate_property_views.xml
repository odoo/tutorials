<odoo>

    <record id="view_estate_property_list" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate_property</field>
        <field name="arch" type="xml">
            <list string="Properties" 
            decoration-muted="state == 'sold'"
            decoration-danger="state == 'cancelled'"
            decoration-primary="state == 'offer_received'"
            decoration-success="state == 'accepted'"
                  >
                <field name="name"/>
                <field name="state"/>
                <field name="date_availability"  optional="hide"/>
                <field name="bedrooms"/>
                <field name="expected_price"/>
            </list>
        </field>
    </record>

       <record id="view_estate_property_search" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate_property</field>
        <field name="arch" type="xml">
            <search string="searcher">
            <field name="name" string="Title"/>
            <field name="postcode"/>
            <field name="expected_price"/>
            <field name="bedrooms"/>
            <field name="living_area"/>
            <filter name="available" string="Available"
                    domain = "['|', ('state', '=', 'New'), ('state', '=', 'Offer Received')]"/>
            <filter name="estate_house_postcode_grouping_filter" string="Postcode"
                    context="{'group_by': 'postcode'}" />
            
            </search>
        </field>
    </record>

    <record id="view_estate_property_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate_property</field>
        <field name="arch" type="xml">
            <form string="Property">
                <!-- Header -->
                <header>
                <button name="action_sold" string="Sold" type="object" class="oe_highlight"
                        />
                <button name="action_cancel" string="Cancel" type="object" class="oe_highlight"
                       />
                    <field name="state" widget="statusbar" statusbar_visible="new,offer_received,offer_accepted,sold,cancelled"/>
                </header>

                <!-- Main Content -->
                <sheet>
                    <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>



                    <group>
                        <field name="name"/>
                        <field name="description"/>
                    </group>            <field name="name" string="Title"/>
                    
                    <group>
                        <field name="property_type_id" options="{'no_create': True, 'no_open': True}"/>
                                                <field name="postcode"/>

                        <field name="date_availability"/>
                    </group>
                    <group>
                        <field name="expected_price"/>
                        <field name="selling_price"/>
                    </group>

                    <group>
                        <field name="bedrooms"/>
                        
                    </group>
                </sheet>

                <!-- Optional Features -->
                <notebook>
                    <page string="Description">
                        <group>
                            <field name="bedrooms"/>
                            <field name="living_area"/>
                            <field name="facades"/>
                        </group>
                        <group>
                            <field name="garage"/>
                            <field name="garden"/>
                            <field name="garden_area" invisible="not garden"/>
                            <field name="garden_orientation" invisible="not garden"/>
                            <field name="total_area"/>
                <field name="best_price" readonly="1"/>

                        </group>
                    </page> 
                    <page string="Other Information">
                        <group>
                            <field name="seller_id"/>
                            <field name="buyer_id"/>
                        </group>
                      
                    </page>      
                    <page string="Offers">
                         <group>


                        <field name="offer_ids"  readonly="state in ('offer_accepted', 'refused', 'Cancelled')" editable="bottom">
                            <list string="Offers"            
                             decoration-danger="status == 'refused'"
                                decoration-success="status == 'accepted'"
                                decoration-muted="status == 'cancelled'">
                            

                                <field name="partner_id"/>

                                <field name="price"/>

                                <button name="accept_offer" type="object" string="Accept" class="btn-primary" icon="fa-check"
                                    attrs="{'invisible': [('status', '=', 'accepted')]}"/>

                                <button name="refuse_offer" type="object" string="Refuse" class="btn-secondary" icon="fa-times"
                                        attrs="{'invisible': [('status', 'in', ['accepted', 'refused'])]}"/>
                              

                                <field name="status"/>

                            </list>

                        </field>
                </group>

                      
                    </page>
                      
                </notebook>
            </form>
        </field>
    </record>


    <record id="action_estate_property" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate_property</field>
        <field name="view_mode">list,form,kanban,search</field>
                    <field name="view_id" ref="view_estate_property_list"/>
                    <field name="view_id" ref="view_estate_property_form"/>
                    <field name="view_id" ref="view_estate_property_search"/>

    </record>

    <record id="view_estate_property_type_list" model="ir.ui.view">
        <field name="name">estate.property.type.list</field>
        <field name="model">estate_property</field>
        <field name="arch" type="xml">
            <list string="Properties" default_order="sequence">
                <field name="sequence" widget="handle"/>

                <field name="name"/>
                
            </list>
        </field>
    </record>

    <record id="action_estate_property_type" model="ir.actions.act_window">
        <field name="name">Property Type</field>
        <field name="res_model">estate_property_type</field>
        <field name="view_mode">list,form</field>
        <field name="view_id" ref="view_estate_property_type_list"/>


    </record>





    <record id="action_estate_property_tag" model="ir.actions.act_window">
        <field name="name">Property Tag</field>
        <field name="res_model">estate_property_tag</field>
        <field name="view_mode">list,form</field>

    </record>

    <record id="action_estate_property_offer" model="ir.actions.act_window">
        <field name="name">Property Offer</field>
        <field name="res_model">estate_property_offer</field>
        <field name="view_mode">list,form</field>

 

    </record>
    
</odoo>
