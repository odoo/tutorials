<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="estate_property_view_search" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Property">
                <field name="name" string="Property" />
                <field name="postcode" />
                <field name='expected_price' />
                <field name='bedrooms' />
                <field name='living_area' filter_domain="[('living_area', '>=', self)]"/>
                <field name='facades' />
                <field name="property_type_id" />
                <filter string="Available" name="available"
                    domain="[('state', 'in', ['new', 'offer_received'])]" />
                <group expand="0" string="Group By">
                    <filter string="Postcode" name="postode" context="{'group_by': 'postcode'}" />
                </group>
            </search>
        </field>
    </record>

    <record id="estate_properties_view_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Property">
                <header>
                    <button name="action_set_sold" type="object" string="Sold"
                        invisible="state in ['sold', 'canceled']" />
                    <button name="action_set_cancel" type="object" string="Cancel"
                        invisible="state in ['sold', 'canceled']" />
                    <field name="state" widget="statusbar"
                        statusbar_visible="new,offer_received,offer_acepted,sold"
                        options="{'clickable': '1'}" />
                </header>
                <sheet>
                    <h1>
                        <field name='name' />
                    </h1>
                    <h2>
                        <field name="tag_ids" widget="many2many_tags"
                            options="{'color_field': 'color'}" />
                    </h2>
                    <group col="2">
                        <group>
                            <field name="property_type_id" options="{'no_create': true}" />
                            <field name='postcode' />
                            <field name='date_availability' />
                        </group>
                        <group>
                            <field name='expected_price' />
                            <field name="best_price" string="Best Offer" />
                            <field name='selling_price' />
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description" />
                                <field name='bedrooms' />
                                <field name='living_area' />
                                <field name='facades' />
                                <field name='garage' />
                                <field name='garden' />
                                <field name='garden_area' invisible="not garden" />
                                <field name='garden_orientation' invisible="not garden" />
                                <field name="total_area" />
                            </group>
                        </page>
                        <page string="Offers">
                            <field name="offer_ids"
                                readonly="state in ['offer_acepted', 'sold', 'canceled']" />
                        </page>
                        <page string="Other Info">
                            <group>
                                <field name="seller_id" />
                                <field name="buyer_id" />
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="estate_property_view_tree" model="ir.ui.view">
        <field name="name">estate.property.tree</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <tree string="Properties" decoration-muted="state == 'sold'"
                decoration-success="state == 'offer_received' or state == 'offer_acepted'"
                decoration-bf="state == 'offer_acepted'">
                <field name="name" />
                <field name='postcode' />
                <field name='bedrooms' />
                <field name='living_area' />
                <field name='expected_price' />
                <field name='selling_price' />
                <field name='date_availability' optional="hidden" />
                <field name="property_type_id" />
                <field name="state" column_invisible="1" />
            </tree>
        </field>
    </record>

    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">tree,form</field>
        <field name="search_view_id" ref="estate.estate_property_view_search"/>
        <field name="context">{'search_default_available': True,}</field>
    </record>
</odoo>