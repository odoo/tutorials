<?xml version='1.0' encoding='utf-8'?>
<odoo>


    <record id="estate_property_view_list" model="ir.ui.view">
        <field name="name">estate.property.view.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list>
                <field name="property_type_id" string="Property Type" />
                <field name="name" string="Title" />
                <field name="postcode" />
                <field name="bedrooms" />
                <field name="living_area" />
                <field name="expected_price" />
                <field name="selling_price" />
                <field name="date_availability" />
            </list>
        </field>
    </record>

    <record id="estate_property_view_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="properties">
                <header>

                    <field name="state"
                        widget="statusbar"
                    />


                    <!-- <button string="Assign"
                        name="action_assign"
                        type="object"
                        class="btn-primary"
                        invisible="progress_state != 'new'" /> -->

                    <button name="action_start"
                        string="Start"
                        type="object"
                        class="btn-primary"
                        invisible="progress_state != 'assigned'" />

                    <button name="action_stop"
                        string="Stop"
                        type="object"
                        class="btn-danger"
                        invisible="progress_state != 'inprogress'" />
                    <button string="Done"
                        name="action_done"
                        type="object"
                        class="btn-success"
                        invisible="progress_state != 'in_progress'" />


                    <button string="Sold" name="sold" type="object" class="btn-success" />
                    <button string="Cancel" name="cancel" type="object" class="btn-danger" />
                    <!-- <button string="Reset " name="reset" type="object" class="btn-secondary" /> -->
                    <button string="Accept Best" name="accept_best_offer" type="object" />
                </header>
                <sheet>
                    <group name="name">
                        <field name="state" />
                        <field name="property_type_id" string="Property Type" />
                        <field name="name" />
                        <field name="tag_ids" widget="many2many_tags" />
                        <field name="postcode" />
                        <field name="date_availability" />
                        <field name="best_price" string="Best Price" />
                    </group>
                    <group>
                        <field name="expected_price" string="Expected Price" />
                        <field name="best_price" string="Best Price" />
                        <field name="selling_price" string="Selling Price" />
                    </group>
                    <notebook>
                        <page name="Description" string="Description">
                            <group name="name" string="Enter the required fields">
                                <field name="description" string="Description" />
                                <field name="bedrooms" string="Bedrooms" />
                                <field name="living_area" string="Living Area" />
                                <field name="facades" string="Facades" />
                                <field name="garage" string="Garage" />
                                <field name="garden" string="Garden" />
                                <field name="garden_area" string="Garden Area" />
                                <field name="garden_orientation" string="Garden Orientation" />
                                <field name="total_area" string="Total Area" />
                            </group>
                        </page>
                        <page name="Offers" string="Offers">
                            <field name="offer_ids">
                                <list editable="bottom">
                                    <field name="price" />
                                    <field name="partner_id" />
                                    <field name="status" />

                                    <button name="accept"
                                        type="object"
                                        icon="fa-check"
                                        title="Accept Offer"
                                        class="btn btn-success"
                                        invisible="parent.state == 'sold'"
                                    />

                                    <button name="reject"
                                        type="object"
                                        icon="fa-times"
                                        title="Reject Offer"
                                        class="btn btn-danger"
                                        invisible="parent.state == 'sold'" />
                                </list>
                            </field>
                        </page>
                        <page name="other_info" string="Other Info">
                            <group>
                                <field name="salesman_id" string="Salesman" />
                                <field name="buyer_id" string="Buyer" />
                            </group>
                        </page>
                        <!-- <page name="request" string="Request">
                            <group name="request" string="string">
                                <field name="request" />
                                <field name="technichine" />
                            </group>

                        </page> -->


                        <page string="Requests">
                            <field name="request_ids">
                                <list editable="bottom">
                                    <field name="name" />
                                    <field name="technician_id" />
                                    <field name="estimated_cost" />
                                    <field name="state" />

                                    <button name="action_start"
                                        type="object"
                                        string="Start"
                                        invisible="state != 'assigned'"
                                        class="btn-primary" />

                                    <button name="action_stop"
                                        type="object"
                                        string="Stop"
                                        invisible="state != 'inprogress'"
                                        class="btn-success" />
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="estate_property_view_search" model="ir.ui.view">
        <field name="name">estate.property.view.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search>

                <field name="name" string="Title" />
                <field name="postcode" string="Postcode" />
                <field name="expected_price" string="Expected Price" />
                <field name="bedrooms" string="Bedrooms" />
                <field name="living_area" string="Living Area" />
            </search>
        </field>
    </record>
    <record id="estate_property_view_search" model="ir.ui.view">
        <field name="name">estate.property.view.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search>
                <filter name="Available Properties" string="Available Properties"
                    domain="[('state', 'in', ['new','offer_received'])]" />


                <field name="name" string="Title" />
                <field name="postcode" string="Postcode" />
                <field name="expected_price" string="Expected Price" />
                <field name="bedrooms" string="Bedrooms" />
                <field name="living_area" string="Living Area" />
            </search>
        </field>
    </record>

    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form</field>
    </record>
    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
