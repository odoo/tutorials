<odoo>
    <data>

        <record id="mon_action" model="ir.actions.act_window">
            <field name="name">Properties</field>
            <field name="res_model">estate.property</field>
            <field name="view_mode">list,form,kanban</field>
            <field name="context">{'search_default_availibility': True}</field>
        </record>

        <record id="estate_property_view_kanban" model="ir.ui.view">
            <field name="name">estate.property.view.kanban</field>
            <field name="model">estate.property</field>
            <field name="arch" type="xml">
                <kanban default_group_by="property_type_id" records_draggable="False">
                    <field name="state"/>
                    <templates>
                        <t t-name="card">
                            <div><b><field name="name"/></b></div>
                            <div>Expected price: <field name="expected_price"/></div>
                            <div t-if="record.state.raw_value == 'Offer Received'">Best price: <field name="best_price"/></div>
                            <div t-if="['Offer Accepted', 'Sold'].includes(record.state.raw_value)">Selling price: <field name="selling_price"/></div>
                            <div><field name="tag_ids"/></div>
                        </t>
                    </templates>
                </kanban>
            </field>
        </record>

        <record id="estate_property_view_list" model="ir.ui.view">
            <field name="name">estate.property.view.list</field>
            <field name="model">estate.property</field>
            <field name="arch" type="xml">
            <list decoration-success="state in ['Offer Received', 'Offer Accepted']" decoration-bf="state == 'Offer Accepted'" decoration-muted="state == 'Sold'">
                <field name="name"/>
                <field name="postcode"/>
                <field name="bedrooms"/>
                <field name="living_area"/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="date_availability" optional="hide"/>
            </list>
            </field>
        </record>

        <record id="perso_tab1" model="ir.ui.view">
        <field name="name">estate.property.view.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
        <form string="Test">
            <header>
                <button name="action_mark_as_sold" type="object" string="Sold" invisible="state in ['Offer Accepted', 'Sold', 'Cancelled']"/>
                <button name="action_mark_as_sold" type="object" string="Sold" class="btn-primary" invisible="state != 'Offer Accepted'"/>
                <button name="action_mark_as_cancelled" type="object" string="Cancelled" invisible="state == 'Sold' or state == 'Cancelled'"/>
                <field name="state" widget="statusbar" statusbar_visible="New,Offer Received,Offer Accepted,Sold"/>
            </header>
            <sheet>
                <h1>
                    <field name="name"/>
                </h1>
                <group>
                    <group>
                        <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                        <field name="property_type_id" options="{'no_create_edit': True}"/>
                        <field name="postcode"/>
                        <field name="date_availability"/>
                    </group>
                    <group>
                        <field name="expected_price"/>
                        <field name="best_price"/>
                        <field name="selling_price"/>
                    </group>
                </group>
                <notebook>
                    <page string="Description">
                        <group>
                            <field name="description"/>
                            <field name="bedrooms"/>
                            <field name="living_area"/>
                            <field name="facades"/>
                            <field name="garage"/>
                            <field name="garden"/>
                            <field name="garden_area" invisible="not garden"/>
                            <field name="garden_orientation" invisible="not garden"/>
                            <field name="total_area"/>
                        </group>
                    </page>
                    <page string="Offers">
                        <field name="offer_ids" readonly="state in ['Offer Accepted', 'Sold', 'Cancelled']">
                            <list editable="bottom" decoration-success="status == 'Accepted'" decoration-danger="status == 'Refused'">
                                <field name="price"/>
                                <field name="partner_id"/>
                                <field name="property_type_id"/>         
                                <button name="action_accept" type="object" icon="fa-check" string="accept" invisible="status in ['Accepted', 'Refused']"/>
                                <button name="action_refuse" type="object" icon="oi-close" string="refuse" invisible="status in ['Accepted', 'Refused']"/>
                            </list>
                        </field>
                    </page>
                    <page string="Other Info">
                        <group>
                            <field name="salesman"/>
                            <field name="buyer"/>
                        </group>
                    </page>

                </notebook>
            </sheet>
        </form>
        </field>
    </record>

    <record id="estate_property_search" model="ir.ui.view">
        <field name="name">estate.property.view.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Search something">
                <field name="name"/>
                <field name="postcode"/>
                <field name="expected_price"/>
                <field name="bedrooms"/>
                <field name="living_area" filter_domain="[('living_area', '>=', self)]"/>
                <field name="facades"/>
                <filter name="group_by_postcode" context="{'group_by': 'postcode'}"/>
                <filter name="availibility" string="Available" domain="['|', ('state', '=', 'New'),('state', '=', 'Offer Received')]"/>
            </search>
        </field>
    </record>

    </data>
</odoo>
