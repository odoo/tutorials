<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id='action_estate_property' model='ir.actions.act_window'>
        <field name="name">Estate</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form</field>
        <field name = "context">{'search_default_availible_property': 1}</field>
    </record>

    <record id="property_view" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list decoration-success="state == 'offer_received' or state == 'offer_accepted'"
                decoration-bf="state == 'offer_accepted'" decoration-muted="state == 'sold'">
                <field name="name" />
                <field name="property_type_id" />
                <field name="postcode" />
                <field name="tag_ids" widget="many2many_tags" options="{'color_field':'color'}" />
                <field name="bedrooms" />
                <field name="living_area" />
                <field name="expected_price" />
                <field name="selling_price" />
                <field name="date_availability" optional="1"/>
            </list>
        </field>
    </record>

    <record id="propety_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Property">
                <header>
                    <button name="action_property_sold" type="object" string="SOLD" class="btn btn-primary" invisible="state == 'sold' or state == 'canceled'" />
                    <button name="action_property_cancel" type="object" string="CANCEL"  class="btn btn-secondary"   invisible="state == 'sold' or state == 'canceled'"/>
                    <field name="state" widget="statusbar" statusbar_visible="new,offer_received,offer_accepted,sold"/>
                </header>
                <sheet>
                    <h1>
                        <field name="name" />
                    </h1>
                    <field name="tag_ids" widget="many2many_tags" string="Tags" options="{'color_field': 'color'}" />
                    <group>
                        <group>
                            <field name="property_type_id" />
                            <field name="postcode" />
                            <field name="date_availability" />
                        </group>
                        <group>
                            <field name="expected_price" />
                            <field name="selling_price" />
                            <field name="best_price" string="Best Offer"/>
                        </group>
                    </group>

                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description" />
                                <field name="bedrooms" />
                                <field name="living_area" />
                                <field name="facades" />
                                <field name="garage" />
                                <field name="garden" />
                                <field name="garden_area" invisible="not garden" />
                                <field name="garden_orientation" invisible="not garden" />
                                <field name="active" />
                                <field name="state" />
                                <field name="total_area"/>
                            </group>
                        </page>

                        <page string="Offer">
                            <group>
                                <field name="offer_ids"  widget="many2many_offer" nolabel="1" readonly="state in ('offer_accepted','cancelled','sold')" />
                            </group>
                        </page>

                        <page string="Other Info">
                            <group>
                                <field name="user_id" />
                                <field name="buyer" />
                            </group>
                        </page>

                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="property_search" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Estate Property">
                <filter string="Available" name="availible_property" domain="['|', ('state', '=', 'new'), ('state', '=', 'offer_received')]" />
                <!-- domain :  static and predefined (ORM) level -->

                <field name="living_area" string="Living Area by Minimum Size(sqrm)" filter_domain="[('living_area', '>=', self)]"/>
                <!-- filter_domain : dynamic and user defined (View) level -->

                <group expand="1" string="Group By">
                    <filter string="State" name="state" context="{'group_by':'state'}" />
                    <filter string="Postcode" name="postcode" context="{'group_by':'postcode'}" />
                </group>
                <field name="name" string="Name" />
                <field name="postcode" string="Postcode" />
                <field name="bedrooms" string="Bedrooms" />
                <field name="living_area" string="Living Area" />
                <field name="expected_price" string="Expected Price" />
                <field name="selling_price" string="Selling Price" />
                <field name="date_availability" string="Date Availability" />
            </search>
        </field>
    </record>
</odoo>
