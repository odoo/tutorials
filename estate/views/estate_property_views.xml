<odoo>
    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Property</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form,kanban</field>
        <field name="context">{'search_default_available':1}</field>
    </record>

    <record id="estate_property_list_view" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="Properties List" default_order="id desc" decoration-success="state=='offer received' or state=='offer accepted'" decoration-bf="state=='offer accepted'" decoration-muted="state=='sold'">
                <field name="name" string="Title"/>
                <field name="postcode" string="Postcode"/>
                <field name="bedrooms" string="Bedrooms"/>
                <field name="living_area" string="Living Area(sqm)"/>
                <field name="expected_price" string="Expected Price"/>
                <field name="selling_price" string = "Selling Price"/>
                <field name="date_availability" string="Available From" optional="hidden"/>
            </list>
        </field>
    </record>

     <record id="estate_property_form_view" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Properties Form">
                <header>
                    <button name="action_sold_property" type="object" string="Sold" invisible="state=='sold' or state=='cancelled'"/> <!--type="object", the Odoo framework will execute a Python method with name="action_sold_property" on the given model.-->
                    <button name="action_cancel_property" type="object" string="Cancel" invisible="state=='sold' or state=='cancelled'"/>
                    <field name="state" widget="statusbar" statusbar_visible="new, offer received, offer accepted, sold"/>
                </header>
                 <sheet>
                    <h1>Property Detail</h1>
                    <group>
                        <group>
                            <field name="property_tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/> <!-- widget display this field in tag-style UI-->
                            <field name="name"/>
                            <field name="property_type_id" options="{'no_create': True, 'no_create_edit': True}"/>
                            <field name="postcode"/>
                        </group>
                        <group>
                            <field name="date_availability"/>
                            <field name="expected_price"/>
                            <field name="best_offer"/>
                            <field name="selling_price"/>
                        </group>
                    </group>

                    <notebook>
                        <page string="Description">
                            <group>
                                <group>
                                    <field name="description"/>
                                    <field name="bedrooms"/>
                                    <field name="living_area"/>
                                    <field name="facades"/>
                                    <field name="total_area"/>
                                </group>
                                <group>
                                    <field name="garage"/>
                                    <field name="garden"/>
                                    <field name="garden_orientation" invisible="not garden"/>
                                    <field name="garden_area" invisible="not garden"/>
                                </group>
                            </group>
                        </page>
                        <page string="Other Info">
                            <group>
                                <field name="salesmen_id"/>
                                <field name="buyer_id"/>
                            </group>
                        </page>
                        <page string="Offers">
                            <field name="offer_ids" readonly="state!='new' and state!='offer received'"/>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="estate_property_search_view" model="ir.ui.view">
            <field name="name">estate.property.search</field>
            <field name="model">estate.property</field>
            <field name="arch" type="xml">
                <search string="Properties Search">

                    <!-- search using property -->
                    <field name="name" string="Title"/>
                    <field name="postcode" string="Postcode"/>
                    <field name="expected_price" string="Expecte Price"/>
                    <field name="bedrooms" string="Bedrooms"/>
                    <field name="living_area" string="Living Area(sqm)" filter_domain="[('living_area', '>=', self)]"/> <!--self is entred value-->
                    <field name="facades" string="Facades" />

                    <!-- filter record -->
                    <filter name="available" string="Available" domain="['|',('state','=','new'), ('state', '=', 'offer received')]"/>
                    <filter string="New" name="new property filter" domain="[('state','=','new')]"/>
                    <filter string="Offer Recieved" name="offer recieved property filter" domain="[('state','=','offer received')]"/>

                    <!-- group record -->
                    <group expand="1" string="group by postcode">
                        <filter string="PostCode" name="filter for group by postcode" context="{'group_by':'postcode'}"/>
                    </group>
                </search>
            </field>
    </record>

    <record id="estate_property_kanban_view" model="ir.ui.view">
            <field name="name">estate.property.kanban</field>
            <field name="model">estate.property</field>
            <field name="arch" type="xml">
                <kanban default_group_by="state" records_draggable='False'>
                    <field name="state"/>
                    <templates>
                        <t t-name="kanban-box">
                            <div class="oe_kanban_global_click">
                                <strong>
                                    Name : <field name="name" string="Name"/>
                                </strong>
                                <div>
                                    Expected Price : <field name="expected_price" string="Expected Price"/>
                                </div>
                                <t t-if="record.state.raw_value == 'offer received'">
                                    Best Price : <field name="best_offer" string="Best Offer"/>
                                </t>
                                <t t-if="record.state.raw_value == 'offer accepted'">
                                    Selling Price : <field name="selling_price" string="Selling Price"/>
                                </t>
                                <div>
                                    <field name="property_tag_ids" widget="many2many_tags" string="Tags" options="{'color_field': 'color'}"/>
                                </div>
                            </div>
                        </t>
                    </templates>
                </kanban>
            </field>
    </record>
</odoo>
