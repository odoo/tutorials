<odoo>
    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Real Estate Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">tree,form,kanban</field>
        <!-- <field name="context">{'form_view_initial_mode': 'edit'}</field> -->
    </record>

    <record id="view_estate_property_tree" model="ir.ui.view">
        <field name="name">estate.property.tree</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <tree string="Properties">
                <field name="name"/>
                <field name="expected_price" string="Expected Price"/>
                <field name="best_price" string="Best Offer"/>
                <field name="selling_price" string="Selling Price"/>
                <field name="bedrooms"/>
                <field name="postcode"/>
                <field name="total_area" string="Total Area"/>
                <field name="state" string="Status"/>
                <field name="property_type_id" string="Property Type"/>
                <field name="buyer_id" string="Buyer"/>
                <field name="seller_id" string="Salesperson"/>
            </tree>
        </field>
    </record>

    <record id="view_estate_property_kanban" model="ir.ui.view">
        <field name="name">estate.property.kanban</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <tree string="Properties">
                <field name="name"/>
                <field name="expected_price" string="Expected Price"/>
                <field name="selling_price" string="Selling Price"/>
                <field name="bedrooms"/>
                <field name="postcode"/>
                <field name="state" string="Status"/>
            </tree>
        </field>
    </record>

    <record id="view_estate_property_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Property Details">
                <div style="gap: 5px;">
                <button name="action_set_sold_property" type="object" string="Sold" class="btn-link" modifiers='{"invisible": [[ "state", "not in", ["new", "offer_received", "offer_accepted"]]]}'/>
                <button name="action_cancel_property" type="object" string="Cancel" class="btn-link" modifiers='{"invisible": [[ "state", "not in", ["new", "offer_received", "offer_accepted"]]]}'/>
                </div>
                <sheet>
                    <h1><field name="name" options="{'no_label': True}"/></h1>
                    <group> 
                        <group>  
                            <field name="property_type_id" string="Property Type"/>
                            <field name="postcode" string="Postcode"/>
                            <field name="tag_ids" string="Tags" widget="many2many_tags"/>
                        </group>
                        <group>
                                <field name="expected_price" string="Expected Price"/>
                                <field name="best_price" string="Best Offer"/>
                                <field name="selling_price" string="Selling Price" readonly="1"/>
                        </group>
                        
                    </group>

                    <notebook>

                        <page string="Description">
                            <group>
                                <field name="description" string="Detailed Description"/>
                                <field name="bedrooms" string="Number of Bedrooms"/>
                                <field name="living_area" string="Living Area (m²)"/>
                                <field name="facades" string="Number of Facades"/>
                                <field name="garage" string="Garage"/>
                                <field name="garden" string="Garden"/>
                                <field name="garden_area" string="Garden Area (m²)"/>
                                <field name="garden_orientation" string="Garden Orientation"/>
                                <field name="total_area" string="Total Area" readonly="1"/>
                                <field name="state" string="Status"/>
                            </group>
                        </page>
                        
                        <page string="Sales Info">
                            <group>
                                <field name="buyer_id" string="Buyer"/>
                                <field name="seller_id" string="Salesperson"/>
                            </group>
                        </page>

                        <!-- <page string="Tags">
                            <group>
                                <field name="tag_ids" widget="many2many_tags"/>
                            </group>
                        </page> -->
                        
                        <page string="Offers">
                            <field name="offer_ids">
                                <tree> <!-- editable="bottom" create="true" -->
                                    <field name="partner_id"/>
                                    <field name="price"/>
                                    <field name="created_date"/>
                                    <field name="validity"/>
                                    <field name="date_deadline"/>
                                    <field name="status"/>
                                    <button name="action_accept_offer" type="object" icon="fa-check" string="Accept" states="pending"/>
                                    <button name="action_refuse_offer" type="object" icon="fa-times" string="Refuse" states="pending"/>
                                </tree>
                            </field>
                        </page>
                        
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_estate_property_search" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Search Properties">
                <field name="name"/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="bedrooms"/>
                <filter string="Active" name="active" domain="[('active', '=', True)]"/>
                <filter string="Sold" name="sold" domain="[('state', '=', 'sold')]"/>
                <filter string="New" name="new" domain="[('state', '=', 'new')]"/>
            </search>
        </field>
    </record>

</odoo>
