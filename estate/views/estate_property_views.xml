<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- action window for managing property -->
    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form</field>
        <field name="context">{'search_default_availible_property': True}</field>
    </record>

    <!-- List view -->
    <record id="estate_property_list" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="Estate Properties" decoration-bf="state=='offer_accepted'" decoration-success="state in ('offer_received', 'offer_accepted')" decoration-muted="state=='sold'">
                <field name="name" string="Title"/>
                <field name="state"/>
                <field name="tag_ids" widget="many2many_tags"/>
                <field name="property_type_id" string="Property Type"/>
                <field name="postcode"/>
                <field name="bedrooms"/>
                <field name="living_area" string="Living Area(sqrm)"/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="date_availability" string="Available From" optional="hide"/>
            </list>
        </field>
    </record>

    <!-- Form view -->
    <record id="estate_property_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Estate Properties Form">
                <header>
                    <button name="action_set_sold" type="object" string="Sold" class="oe_highlight" invisible="state in ('sold', 'cancelled')"/>
                    <button name="action_set_cancel" type="object" string="Cancel" invisible="state in ('sold', 'cancelled')"/>
                    <field name="state" widget="statusbar" statusbar_visible="new, offer_received, offer_accepted, sold"/> 
                </header>
                <sheet>
                    <h1>
                        <field name="name" placeholder="Name" class="mb16"/>
                    </h1>
                    <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>         
                    <group col="2">
                        <group>                    
                            <field name="property_type_id" string="Property Type" options="{'no_create': true, 'no_edit': true, 'no_open': true}"/>
                            <field name="postcode"/>
                            <field name="date_availability" string="Available From"/>
                        </group>
                        <group >
                            <field name="expected_price"/>
                            <field name="best_price"/>
                            <field name="selling_price"/>
                        </group>
                    </group>

                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description" class="mb16"/>
                                <field name="bedrooms" class="mb16"/>
                                <field name="living_area" string="Living Area (sqm)" class="mb16"/>
                                <field name="facades" class="mb16"/>
                                <field name="garage" class="mb16"/>
                                <field name="garden" class="mb16"/>
                                <field name="garden_area" string="Garden Area (sqm)" class="mb16" invisible="not garden"/>
                                <field name="garden_orientation" class="mb16" invisible="not garden"/>
                                <field name="total_area"/>
                            </group>
                        </page>
                        <page string="Offers">
                            <group>
                                <field name="offer_ids" string="Offers" class="mb16" readonly="state in ('offer_accepted','cancelled','sold')"/>   
                            </group>
                        </page>
                        <page string="Other Info">
                            <group>
                                <field name="user_id" string="Salesperson" class="mb16"/>
                                <field name="partner_id" string="Buyer" class="mb16"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Search Feature -->
    <record id="estate_property_search" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Estate Properties Search">
                <field name="name" string="Title"/>
                <field name="property_type_id" string="Property Type"/>
                <field name="postcode"/>
                <field name="bedrooms"/>
                <field name="living_area" string="Living Area(sqrm)" filter_domain="[('living_area', '>=', self)]"/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="date_availability" string="Availible From"/>
                <filter name="availible_property" string="Avalible Property" domain="[('state', 'in', ('offer_received','new'))]"/>
                <group expand="1" string="Group By">
                    <filter name="postcode" context="{'group_by': 'postcode'}"/>        
                </group>
            </search>
        </field>
    </record>
</odoo>
