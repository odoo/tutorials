<?xml version="1.0" ?>
<odoo>
    <!-- Estate Property List -->
    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Estate Property</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="context">{'search_default_available': 1}</field>
    </record>

    <!-- Estate Property Kanban -->
    <record id="estate_property_kanban" model="ir.ui.view">
        <field name="name">estate.property.kanban</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <kanban default_group_by="property_type_id" records_draggable="false" groups_draggable="false">
                <field name="state"/>
                <templates>
                    <t t-name="card">
                        <field name="name" class="fw-bold fs-5"/>
                        <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                        <ul>
                            <li>Expected Price: <field name="expected_price"/></li>
                            <li t-if="record.state.raw_value == 'offer_received'">
                                Best Price: <field name="best_price"/>
                            </li>
                            <li t-if="record.state.raw_value == 'offer_accepted'">
                                Selling Price: <field name="selling_price"/>
                            </li>
                        </ul>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

     <!-- Estate Property List -->
    <record id="estate_property_list" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="Properties"
                  decoration-success="state in ('offer_received', 'offer_accepted')"
                  decoration-bf="state == 'offer_accepted'"
                  decoration-muted="state == 'sold'">
                <field name="name" class="text-center" width="50px"/>
                <field name="property_type_id" class="text-center" width="50px"/>
                <field name="postcode" class="text-center" width="50px"/>
                <field name="bedrooms" class="text-center" width="50px"/>
                <field name="living_area" class="text-center" width="50px"/>
                <field name="expected_price" class="text-center" width="50px"/>
                <field name="selling_price" class="text-center" width="50px"/>
                <field name="date_availability" class="text-center" width="50px" optional="hide"/>
                <field name="state" column_invisible="True"/> <!-- Used by list decorations -->
            </list>
        </field>
    </record>

    <!-- Estate Property Form -->
    <record id="estate_property_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_sold" type="object" class="oe_highlight" string="Sold"
                            invisible="state in ('sold', 'canceled')"/>
                    <button name="action_cancel" type="object" class="oe_highlight" string="Cancel"
                            invisible="state in ('sold', 'canceled')"/>
                    <field name="state" widget="statusbar" statusbar_visible="new,offer_received,offer_accepted,sold"/>
                </header>
                <sheet>
                    <group>
                        <div class="oe_title">
                            <h2 class="mb32">
                                <field name="name" class="mb16"/>
                            </h2>
                            <field name="tag_ids" widget="many2many_tags" nolabel="1" options="{'color_field': 'color'}"/>
                        </div>
                    </group>
                    <group>
                        <field name="postcode"/>
                        <field name="property_type_id" options="{'no_create': True, 'no_edit': True}"/>
                        <field name="expected_price"/>
                        <field name="best_price"/>
                        <field name="date_availability"/>
                        <field name="selling_price"/>
                    </group>
                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description"/>
                                <field name="bedrooms"/>
                                <field name="living_area"/>
                                <field name="facades"/>
                                <field name="garage"/>
                                <field name="garden"/>
                                <field name="garden_area" invisible="not garden"/>
                                <field name="total_area"/>
                                <field name="garden_orientation" invisible="not garden"/>
                            </group>
                        </page>
                        <page string="Offers">
                            <field name="offer_ids" readonly="state in ('offer_accepted', 'sold', 'canceled')"/>
                        </page>
                        <page string="Other Info">
                            <group>
                                <field name="salesperson_id"/>
                                <field name="buyer_id"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Estate Property Search -->
    <record id="estate_property_search" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search>
                <group>
                    <field name="name"/>
                    <field name="postcode"/>
                    <field name="expected_price"/>
                    <field name="bedrooms"/>
                    <field name="living_area" filter_domain="[('living_area', '&gt;=', self)]"/>
                    <field name="selling_price"/>
                    <field name="facades"/>
                </group>
                <group>
                    <!-- ISSUE HERE can't find the way to set the domain to be >= today date  -->
                    <!-- <filter
                        name="date_availability" string="Available" domain="[('date_availability', '&gt=;', 'today')]"
                    /> -->
                    <filter string="Postcode" name="postcode" context="{'group_by':'postcode'}"/>
                    <filter string="Available" name="available" domain="[('state', 'in', ['new', 'offer_received'])]"/>
                </group>
            </search>
        </field>
    </record>
</odoo>
