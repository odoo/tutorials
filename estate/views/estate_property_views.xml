<?xml version="1.0" encoding="utf-8"?>

<odoo>

    <!-- Configuration/Estate/Properties Search -->
    <record id="estate_property_view_search" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Search Properties">
                <field name="name" string="Title"/>
                <field name="postcode" string="Postcode"/>
                <field name="expected_price" string="Expected Price"/>
                <field name="bedrooms" string="Bedrooms"/>
                <field name="living_area" string="Living Area (sqm)" filter_domain="[('living_area', '&gt;=', self)]"/>
                <field name="facades" string="Facades"/>
                <separator/>
                <filter name="available" string="Available" domain="['|', ('state', '=', 'new'), ('state', '=', 'offer received')]"/>
                <group expand="1" string="Group By">
                    <filter string="Postcode" name="postcode" context="{'group_by':'postcode'}"/>
                </group>
            </search>
        </field>
    </record>
   
   <!-- Configuration/Estate/Properties Form -->
    <record id="estate_property_view_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Property">
                <header>
                    <button name="action_cancel_the_property" type="object" string="Cancel" invisible="state in ('cancelled', 'sold')"/>
                    <button name="action_sell_the_property" type="object" string="Sold" invisible="state in ('cancelled', 'sold')"/>
                    <field name="state" widget="statusbar" statusbar_visible="new, offer received, offer accepted, cancelled, sold"/>
                </header>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="property_tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                        <group>
                            <field name="property_type_id" options="{'no_create': true}"/>
                            <field name="postcode"/>
                            <field name="date_availability"/>
                        </group>
                        <group>
                            <field name="expected_price"/>
                            <field name="best_price"/>
                            <field name="selling_price"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description"/>
                                <field name="bedrooms"/>
                                <field name="living_area"/>
                                <field name="facades"/>
                                <field name="garage"/>
                                <field name="garden"/>
                                <field name="garden_area" invisible="not garden"/>
                                <field name="garden_orientation" invisible="not garden"/>
                                <field name="total_area"/>
                            </group>
                        </page>
                        <page string="Offers">
                            <field name="offer_ids" readonly="state in ('sold', 'cancelled', 'offer accepted')"/>
                        </page>
                        <page string="Other Info">
                            <group>
                                <field name="property_buyer_id" widget="many2one"/>
                                <field name="property_seller_id" widget="many2one"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Configuration/Estate/Properties List -->
    <record id="estate_property_view_list" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="Properties list" decoration-muted="state=='sold'" decoration-success="(state=='offer received') or (state=='offer accepted')" decoration-bf="state=='offer accepted'">
                <field name="name" string="Title" widget="text"/>
                <field name="property_type_id" string="Type"/>
                <field name="postcode" string="Postcode" widget="text"/>
                <field name="bedrooms" string="Bedrooms" widget="text"/>
                <field name="living_area" string="Living Area (sqm)" widget="text"/>
                <field name="expected_price" string="Expected Price" widget="text"/>
                <field name="selling_price" string="Selling Price" widget="text"/>
                <field name="date_availability" string="Available From" widget="datetime" optional="hide"/>
                <field name="state" string="Status" optional="hide"/>
            </list>
        </field>
    </record>

    <!-- Configuration/Estate/Properties Menu -->
    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form</field>
        <field name="context">{'search_default_available': True}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">Define a new property</p>
        </field>
    </record>
</odoo>