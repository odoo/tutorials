<?xml version="1.0" ?>
<odoo>
    <record id="estate_property_view_kanban" model="ir.ui.view">
        <field name="name">estate.property.kanban</field>
        <field name="model">estate.property</field>
        <field name="type">kanban</field>
        <field name="arch" type="xml">
            <kanban default_group_by="property_type_id" records_draggable="0">
                <field name="status"/>
                <field name="expected_price"/>
                <field name="best_offer"/>
                <field name="selling_price"/>
                <field name="property_tag_id"/>
                <templates>
                    <t t-name="card">
                    <main>
                        <strong>
                            <field name="name"/>
                        </strong>
                        <div>
                            <span>Expected Price: </span>
                            <span t-esc="record.expected_price.value"/>
                        </div>
                        <div t-if="record.best_offer.raw_value">
                            <span>Best Price: </span>
                            <span t-esc="record.best_offer.value"/>
                        </div>
                        <div t-if="record.selling_price.raw_value">
                            <span>Selling Price: </span>
                            <span t-esc="record.selling_price.value"/>
                        </div>
                        <div>
                            <field name="property_tag_id" widget="many2many_tags"
                                options="{'color_field': 'color'}" />
                        </div>
                    </main>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="estate_property_view_list" model="ir.ui.view">
        <field name="name">estate.property.view.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="List" decoration-success="status == 'offer_received' or status == 'offer_accepted'" decoration-muted="status == 'sold'" decoration-bf="status == 'offer_accepted'">
                <field name="name" string="Title"/>
                <field name="property_type_id"/>
                <field name="postcode"/>
                <field name="property_tag_id" widget = "many2many_tags" options="{'color_field': 'color'}"/>
                <field name="bedrooms"/>
                <field name="living_area" />
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="date_availability" optional = "hide"/>
            </list>
        </field>
    </record>

    <record id="estate_property_view_form" model="ir.ui.view">
        <field name="name">estate.property.view.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Form">
                <header>
                    <button name="property_sold" type="object" string="Sold" invisible="status == 'sold' or status == 'cancelled'"/>
                    <button name="property_cancel" type="object" string="Cancel" invisible="status == 'sold' or status == 'cancelled'"/>
                    <field name = "status" widget = "statusbar" />
                </header>
                <sheet>
                    <h1><field name="name"/></h1>
                    <h3><field name="property_tag_id" widget='many2many_tags' options="{'color_field': 'color'}"/></h3>
                    <group>
                        <group>
                            <field name="property_type_id" options="{'no_create': true}"/>
                            <field name="postcode"/>
                            <field name="date_availability"/>
                        </group>
                        <group>
                            <field name="expected_price"/>
                            <field name="best_offer"/>
                            <field name="selling_price"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description"/>
                                <field name="bedrooms"/>
                                <field name="living_area"/>
                                <field name="facades"/>
                                <field name="garage"/>
                                <field name="garden"/>
                                <field name="garden_area" invisible = "not garden"/>
                                <field name="garden_orientation" invisible = "not garden"/>
                                <field name="total_area"/>
                            </group>
                        </page>
                        <page string="Offers">
                            <field name="offer_ids" string="Offers" readonly="status in ('offer_accepted','cancelled','sold')"/>
                        </page>
                        <page string="Other Info">
                            <group>
                                <field name = "salesperson_id"/>
                                <field name = "buyer_id"/>
                                <field name = "company_id"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <chatter open_attachments="True"/>
            </form>
        </field>
    </record>

    <record id="estate_property_view_search" model="ir.ui.view">
        <field name="name">estate.property.view.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Search">
                <field name="name"/>
                <field name="expected_price"/>
                <field name="bedrooms"/>
                <field name="postcode"/>
                <field name="living_area" filter_domain="[('living_area','>=',self)]"/>
                <field name="facades"/>
                <filter name='available_property' string='Available' domain="['|',('status','=','new'),('status','=','offer_received')]"/>
                <group string='Group By'>
                    <filter name='postcode' context="{'group_by':'postcode'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="context">{'search_default_available_property': True}</field>
        <field name="view_mode">kanban,list,form</field>
    </record>
</odoo>
