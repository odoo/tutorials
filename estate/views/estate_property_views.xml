<?xml version="1.0" encoding="utf-8"?>

<odoo>

    <record id="estate_action_advertisements" model="ir.actions.act_window">
        <field name="name">Test action</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form</field>
    </record>

    <record id="estate_advertisements_list" model="ir.ui.view">    
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="Real Estates">
                <field name="name"/>
                <field name="postcode"/>
                <field name="bedrooms"/>
                <field name="living_area"/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="date_availability"/>
                <field name="property_type_id"/>
                <field name="property_tag_ids" widget="many2many_tags"/>
            </list>
        </field>
    </record>

    <record id="estate_property_view_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="sell_property" type="object" string="SOLD" 
                        invisible="(
                            state in ('sold', 'cancelled', 'offer-accepted') 
                            )"/>
                    <button name="sell_property" class="oe_highlight" type="object" string="SOLD" 
                        invisible="(
                            state in ('sold', 'cancelled')
                            or state != 'offer-accepted'
                            )"/>
                    <button name="cancel_property" type="object" string="CANCEL" invisible="state == 'sold'"/>
                    <field name="state" widget="statusbar" statusbar_visible="new,offer-received,offer-accepted,sold"/>
                </header>
                <sheet>
                    <group>
                        <h1>
                            <field name="name"/>
                        </h1>
                        <field name="property_tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                    </group>
                    <group>
                        <group>
                            <field name="postcode"/>
                            <field name="date_availability"/>
                        </group>
                        <group>
                            <field name="expected_price"/>
                            <field name="selling_price"/>
                            <field name="best_price"/>
                        </group>
                    </group>
                    
                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description"/>
                                <field name="bedrooms"/>
                                <field name="living_area"/>
                                <field name="facades"/>
                                <field name="garage"/>
                                <field name="garden"/>
                                <field name="garden_area" invisible="not garden"/>
                                <field name="garden_orientation" invisible="not garden"/>
                                <field name="property_type_id" options="{'no_create_edit': true, 'no_quick_create': true}"/>
                                <field name="total_area"/>
                            </group>
                        </page>
                        <page string="Offers">
                            <field name="offer_ids" >
                                <list>
                                    <field name="price"/>
                                    <field name="partner_id"/>
                                    <button name="action_validate" string=" " type="object" icon="fa-check" invisible="property_id.state in ('sold', 'cancelled')"/>
                                    <button name="action_refuse" string=" " type="object" icon="fa-times" invisible="property_id.state in ('sold', 'cancelled')"/>
                                    <field name="status"/>
                                </list>
                            </field>
                        </page>
                        <page string="Other info">
                            <group>
                                <field name="user_id"/>
                                <field name="partner_id"/>
                            </group>
                        </page>
                        
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="estate_advertisements_search" model="ir.ui.view">    
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="postcode"/>
                <field name="bedrooms"/>
                <field name="living_area"/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="date_availability"/>
                <field name="property_type_id"/>
                <field name="property_tag_ids"/>
                <separator/>
                <filter string="Available properties" name="available" domain="['|', ('state', '=', 'new'), ('state', '=', 'offer-received')]"/>
                <filter string="Postcode" name="postcode" context="{'group_by':'postcode'}"/>
            </search>
        </field>
    </record>



</odoo>
