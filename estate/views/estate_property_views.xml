<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="act_estate_property" model="ir.actions.act_window">
        <field name="name">Property</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form</field>
    </record>
    <record id="estate_property_view_list" model="ir.ui.view">
        <field name="name">ep.view.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="Estate property">
                <field name="name" string="Title"/>
                <field name="postcode" string="Postcode"/>
                <field name="bedrooms" string="Bedrooms"/>
                <field name="living_area" string="Living area (sqm)"/>
                <field name="expected_price" string="Expected price"/>
                <field name="selling_price" string="Selling price"/>
                <field name="date_availability" string="Available from"/>
            </list>
        </field>
    </record>
    <record id="estate_property_view_form" model="ir.ui.view">
        <field name="name">ep.view.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Estate property">
                <sheet>
                    <!-- IDK how to style the title to look title-ish from here -->
                    <field name="name" string="Title"/>
                    <field name="tag_ids" string="Tags" widget="many2many_tags"/>
                    <group>
                        <group>
                            <field name="property_type_id" string="Type"/>
                            <field name="postcode" string="Postcode"/>
                            <field name="date_availability" string="Available from"/>
                        </group>
                        <group>
                            <field name="expected_price" string="Expected price"/>
                            <field name="best_offer" string="Best offer"/>
                            <field name="selling_price" string="Selling price"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description" string="Description"/>
                                <field name="bedrooms" string="Bedrooms"/>
                                <field name="living_area" string="Living area (sqm)"/>
                                <field name="facades" string="Facade"/>
                            </group>
                            <group>
                                <field name="garage" string="Garage"/>
                                <field name="garden" string="Garden"/>
                                <field name="garden_area" string="Garden area (sqm)"/>
                                <field name="garden_orientation" string="Garden orientation"/>
                            </group>
                            <group>
                                <field name="active" string="Active"/>
                                <field name="state" string="State"/>
                            </group>
                        </page>
                        <page string="Offers">
                            <field name="offer_ids"/>
                        </page>
                        <page string="Other info">
                            <group>
                                <field name="buyer_id" string="Buyer"/>
                                <field name="salesperson_id" string="Salesperson"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>
    <record id="estate_propety_search_view" model="ir.ui.view">
        <field name="name">ep.view.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Search">
                <field name="name" string="Title"/>
                <field name="postcode" string="Postcode"/>
                <field name="expected_price" string="Expected price"/>
                <field name="bedrooms" string="Bedrooms"/>
                <field name="living_area" string="Living area (sqm)"/>
                <field name="facades" string="Facade"/>
                <!--Figure out how domains work and fix this, should be if date_available <= *today*-->
                <filter name="is_available" string="Available" domain="[('state', 'in', ['new', 'recieved'])]"/>
                <!--Add group by postcode when caught up-->
                <group expand="1" string="Group By">
                    <filter string="Postcode" name="postcode" context="{'group_by':'postcode', 'residual_visible':True}"/>
                </group>
            </search>
        </field>
   </record>
</odoo>