<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form</field>
    </record>
    <!-- this the list view-->
    <record id="estate_property_view_list" model="ir.ui.view">
        <field name="name">estate.property.list.view</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="Estate Properties">
                <field name="name" />
                <field name="postcode" />
                <field name="bedrooms" />
                <field name="living_area" />
                <field name="expected_price" />
                <field name="selling_price" />
                <field name="available_from" />

            </list>
        </field>
    </record>

    <!-- this the form view -->
    <record id="estate_property_view_form" model="ir.ui.view">
        <field name="name">estate.property.form.view</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="description">
                <sheet>
                    <group>
                        <field name="name" />
                    </group>

                    <group>

                        <group>
                            <field name="postcode" />
                            <field name="available_from" />
                        </group>

                        <group>
                            <field name="expected_price" />
                            <field name="selling_price" />
                        </group>
                    </group>


                    <notebook>
                        <page string="description">
                            <group>
                                <field name="description"></field>
                                <field name="bedrooms" />
                                <field name="living_area" />
                                <field name="facades" />
                                <field name="garage" />
                                <field name="garden" />
                                <field name="garden_area" />
                                <field name="garden_orientation" />
                                <field name="status"></field>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>

        </field>

    </record>
    <!-- This is the search view and filter creation-->
    <record id="estate_property_search_view" model="ir.ui.view">
        <field name="name">estate.property.search.view</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Estate Properties">
                <field name="name" />
                <field name="postcode" />
                <field name="expected_price" />
                <field name="selling_price" />
                <field name="available_from" />
                <field name="status" />

                <!-- filters-->
                <filter string="Available Properties" name="available_properties"
                    domain="[('status', 'in', ['new', 'offer_received'])]"
                ></filter>
                <filter name="sold_property" string="Sold Property"
                    domain="[('status', 'in', ['sold', 'offer_accepted'])]"
                ></filter>

                <!-- Group By-->
                <group expand="0" string="Group by">
                    <filter string="Postcode" name="group_by_postcode"
                        context="{'group_by': 'postcode'}" />
                </group>
            </search>
        </field>
    </record>

</odoo>