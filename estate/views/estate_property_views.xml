<odoo>
    <record id="estate_property_view_tree" model="ir.ui.view">
        <field name="name">estate.property.view.tree</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list decoration-success="state in ['offer_received', 'offer_accepted']" decoration-bf="state == 'offer_accepted'" decoration-muted="state == 'sold'">
                <field name="name" />
                <field name="postcode" />
                <field name="bedrooms" />
                <field name="living_area" />
                <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}" />
                <field name="expected_price" />
                <field name="selling_price" />
                <field name="date_availability" optional="hide" />
            </list>
        </field>
    </record>
    <record id="estate_property_view_form" model="ir.ui.view">
        <field name="name">estate.property.view.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Estate Property Form">
                <header>
                    <button name="action_property_sold" type="object" string="SOLD" invisible="state not in ['new', 'offer_received']" />
                    <button name="action_property_sold" type="object" string="SOLD" class="oe_highlight" invisible="state in ['sold', 'cancelled', 'new', 'offer_received','invoiced']" />
                    <button name="action_to_invoice_property" type="object" string="INVOICE" class="oe_highlight" invisible="state in ('new', 'offer_received', 'offer_accepted', 'cancelled', 'invoiced')"/>
                    <button name="action_property_cancelled" type="object" string="CANCEL" invisible="state in ['sold', 'cancelled','invoiced']" />
                    <field name="state" widget="statusbar" statusbar_visible="new,offer_received,offer_accepted,sold" />
                </header>
                <sheet>
                    <group>
                        <h1>
                            <field name="name" placeholder="Name" />
                        </h1>
                        <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}" />
                    </group>
                    <group>
                        <group>
                            <field name="property_type_id" options="{'no_create': True}" />
                            <field name="postcode" />
                            <field name="date_availability" />
                        </group>
                        <group>
                            <field name="expected_price" />
                            <field name="best_price" />
                            <field name="selling_price" />
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description" />
                                <field name="bedrooms" />
                                <field name="living_area" />
                                <field name="facades" />
                                <field name="garage" />
                                <field name="garden" />
                                <field name="garden_area" invisible="not garden" />
                                <field name="garden_orientation" invisible="not garden" />
                                <field name="total_area" />
                            </group>
                        </page>
                        <page string="Offers">
                            <field name="offer_ids" readonly="state in ['sold', 'cancelled', 'offer_accepted']">
                                <list editable="bottom" decoration-success="status == 'accepted'" decoration-danger="status == 'refused'">
                                    <field name="price" />
                                    <field name="partner_id" />
                                    <field name="validity" />
                                    <field name="date_deadline" />
                                    <button name="action_confirm" type="object" icon="fa-check" help="Accept" string=" " invisible="status in ['accepted', 'refused']" />
                                    <button name="action_refuse" type="object" icon="fa-times" help="Refuse" string=" " invisible="status in ['accepted', 'refused']" />
                                </list>
                            </field>
                        </page>
                        <page string="Other Info">
                            <group>
                                <field name="sales_person" />
                                <field name="partner_id" />
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <chatter />
            </form>
        </field>
    </record>
    <record id="estate_property_view_search" model="ir.ui.view">
        <field name="name">estate.property.view.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Estate Property Search">
                <field name="name" />
                <field name="postcode" />
                <field name="expected_price" />
                <field name="bedrooms" />
                <field name="living_area" filter_domain="['|', ('living_area', '&gt;', self), ('living_area', '=', self)]" />
                <field name="facades" />
                <filter string="Available" name="date_availability" domain="[('date_availability', '>=', context_today())]" />
                <filter string="New or Offer Recieved" name="state" domain="[('date_availability', '>=', context_today()), '|', ('state', '=', 'new'),('state', '=', 'offer_received')]" />
                <separator />
                <group string="Group By">
                    <filter string="Postcode" name="postcode" context="{'group_by':'postcode'}" />
                </group>
            </search>
        </field>
    </record>

    <record id="estate_property_view_kanban" model="ir.ui.view">
        <field name="name">estate_property.view.kanban</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <kanban default_group_by="property_type_id" records_draggable="False" groups_draggable="False">
                <field name="state" />
                <templates>
                    <t t-name="kanban-box">
                        <div name="card">
                            <strong>
                                <field name="name" />
                            </strong>
                            <div class="mt-2"> Expected Price : <field name="expected_price" />
                            </div>
                            <div t-if="record.state.raw_value == 'offer_received'"> Best price : <field name="best_price" />
                            </div>
                            <div t-if="record.state.raw_value == 'offer_accepted'"> Selling Price : <field name="selling_price" />
                            </div>
                            <field name="tag_ids" options="{'color_field': 'color'}" class="mt-1" />
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>
    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Estate Property</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form,kanban</field>
        <field name="context">{'search_default_date_availability': True}</field>
    </record>
</odoo>