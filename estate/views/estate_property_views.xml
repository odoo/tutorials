<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="estate_property_view_kanban" model="ir.ui.view">
        <field name="name">estate.property.kanban</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <kanban default_group_by="property_type_id" records_draggable="0">
                <field name="status"/>
                <templates>
                    <t t-name="card">
                        <div class="o_kanban_record">
                            <h3><field name="name"/></h3>
                            <div class="o_kanban_details">
                                <strong>Expected price: </strong>
                                <field name="expected_price"/>
                            </div>
                            <t t-if="record.status.raw_value == 'offer_received'">
                                <div class="o_kanban_details">
                                    <strong>Best offer: </strong>
                                    <field name="best_price"/>
                                </div>
                            </t>
                            <t t-if="record.status.raw_value == 'offer_accepted' or record.status.raw_value == 'sold'">
                                <div class="o_kanban_details">
                                    <strong>Selling price: </strong>
                                    <field name="selling_price"/>
                                </div>
                            </t>
                            <footer class="o_kanban_footer">
                                <field name="property_tag_id" widget="many2many_tags" options="{'color_field': 'color'}"/>
                            </footer>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="view_estate_property_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Estate Property">
               <header>
                    <button name="action_set_sold" type="object" string="Mark as Sold"
                        class="btn-primary" invisible ="status == 'sold' or status == 'cancelled'"/>
                    <button name="action_cancel" type="object" string="Cancel"
                        class="btn-secondary"  invisible ="status == 'sold' or status == 'cancelled' "/>
                     <field name="status" widget="statusbar"/>
                </header>
                <sheet>
                    <h1>
                        <field name="name"/>
                    </h1>
                    <h3>
                        <field name="property_tag_id" widget='many2many_tags'  options="{'color_field': 'color'}"/>
                    </h3>
                    <field name="image_1920" widget='image' optional="show" class="oe_avatar" style="max-width: 150px; max-height: 150px;" />
                <group>
                    <group>
                        <field name="property_type_id"/>
                        <field name="postcode"/>
                        <field name="date_availability"/>
                    </group>
                    <group>
                        <field name="expected_price"/>
                        <field name="selling_price"/>
                        <field name="best_price"/>
                    </group>
                </group>
                    <notebook>
                        <page string="Description">
                        <group>
                            <field name="description"/>
                            <field name="bedrooms"/>
                            <field name="living_area"/>
                            <field name="facades"/>
                            <field name="garage"/>
                            <field name="garden"/>
                            <field name="garden_area" invisible="not garden"/>
                            <field name="garden_orientation" invisible="not garden"/>
                            <field name="total_area"/>
                        </group>
                        </page>
                           <page string="Offers">
                            <field name="offer_ids" string="Offers" readonly="status in ('offer_accepted','cancelled','sold')"/>
                        </page>
                        <page string="Other Info">
                            <group>
                                <field name = "salesperson_id"/>
                                <field name = "buyer_id"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_estate_property_tree" model="ir.ui.view">
        <field name="name">estate.property.tree</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="Estate Properties"
                decoration-success="status == 'offer received'"
                decoration-warning="status == 'offer accepted'"
                decoration-muted="status == 'sold'">
                 <header>
                    <button name="%(estate_offer_wizard)d" type="action" string="Add Offer" class="oe_highlight"/>
                </header>
                <field name="name" string="Title" />
                <field name="property_type_id" options="{'no_create': True, 'no_open': True}"/>
                <field name="postcode" />
                <field name="bedrooms" />
                <field name="living_area" string="Living area(sqm)" />
                <field name="expected_price" />
                <field name="selling_price" />
                <field name="date_availability" string="Available from" optional="hide"/>
            </list>
        </field>
    </record>

     <record id="search_view" model="ir.ui.view">
        <field name="name">SearchView</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Search">
                <field name="name"/>
                <field name="expected_price"/>
                <field name="bedrooms"/>
                <field name="postcode"/>
                <field name="living_area" filter_domain="[('living_area','>=',self)]"/>
                <field name="facades"/>
                <filter name='available_property' string='Available' domain="['|',('status','=','new'),('status','=','offer received')]"/>
                <group string='Group By'>
                    <filter name='postcode' context="{'group_by':'postcode'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Property </field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="context">{'search_default_available_property': True }</field>
    </record>
</odoo>
