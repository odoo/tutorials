<?xml version="1.0"?>
<odoo>
    <!-- Action Window -->
    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- List View -->
    <record id="estate_properties_list_view" model="ir.ui.view">
        <field name="name">estate.list.view</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list multi_edit="1">
                <field name="name" string="Name"/>
                <field name="property_type_id" string="Property Type"/>
                <field name="postcode" string="Postcode"/> 
                <field name="bedrooms" string="Bedrooms"/>
                <field name="measurement_unit" string="Unit" widget="selection"/> 
                <field name="living_area" string="Living Area"/>
                <field name="garden_area" string="Garden Area"/>
                <field name="total_area" string="Total Area" /> 
                <field name="expected_price" string="Expected Price" />
                <field name="selling_price" string="Selling Price"/>
                <field name="date_availability" string="Available From"/>
                <field name="tag_ids" string="Tags" widget="many2many_tags"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="estate_property_form_view" model="ir.ui.view">
        <field name="name">estate.form.view</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Property Form">
                <header>
                <button name="property_sold_action" type="object" string="Sold"/>
                <button name="property_cancel_action" type="object" string="Cancelled"/>
                <!-- <field name="state" widget="statusbar" statusbar_visible="new, sold, offer_recived, offer_accepted" /> -->
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="state" string="State"/>
                            <field name="name" string="Name"/>
                            <field name="property_type_id" string="Property Type"/>
                            <field name="postcode" string="Post Code"/>
                            <field name="date_availability" string="Available From"/>
                        </group>
                        <group>
                            <field name="expected_price" string="Expected Price" />
                            <field name="selling_price" string="Selling Price" />
                            <field name="best_price" string = "Best Offer"/>
                            <field name="tag_ids" string="Tags" widget="many2many_tags"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Property Details">
                            <group>
                                <group>
                                    <field name="description"/>
                                    <field name="bedrooms" string="Bedrooms"/>
                                    <field name="measurement_unit" string="Unit of Measurement" widget="selection"/>   <!-- Dropdown for unit selection -->
                                    <field name="living_area" string="Living Area"/>
                                    <field name="facades" string="Facades"/>
                                    <field name="is_garage" string="Garage"/>
                                    <field name="is_garden" string="Garden"/>
                                    <field name="garden_area" string="Garden Area"/>
                                    <field name="total_area" string="Total Area"/>
                                    <field name="garden_orientation" string="Garden Orientation"/>
                                </group>
                            </group>
                        </page>
                        <page string="Offers">
                            <group>
                                <field name="offer_ids" nolabel="1"/>
                            </group>
                        </page>
                        <page string="Other Info">
                            <group>
                                <group>
                                    <field name="buyer_id" string="Buyer"/>
                                    <field name="seller_id" string="Salesman"/>
                                </group>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Search View -->
    <record id="estate_property_search_view" model="ir.ui.view">
        <field name="name">estate.search.view</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="property_type_id"/>
                <field name="postcode"/>
                <field name="expected_price"/>
                <field name="bedrooms"/>
                <field name="living_area" filter_domain="[('living_area', '>=', self)]"/>
                <field name="facades"/>
                <field name="is_garage"/>
                <field name="is_garden"/>
                <filter string="Available" name="available" domain="[('state', 'in', ('new', 'offer_received'))]"/>
                <group expand="1" string="Group By">
                    <filter string="Postcode" name="group_by_postcode" context="{'group_by': 'postcode'}"/>
                </group>
            </search>
        </field>
    </record>
</odoo>
