<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="estate_property_offer_view_list" model="ir.ui.view">
        <field name="name">estate.property.offer.view.list</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <list string="Offers" editable="bottom" decoration-success="status == 'accepted'" decoration-danger="status == 'refused'">
                <field name="price" readonly="status != 'pending'"/>
                <field name="partner_id" readonly="status != 'pending'"/>
                <field name="validity" readonly="status != 'pending'"/>
                <field name="date_deadline" readonly="status != 'pending'"/>
                <button name="action_accept" icon="fa-check" type="object" title="Accept" invisible="status != 'pending'"/>
                <button name="action_refuse" icon="fa-times" type="object" title="Refused" invisible="status != 'pending'"/>
                <button name="action_revert" icon="fa-undo" type="object" title="Revert" invisible="status == 'pending'"/>
                <field name="status" widget="badge" decoration-success="status == 'accepted'" decoration-danger="status == 'refused'"/>
            </list>
        </field>
    </record>

    <record id="estate_property_offer_action" model="ir.actions.act_window">
        <field name="name">Property Offers</field>
        <field name="res_model">estate.property.offer</field>
        <field name="view_mode">list</field>
        <field name="domain">[('property_type_id', '=', active_id)]</field>
    </record>
</odoo>
