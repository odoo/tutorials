<?xml version="1.0"?>
<odoo>

    <record id="estate.estate_property_offer_view_list" model="ir.ui.view">
        <field name="name">estate.property.offer.list</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <list editable="top" decoration-success="status == 'accepted'" decoration-danger="status == 'refused'">
                <field name="price" string="Price"/>
                <field name="partner_id" string="Partner"/>
                <field name="status" string="Status" optional="hidden"/>
                <field name="validity" string="Validity (days)"/>
                <field name="date_deadline" string="Deadline"/>
                <button 
                    name="action_accept_offer"
                    string="Accept"
                    type="object"
                    icon="fa-check"
                    invisible="status == 'accepted' or status == 'refused'"/>
                <button
                    name="action_reject_offer"
                    string="Refuse"
                    type="object"
                    icon="fa-trash"
                    invisible="status == 'accepted' or status == 'refused'"/>
            </list>
        </field>
    </record>

    <record id="estate.estate_property_offer_list_form" model="ir.actions.act_window">
        <field name="name">Offers</field>
        <field name="res_model">estate.property.offer</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
