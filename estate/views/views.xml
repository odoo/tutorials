<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="some_model_action_1" model="ir.actions.act_window">
        <field name="name">Buildings</field>
        <field name="res_model">estate.building</field>
        <field name="view_mode">list,form,kanban</field>
        <field name="context">{'search_default_Available': True}</field>
    </record>

    <record id="some_model_action_2" model="ir.actions.act_window">
        <field name="name">Building Types</field>
        <field name="res_model">estate.building_type</field>
        <field name="view_mode">list,form</field>
    </record>

    <record id="some_model_action_3" model="ir.actions.act_window">
        <field name="name">Offers</field>
        <field name="res_model">estate.offer</field>
        <field name="view_mode">list</field>
        <field name="domain">[('property_type_id', '=', active_id)]</field>
    </record>

    <record id="list_view_" model="ir.ui.view">
        <field name="name">estate.building.list</field>
        <field name="model">estate.building</field>
        <field name="arch" type="xml">
            <list string="test_list" decoration-success="state in ('offer_received', 'offer_accepted')" decoration-bf="state=='offer_accepted'" decoration-muted="state=='sold'">
                <field name="name"/>
                <field name="number_of_rooms"/>
                <field name="value"/>
                <field name="state"/>
                <field name="building_type_id"/>
                <field name="post_code" optional="hidden"/>
            </list>
        </field>
    </record>

    <record id="first_form_view" model="ir.ui.view">
        <field name="name">estate.building.form</field>
        <field name="model">estate.building</field>
        <field name="arch" type="xml">
            <form string="Test">
                <header>
                    <button name="action_set_sold" type="object" string="Sell Building" invisible="state in ('sold', 'canceled')"/>
                    <button name="action_set_canceled" type="object" string="Cancel Building" invisible="state in ('canceled', 'sold')"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                            <field name="value"/>
                            <field name="state" widget="statusbar" statusbar_visible="new,offer_received,offer_accepted,sold"/>
                            <field name="availability_date"/>
                            <field name="building_type_id" options="{'no_create': true}"/>
                        </group>
                        <group>
                            <field name="number_of_rooms"/>
                            <field name="post_code"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description"/>
                                <field name="building_area"/>
                                <field name="has_garden"/>
                                <field name="garden_area" invisible="not has_garden"/>
                                <field name="garden_orientation" invisible="not has_garden"/>
                                <field name="total_area"/>
                            </group>
                        </page>
                        <page string="Info">
                            <group>
                                <field name="buyer_id"/>
                                <field name="salesperson_id"/>
                            </group>
                        </page>
                        <page string="Offers">
                            <field name="offer_ids" readonly="state in ('sold', 'canceled', 'offer_accepted')"> 
                                <list edit="true" editable="bottom" decoration-danger="status=='refused'" decoration-success="status=='accepted'">
                                    <field name="price"/>
                                    <field name="partner_id"/>
                                    <field name="date_deadline"/>
                                    <field name="validity"/>
                                    <button name="action_accept_offer" type="object" string="Accept" icon="fa-check" invisible="status in ('accepted', 'refused')"/>
                                    <button name="action_refuse_offer" type="object" string="Refuse" icon="fa-times" invisible="status in ('accepted', 'refused')"/>
                                </list>
                            </field>
                            <group>
                                <field name="best_price"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="first_search_view" model="ir.ui.view">
        <field name="name">estate.building.search</field>
        <field name="model">estate.building</field>
        <field name="arch" type="xml">
            <search string="test_search">
                <field name="name"/>
                <field name="value"/>
                <field name="building_area" string="area size" filter_domain="[('building_area', '>=', self)]"/>
                <separator/>
                <filter string="Available" name="Available" domain="[('state', 'in', ['new', 'offer_received'])]"/>
                <filter string="postcode" name="postcode" context="{'group_by':'post_code'}"/>
            </search>

        </field>
    </record>

    <record id="building_type_form_view" model="ir.ui.view">
        <field name="name">estate.building_type.form</field>
        <field name="model">estate.building_type</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="%(some_model_action_3)d" type="action" string="Offers" class="oe_stat_button"/>
                </header>
                <sheet>
                    <group>
                        <field name="name"/>
                    </group>
                    <notebook>
                        <page string="Buildings">
                            <field name="building_ids">
                                <list>
                                    <field name="name"/>
                                    <field name="value"/>
                                    <field name="state"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="building_type_list_view" model="ir.ui.view">
        <field name="name">estate.building_type.list</field>
        <field name="model">estate.building_type</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="sequence" widget="handle"/>
            </list>
        </field>
    </record>
    <record id="res_users_view_form" model="ir.ui.view">
        <field name="name">test_inheretance</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook/page[@name='preferences']" position="after">
                <page string="Buildings">
                    <field name="building">
                        <list>
                            <field name="name"/>
                            <field name="value"/>
                            <field name="state"/>
                        </list>
                    </field>
                </page>
            </xpath>
        </field>
    </record>
    <record id="kanban_1" model="ir.ui.view">
        <field name="name">estate.building.kanban</field>
        <field name="model">estate.building</field>
        <field name="arch" type="xml">
            <kanban default_group_by="building_type_id">
                <template>
                    <t t-name="card">
                        <div>
                            <field name="name"/>
                            <div>
                                <strong>Price:</strong>
                                <span><field name="value"/></span>
                            </div>
                            <div>
                                <strong>State:</strong>
                                <span><field name="state"/></span>
                            </div>
                            <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                        </div>
                    </t>
                </template>
            </kanban>
        </field>
    </record>
</odoo>
