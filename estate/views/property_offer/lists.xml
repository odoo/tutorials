<?xml version="1.0"?>
<odoo>
    <record id="property_offer_list" model="ir.ui.view">
        <field name="name">estate.property.offer.list</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <list string="Offer" editable="bottom"
                decoration-danger="status == 'refused'"
                decoration-success="status == 'accepted'">
                <field name="price"/>
                <field name="partner_id"/>
                <field name="property_type_id"/>
                <field name="status" invisible="True"/>
                <field name="validity"/>
                <field name="date_deadline"/>
                <button name="action_accept" type="object" string="Accept" icon="fa-check-circle" invisible="status"/>
                <button name="action_refuse" type="object" string="Refuse" icon="fa-times-circle" invisible="status"/>
            </list>
        </field>
    </record>
</odoo>