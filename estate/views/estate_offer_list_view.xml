<?xml version="1.0"?>
<odoo>
    <record id="estate_offer_list_view" model="ir.ui.view">
        <field name="name">estate.offer.list</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <list string="Tests" editable='top' decoration-success="status=='accepted'" decoration-danger="status=='refused'">
                <field name="price"/>
                <field name="partner_id"/>
                <field name="validity"/>
                <field name="date_deadline"/>
                <button name="action_confirm_offer" string="Confirm offer" type="object" icon="fa-check"/>
                <button name="action_refuse_offer" string="Refuse offer" type="object" icon="fa-times"/>
            </list>
        </field>
    </record>
</odoo>
