<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Define the action for Estate Property Type -->
    <record id='action_estate_property_type' model='ir.actions.act_window'>
        <field name="name">Property Types</field>
        <field name="res_model">estate.property.type</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- Define List View for Estate Property Type -->
    <record id="property_type_view" model="ir.ui.view">
        <field name="name">estate.property.type.list</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <list>
                <field name="name" />
            </list>
        </field>
    </record>

    <!-- Define Form View for Estate Property Type -->
    <record id="property_type_form" model="ir.ui.view">
        <field name="name">estate.property.type.form</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <form string="Property Type">
                <sheet>
                    <h1>
                        <field name="name" />
                    </h1>
                </sheet>
            </form>
        </field>
    </record>

</odoo>
