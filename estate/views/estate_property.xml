<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="estate_property_view_list" model="ir.ui.view">
        <field name="name">estate.property.view.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="Properties" decoration-muted="state == 'cancelled'" decoration-success="state in ['sold', 'offer_accepted']" decoration-bf="state == 'sold'" decoration-info="state == 'new'">
                <field name="name"/>
                <field name="postcode"/>
                <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                <field name="bedrooms"/>
                <field name="living_area"/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="date_availability" optional="hide" />
            </list>
        </field>
    </record>

    <record id="estate_property_view_kanban" model="ir.ui.view">
        <field name="name">estate.property.view.kanban</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <kanban string="Properties" default_group_by="property_type_id" records_draggable="0" group_create="0" quick_create="0">
                <field name="state"/>
                <templates>
                    <t t-name="card">
                        <widget name="web_ribbon" title="new" bg_color="text-bg-info" t-if="record.state.raw_value == 'new'"/>
                        <div>
                            <field name="name" class="fw-bold fs-4"/>
                            <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                            <table class="mt-2">
                                <tr>
                                    <td class="pe-2">Expected Price</td>
                                    <td>
                                        <field name="expected_price"/>
                                    </td>
                                </tr>
                                <tr t-if="record.state.raw_value == 'offer_received'">
                                    <td class="pe-2">Best Price</td>
                                    <td>
                                        <field name="best_price"/>
                                    </td>
                                </tr>
                                <tr t-if="record.state.raw_value == 'offer_accepted'">
                                    <td class="pe-2">Selling Price</td>
                                    <td>
                                        <field name="selling_price"/>
                                    </td>
                                </tr>
                            </table>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="estate_property_view_form" model="ir.ui.view">
        <field name="name">estate.property.view.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Property">
                <header>
                    <button name="action_sold" string="Sold" type="object" class="oe_highlight" invisible="state in ['sold', 'cancelled']"/>
                    <button name="action_cancel" string="Cancel" type="object" invisible="state in ['sold', 'cancelled']" />
                    <field name="state" widget="statusbar" />
                </header>

                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" class="mb16"/>
                        </h1>
                    </div>
                    <group class="mb-2">
                        <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}" nolabel="1"/>
                    </group>

                    <group>
                        <group>
                            <field name="property_type_id" options="{'no_create': True}"/>
                            <field name="postcode"/>
                            <field name="date_availability"/>
                        </group>
                        <group>
                            <field name="expected_price"/>
                            <field name="best_price"/>
                            <field name="selling_price"/>
                        </group>
                    </group>

                    <notebook>
                        <page name="description" string="Description">
                            <group>
                                <field name="description"/>
                                <field name="bedrooms"/>
                                <field name="living_area"/>
                                <field name="facades"/>
                                <field name="garage"/>
                                <field name="garden"/>
                                <field name="garden_area" invisible="not garden"/>
                                <field name="garden_orientation" invisible="not garden"/>
                                <field name="total_area"/>
                            </group>
                        </page>

                        <page name="offers" string="Offers">
                            <field name="offer_ids"/>
                        </page>

                        <page name="other_info" string="Other Info">
                            <group>
                                <field name="seller_id"/>
                                <field name="buyer_id"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="estate_property_view_search" model="ir.ui.view">
        <field name="name">estate.property.view.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Properties">
                <field name="name"/>
                <field name="postcode"/>
                <field name="property_type_id"/>
                <field name="expected_price" string="Minimal Price" filter_domain="[('expected_price', '&gt;=', self)]"/>
                <field name="expected_price" string="Maximal Price" filter_domain="[('expected_price', '&lt;=', self)]"/>
                <field name="bedrooms"/>
                <field name="living_area" filter_domain="[('living_area', '&gt;=', self)]"/>
                <field name="facades"/>
                <field name="tag_ids"/>

                <filter name="available" string="Available" domain="[('state', 'in', ['new', 'offer_received'])]" />
                <filter name="available_now" string="Available Now" domain="[('state', 'in', ['new', 'offer_received']), ('date_availability', '&lt;=', context_today())]" />

                <group name="postcode" string="Group by">
                    <filter string="State" name="group_by_state" domain="[]" context="{'group_by': 'state'}"/>
                    <filter string="Postcode" name="group_by_postcode" domain="[]" context="{'group_by': 'postcode'}"/>
                    <filter string="Property Type" name="group_by_property_type" domain="[]" context="{'group_by': 'property_type_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,kanban,form</field>
        <field name="context">{'search_default_available': True}</field>
    </record>
</odoo>
