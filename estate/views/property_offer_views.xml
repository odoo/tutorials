<?xml version="1.0"?>
<odoo>
    <record id="property_offer_view_list" model="ir.ui.view">
        <field name="name">property.offer.list</field>
        <field name="model">property.offer</field>
        <field name="arch" type="xml">
            <list string="Channel" editable="bottom"
            decoration-success="status=='accepted'"
            decoration-danger="status=='refused'">
                <field name="price"/>
                <field name="partner_id"/>
                <field name="validity"/>
                <field name="date_deadline"/>
                <button name="action_set_accepted" type="object" icon="fa-check" title="Accept"
                invisible="status == 'accepted' or status == 'refused'"/>
                <button name="action_set_refused" type="object" icon="fa-times" title="Refuse"
                invisible="status == 'accepted' or status == 'refused'"/>
                <field name="status" optional="hidden"/>                
            </list>
        </field>
    </record>

    <record id="estate_offer_view_form" model="ir.ui.view">
        <field name="name">property.offer.form</field>
        <field name="model">property.offer</field>
        <field name="arch" type="xml">
            <form string="Type">
                <sheet>
                    <group>
                        <field name="price"/>
                        <field name="partner_id"/>
                        <field name="status"/>
                        <field name="validity"/>
                        <field name="date_deadline"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
</odoo>
