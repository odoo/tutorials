<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Agent definition -->
    <record id="estate_group_user" model="res.groups">
        <field name="name">Agent</field>
        <field name="category_id" ref="base.module_category_real_estate_brokerage"></field>
        <field name="comment">This user will be able to create and manage real estate properties</field>
    </record>

    <!-- Manager definition -->
    <record id="estate_group_manager" model="res.groups">
        <field name="name">Manager</field>
        <field name="category_id" ref="base.module_category_real_estate_brokerage"></field>
        <field name="implied_ids" eval="[(4, ref('estate.estate_group_user'))]"></field>
    </record>

    <!-- Agent Access rights -->
    <record id="property_access_rule" model="ir.rule">
        <field name="name">Agent Read Properties</field>
        <field name="model_id" ref="estate.model_estate_property"/>
        <field name="perm_read" eval="True" />
        <field name="perm_write" eval="True" />
        <field name="groups" eval="[Command.link(ref('estate.estate_group_user'))]" />
        <field name="domain_force">[('company_id', 'in', company_ids)]</field>
    </record>

    <record id="estate_manager_rule" model="ir.rule">
        <field name="name">Estate Manager Rules</field>
        <field name="model_id" ref="estate.model_estate_property"/>
        <field name="groups" eval="[Command.link(ref('estate.estate_group_manager'))]" />
        <field name="domain_force">[]</field>
    </record>

</odoo>
