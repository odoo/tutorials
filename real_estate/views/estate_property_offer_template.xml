<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <template id="estate_property_offer_template" name="Make an Offer">
        <t t-call="website.layout">
            <div class="container mt-4">
                <div class="row justify-content-center">
                    <div class="col-md-6">
                        <div class="card shadow-sm border-0 p-4">
                            <h2 class="text-center text-dark">Make an Offer</h2>
                            <form action="/submit-offer" method="post">
                                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                <input type="hidden" name="property_id" t-att-value="property.id" />

                                <div class="mb-3">
                                    <label class="form-label">Offer Price </label>
                                    <input type="number" name="price" class="form-control" required="true" />
                                </div>

                                <div class="mb-3">
                                    <label class="form-label">Validity (days)</label>
                                    <input type="number" name="validity" class="form-control" required="true" />
                                </div>

                                <button type="submit" class="btn btn-success w-100">
                                    Submit Offer
                                </button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </t>
    </template>
</odoo>
