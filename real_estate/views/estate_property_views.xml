<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>
        <record id="estate_property_view_list" model="ir.ui.view">
            <field name="name">estate.property.list</field>
            <field name="model">estate.property</field>
            <field name="arch" type="xml">
                <list string="Properties"
                    decoration-success="state in('offer_received', 'offer_accepted')"
                    decoration-bf="state == 'offer_accepted'"
                    decoration-muted="state == 'sold'">
                    <field name="name" string="Title" />
                    <field name="property_type_id" string="Property Type" />
                    <field name="postcode" />
                    <field name="bedrooms" />
                    <field name="living_area" string="Living Area(sqm)" />
                    <field name="expected_price" />
                    <field name="selling_price" />
                    <field name="date_availability" string="Available From" />
                </list>
            </field>
        </record>

        <record id="estate_property_view_form" model="ir.ui.view">
            <field name="name">estate.property.form</field>
            <field name="model">estate.property</field>
            <field name="arch" type="xml">
                <form string="properties">
                    <header>
                        <button name="action_sold" type="object" string="sold"
                            invisible="state in ('sold', 'cancelled')" />
                        <button name="action_cancelled" type="object" string="cancel"
                            invisible="state in ('sold', 'cancelled')" />
                        <button name="action_approve_best_offer" type="object" string="Approve Best Offer" invisible="state in ('offer_accepted', 'sold')"/>
                        <field name="state" widget="statusbar"
                            statusbar_visible="new,offer_received,offer_accepted,sold" />
                    </header>
                    <sheet>
                        <widget name="web_ribbon" title="Sold" bg_color="text-bg-success" invisible="state != 'sold'"/>
                        <group>
                            <h1>
                                <field name="name" placeholder="e.g Beach View House" readonly="state !='new'"/>
                            </h1>
                        </group>
                        <group>
                            <field name="tag_ids" widget="many2many_tags"
                                options="{'color_field': 'color'}"/>
                        </group>
                        <group>
                            <group>
                                <field name="property_type_id"
                                    options="{'no_create': True, 'no_edit': True}" readonly="state !='new'"/>
                                <field name="postcode" placeholder="e.g 123456" readonly="state !='new'"/>
                                <field name="date_availability" string="Available from"
                                    optional="hide" readonly="state !='new'"/>
                            </group>
                            <group>
                                <field name="expected_price" readonly="state !='new'"/>
                                <field name="best_price" />
                                <field name="selling_price" />
                            </group>
                        </group>

                        <notebook>
                            <page string="Description">
                                <group>
                                    <group>
                                        <field name="description" string="Description"
                                            placeholder="e.g Almost having all facilities" />
                                        <field name="bedrooms" readonly="state != 'new'"/>
                                        <field name="living_area"  readonly="state != 'new'"/>
                                        <field name="facades"
                                            help="Number of external walls (0-4). e.g A detached house has 4."  readonly="state != 'new'"/>
                                    </group>
                                    <group>
                                        <field name="garage"  readonly="state != 'new'"/>
                                        <field name="garden"  readonly="state != 'new'"/>
                                        <field name="garden_area" help="e.g 100(sqm)"
                                            invisible="not garden" readonly="state != 'new'"/>
                                        <field name="garden_orientation" invisible="not garden"  readonly="state != 'new'"/>
                                        <field name="total_area" />
                                        <field name="active" />
                                    </group>
                                </group>
                            </page>

                            <page string="Offers">
                                <field name="offer_ids"
                                    readonly="state in('offer_accepted', 'sold')">
                                    <list editable="bottom" decoration-danger="status == 'refused'"
                                        decoration-success="status == 'accepted'">
                                        <field name="price" readonly="status in ('refused')"/>
                                        <field name="partner_id" readonly="status in ('refused')"/>
                                        <button name="action_accept" type="object" icon="fa-check"
                                            title="Accept"
                                            invisible="status == 'refused' or parent.state == 'sold'" />
                                        <button name="action_refuse" type="object" icon="fa-times"
                                            title="Refuse"
                                            invisible="status == 'refused' or parent.state == 'sold'" />
                                        <field name="status"  readonly="True"/>
                                        <field name="validity"  readonly="status in ('refused', 'accepted')"/>
                                        <field name="date_deadline"  readonly="status in ('refused', 'accepted')"/>
                                    </list>
                                </field>
                            </page>

                            <page string="Other Info">
                                <group>
                                    <field name="salesperson_id" string=" Sales Man"  readonly="state in ('offer_accepted', 'sold')"/>
                                    <field name="buyer_id" string="Buyer"  readonly="state in ('offer_accepted', 'sold')"/>
                                </group>
                            </page>
                        </notebook>
                    </sheet>
                    <chatter/>
                </form>
            </field>
        </record>

        <record id="estate_property_search_view" model="ir.ui.view">
            <field name="name">estate_property_search</field>
            <field name="model">estate.property</field>
            <field name="arch" type="xml">
                <search string="Properties">
                    <field name="name" string="Title" />
                    <field name="postcode" />
                    <field name="expected_price" />
                    <field name="bedrooms" />
                    <field name="living_area"
                        string="Living Area(sqm)"
                        filter_domain="[('living_area', '>=', self)]" />
                    <field name="facades" />
                    <filter name="available_properties" string="Available Properties"
                        domain="['|',
                            ('state', '=', 'new'),
                            ('state', '=', 'offer_received')]" />
                    <filter name="luxury_properties" string="Luxury Properties"
                        domain="[
                            ('expected_price', '>=', 1000000),
                            ('living_area', '>=', 200),
                            ('bedrooms', '>=', 3),
                            ('garden', '=', True)]" />
                    <group>
                        <filter name="group_by_postcode" string="Group By Postcode"
                            context="{'group_by' : 'postcode'}" />
                    </group>
                </search>
            </field>
        </record>

        <record id="estate_property_view_kanban" model="ir.ui.view">
            <field name="name">estate.property.view.kanban</field>
            <field name="model">estate.property</field>
            <field name="arch" type="xml">
                <kanban default_group_by="state" records_draggable="false">
                    <field name="state" />
                    <field name="selling_price" />
                    <field name="best_price" />
                    <progressbar field="state" colors='{"new": "info", "sold": "success", "offer_received": "warning", "offer_accepted": "danger"}' sum_field="expected_price"/>
                    <templates>
                        <t t-name="card">
                            <div class="fw-bold fs-5 mb-1">
                                <field name="name" />
                            </div>
                            <div> Expected Price: <field name="expected_price" />
                            </div>
                            <div t-if="record.state.raw_value == 'offer_received'"> Best Price: <field
                                    name="best_price" />
                            </div>
                            <div t-if="['offer_accepted', 'sold'].includes(record.state.raw_value)">
                                Selling Price: <field name="selling_price" />
                            </div>
                            <div class="mt-1">
                                <field name="tag_ids" widget="many2many_tags" />
                            </div>
                        </t>
                    </templates>
                </kanban>
            </field>
        </record>

        <record id="estate_property_action" model="ir.actions.act_window">
            <field name="name">properties</field>
            <field name="res_model">estate.property</field>
            <field name="view_mode">kanban,list,form</field>
            <field name="context">{'search_default_available_properties': 1}</field>
        </record>
    </data>
</odoo>
