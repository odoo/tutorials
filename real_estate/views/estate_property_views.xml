<?xml version="1.0"?>
<odoo>
    <record id="estate_properties_action" model="ir.actions.act_window">
        <field name="name">Property List</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form,kanban</field>
        <field name="context">{'search_default_available': True, 'search_default_current': True}</field>
    </record>
    <record id="view_estate_property_list" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list default_order="id desc"
                decoration-success="status in ('offer_received', 'offer_accepted')"
                decoration-bf="status == 'offer_accepted'"
                decoration-muted="status == 'sold'">
                <header>
                    <button name="%(action_add_offer_wizard)d" string="Add Offer"
                    type="action" class="oe_stat_button" icon="fa-plus-square"/>
                </header>
                <field name="name"/>
                <field name="property_tag_ids" widget="many2many_tags" options="{'color_field':'color'}"/>
                <field name="postcode"/>
                <field name="bedrooms"/>
                <field name="living_area"/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="date_availability" optional="hide"/>
            </list>
        </field>
    </record>
    <record id="estate_property_view_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="set_sold" type="object" string="SOLD" invisible="status in ('sold','cancelled')"/>
                    <button name="set_cancel" type="object" string="CANCEL" invisible="status in ('sold','cancelled')"/>
                    <field name="status"  widget="statusbar" statusbar_visible="new,offer_received,offer_accepted,sold"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="%(report_real_estate_offer)d"
                                string="Print"
                                type="action"
                                icon="fa-print">
                        </button>
                    </div>
                    <widget name="web_ribbon" title="Sold" bg_color="bg-success" invisible="status not in('sold')"/>
                    <widget name="web_ribbon" title="Cancelled" bg_color="bg-danger" invisible="status not in('cancelled')"/>
                    <group>
                        <group>
                            <h1>
                                <field name="name" placeholder="Property Name"/>
                            </h1>
                        </group>
                        <group>
                            <field name="image" string="" widget="image" class="oe_avatar"/>
                        </group>
                    </group>
                    <group>
                        <group>
                            <field name="property_tag_ids" widget="many2many_tags" options="{'color_field':'color'}"/>
                            <field name="property_type_id" options="{'no_create':True}"/>
                            <field name="postcode"/>
                            <field name="expected_price"/>
                        </group>
                        <group>
                            <field name="best_price"/>
                            <field name="date_availability"/>
                            <field name="selling_price"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <group>
                                <group>
                                    <field name="description"/>
                                    <field name="bedrooms"/>
                                    <field name="living_area"/>
                                    <field name="facades"/>
                                    <field name="garage"/>
                                </group>
                                <group>
                                    <field name="garden"/>
                                    <field name="garden_area" invisible="not garden"/>
                                    <field name="total_area"/>
                                    <field name="garden_orientation" invisible="not garden"/>
                                </group>
                            </group>
                        </page>
                        <page string="Offer">
                            <field name="offer_ids" readonly="status in ('offer_accepted','sold','cancelled')" />     
                        </page>
                        <page string="Other Info">
                            <group>
                                <field name="salesperson_id"/>
                                <field name="buyer_id"/>
                                <field name="company_id"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <chatter open_attachments="True"/>
            </form>
        </field>
    </record>
    <record id="estate_property_view_kanban" model="ir.ui.view">
        <field name="name">estate.property.view.kanban</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <kanban default_group_by="property_type_id">
                <field name="property_type_id"/>
                <field name="status"/>
                <templates>
                    <t t-name="card">
                        <div class="d-flex flex-column p-2" >
                            <t t-if="record.status.raw_value == 'sold'">
                                <widget name="web_ribbon" title="Sold" bg_color="bg-success"  style="z-index: 1;"/>
                            </t>
                            <t t-if="record.status.raw_value == 'cancelled'" >
                                <widget name="web_ribbon" title="Cancelled" bg_color="bg-danger" style="z-index: 1;"/>
                            </t>
                            <div class="d-flex justify-content-between align-items-start mb-2">
                                <div class="me-2">
                                    <strong class="fw-bolder">
                                        <field name="name"/>
                                    </strong>
                                    <div>
                                        <text>Expected Price: </text>
                                        <field name="expected_price"/>
                                    </div>
                                    <div t-if="record.status.raw_value == 'offer_received'">
                                        <text>Best Offer: </text>
                                        <field name="best_price"/>
                                    </div>
                                    <div t-if="record.status.raw_value == 'offer_accepted'">
                                        <text>Selling Price: </text>
                                        <field name="selling_price"/>
                                    </div>
                                    <field name="property_tag_ids" widget="many2many_tags" options="{'color_field':'color'}"/>
                                </div>
                                <div class="d-flex align-items-center justify-content-center" style="width: 80px; height: 80px; overflow: hidden; ">
                                    <field name="image" string="" widget="image" class="oe_avatar" style="width: 100%; height: 100%;"/>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>
    <record id="estate_property_search" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Property Search">
                <field name="name" string="title" />
                <field name="postcode" string="Postcode"/>
                <field name="expected_price" string="Expected Price" />
                <field name="bedrooms" string="Bedrooms"/>
                <field name="living_area" string="Living Area" filter_domain="[('living_area', '>=', self)]"/>
                <field name="facades" string="Facades"  />
                <separator/>
                <filter string="Available" name="available" domain="[('status', 'not in', ['sold', 'cancelled'])]"/>
                <filter string="Unavailable" name="unavailable" domain="[('status', 'in', ['sold', 'cancelled'])]"/>
                <filter string="Sold Properties" name="status" domain="[('status', '=', 'sold')]"/>
                <filter string="New or Offer Received" name="status" domain="['|',('status', '=', 'new'),('status', '=', 'offer_received')]"/>
                <group expand="1" string="Group By">
                    <filter string="Postcode" name="postcode" context="{'group_by':'postcode'}"/>
                </group>
            </search>
        </field>
    </record>
</odoo>
