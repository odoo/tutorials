<?xml version="1.0"?>
<odoo>
    <record id="test_model_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form,search,kanban</field>
        <field name="context">{'search_default_available': 1}</field>
    </record>
    <record id="estate_view_tree" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="Estate" decoration-success="state in ('offered', 'accepted')" decoration-bf="state=='accepted'" decoration-muted="state=='sold'">
                <field name="name"/>
                <field name="description"/>
                <field name="postcode"/>
                <field name="garden_area"/>
                <field name="best_offer"/>
                <field name="date_availability" optional="True"/>
            </list>
        </field>
    </record>
    <record id="estate_view_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Estate Form">
                <header>
                    <button name="action_set_sold" string="Sold" type="object" class="oe_highlight" invisible="state in ('sold', 'cancelled')"/>
                    <button name="action_set_cancel" string="Cancel" type="object" invisible="state in ('sold', 'cancelled')"/>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <notebook>
                        <page string="Primary Details">
                            <h1 class="mb32">
                                <span class="o_stat_text">Primary Details</span>
                            </h1>
                            <group>
                                <field name="name"/>
                                <field name="description"/>
                                <field name="tags" widget="many2many_tags" options="{'color_field': 'color'}"/>
                                <field name="best_offer"/>
                            </group>
                            <h1 class="mb32">
                                <span class="o_stat_text">Property Details</span>
                            </h1>
                            <group>
                                <field name="property_type" options="{'no_create': true}"/>
                                <field name="bedrooms"/>
                                <field name="living_area"/>
                                <field name="facades"/>
                                <field name="garage"/>
                                <field name="garden"/>
                                <field name="garden_area" invisible="not garden"/>
                                <field name="garden_orientation" invisible="not garden"/>
                                <field name="total_area"/>
                            </group>
                            <h1 class="mb32">
                                <span class="o_stat_text">Availability Details</span>
                            </h1>
                            <group>
                                <field name="postcode"/>
                                <field name="date_availability" eval="DateTime.now()"/>
                                <field name="expected_price"/>
                                <field name="selling_price"/>
                                <field name="active"/>
                            </group>
                        </page>
                        <page string="Buyer &amp; Sales">
                            <group>
                                <field name="sales_person" string="Sales Person"/>
                                <field name="buyer" string="Buyer"/>
                            </group>
                        </page>
                        <page string="Offers">
                            <group>
                                <field name="offer_ids" readonly="state in ('accepted', 'sold', 'cancelled')"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>
    <record id="view_estate_search" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search>
                <field name="name" string="Estate Name"/>
                <field name="description"/>
                <field name="active"/>
                <field name="postcode"/>
                <filter name="available" string="Available" domain="['|', ('state', '=', 'new'), ('state', '=', 'offer_received')]"/>
                <filter name="inactive" string="Archived" domain="[('active', '=', False)]"/>
                <field name="living_area" string="Living Area (sqm)" filter_domain="[('living_area', '&gt;=', self)]"/>
                <group>
                    <filter name="postcode" string="Postcode" context="{'group_by':'postcode'}"/>
                </group>
            </search>
        </field>
    </record>
    <record id="view_estate_property_kanban" model="ir.ui.view">
        <field name="name">estate.property.kanban</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <kanban default_group_by="property_type" records_draggable="False">
                <templates>
                    <t t-name="card">
                        <field name="name"/>
                        <field name="state"/>
                        <field name="expected_price"/>
                        <div t-if="record.state.raw_value=='offered'">
                            <label for="best_offer" class="o_form_label">Best Offer: </label>
                            <field name="best_offer"/>
                        </div>
                        <div t-if="record.state.raw_value=='accepted'">
                            <label for="selling_price" class="o_form_label">Selling Price: </label>
                            <field name="selling_price"/>
                        </div>
                        <div class="mt-1">
                            <field name="tags" widget="many2many_tags" options="{'color_field': 'color'}"/>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>
</odoo>
