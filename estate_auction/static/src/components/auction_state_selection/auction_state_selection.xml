<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="estate_auction.AuctionStateSelection"
       t-inherit="web.StateSelectionField"
       t-inherit-mode="primary">

        <!-- Main Button -->
        <xpath expr="//Dropdown/button" position="attributes">
            <attribute name="class" remove="p-0" add="shadow-none"/>
            <attribute name="t-att-class">
                getTogglerClass(currentValue)
            </attribute>
        </xpath>

        <!-- Button Content -->
        <xpath expr="//Dropdown/button/div" position="replace">
            <div class="d-flex align-items-center gap-2">
                <i t-attf-class="{{ stateIcon(currentValue) }} {{ statusColor(currentValue) }}"/>
                <span class="o_status_label">
                    Auction Stages
                </span>
            </div>
        </xpath>

        <!-- Dropdown Item Icon -->
        <xpath expr="//CheckboxItem/span[1]" position="attributes">
            <attribute name="t-attf-class">
                {{ stateIcon(option[0]) }}
            </attribute>
        </xpath>

        <!-- Dropdown Item Color -->
        <xpath expr="//CheckboxItem/span[2]" position="attributes">
            <attribute name="t-attf-class">
                {{ statusColor(option[0]) }}
            </attribute>
        </xpath>

    </t>

</templates>