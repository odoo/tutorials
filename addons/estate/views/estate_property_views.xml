<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="estate_property_view_search" model="ir.ui.view">
        <field name="name">estate.property.view.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search>
                <field name="name" />
                <field name="postcode" />
                <field name="expected_price" />
                <field name="bedrooms" />
                <field name="living_area" filter_domain="[('living_area', '&gt;=', self)]" />
                <field name="facades" />
                <field name="property_type_id" />
                <separator />
                <filter string="Available" name="available_properties"
                        domain="['|', ('state', '=', 'new'), ('state', '=', 'offer_received')]" />
                <filter string="Cozy" name="cozy_properties" domain="[('tag_ids', '=', 'cozy')]" />
                <filter string="Renovated" name="renovated_properties" domain="[('tag_ids', '=', 'renovated')]" />
                <filter string="Cozy &amp; Renovated" name="cozy_and_renovated_properties"
                        domain="['&amp;', ('tag_ids', '=', 'cozy'), ('tag_ids', '=', 'renovated')]" />
                <filter string="Postcode" name="group_properties_by_postcode"
                        context="{'group_by':'postcode'}" />
                <filter string="Tags" name="group_properties_by_tags" context="{'group_by':'tag_ids'}" />
            </search>
        </field>
    </record>

    <record id="estate_property_view_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="New property">
                <group>
                    <header>
                        <button name="action_set_state_sold" type="object" string="SOLD"
                                invisible="state in ['sold', 'cancelled']" />
                        <button name="action_set_state_cancelled" type="object" string="CANCEL"
                                invisible="state in ['sold', 'cancelled']" />
                    </header>
                    <header>
                        <field name="state" widget="statusbar"
                               statusbar_visible="new,offer_received,offer_accepted,sold"
                               statusbar_size="small" />
                    </header>
                </group>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="Property Name" />
                        </h1>
                    </div>
                    <group>
                        <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}" />
                        <group>
                            <field name="property_type_id" options="{'no_create': true}" />
                            <field name="postcode" />
                            <field name="date_availability" />
                        </group>
                        <group>
                            <field name="expected_price" />
                            <field name="best_price" />
                            <field name="selling_price" />
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <group>
                                <group>
                                    <field name="description" />
                                    <field name="bedrooms" />
                                    <field name="living_area" />
                                    <field name="facades" />
                                    <field name="garage" />
                                    <field name="garden" />
                                    <field name="garden_area" invisible="not garden" />
                                    <field name="garden_orientation" invisible="not garden" />
                                    <field name="total_area" invisible="not garden" />
                                </group>
                            </group>
                        </page>
                        <page string="Offers">
                            <div>
                                <field name="offer_ids" string=""
                                       readonly="state in ['offer_accepted', 'sold', 'cancelled']" />
                            </div>
                        </page>
                        <page string="Other info">
                            <group>
                                <group>
                                    <field name="salesperson_id" />
                                    <field name="buyer_id" />
                                </group>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="estate_property_view_tree" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="Properties" decoration-success="state in ['offer_received', 'offer_accepted']"
                  decoration-bf="state=='offer_accepted'" decoration-muted="state=='sold'">
                <field name="name" />
                <field name="property_type_id" />
                <field name="tag_ids" widget="many2many_tags" />
                <field name="postcode" />
                <field name="bedrooms" />
                <field name="living_area" />
                <field name="expected_price" />
                <field name="selling_price" />
                <field name="date_availability" optional="hide" />
            </list>
        </field>
    </record>

    <record id="estate_property_view_kanban" model="ir.ui.view">
        <field name="name">estate.property.kanban</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <kanban default_group_by="property_type_id" records_draggable="False">
                <field name="state" />
                <templates>
                    <t t-name="card">
                        <div>
                            <strong>
                                <field name="name" />
                            </strong>
                        </div>
                        <div>
                            Expected price:
                            <field name="expected_price" />
                        </div>
                        <div>
                            <p t-if="record.state.raw_value == 'offer_received'">
                                Best price:
                                <field name="best_price" />
                            </p>
                            <p t-elif="record.state.raw_value == 'offer_accepted'">
                                Selling price:
                                <field name="selling_price" />
                            </p>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form,kanban</field>
        <field name="search_view_id" ref="estate.estate_property_view_search" />
        <field name="context">{"search_default_available_properties": True}
        </field>
    </record>
</odoo>
