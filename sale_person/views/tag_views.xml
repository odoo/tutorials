<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--This file acts as a view.xml that we define for models-->

    <!--Action for tag view-->
    <record id="action_x_sale_person_tag" model="ir.actions.act_window">
        <field name="name">Tags</field>
        <field name="res_model">x_sale_person_tag</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- Tag Form View -->
    <record id="view_x_sale_person_tag_form" model="ir.ui.view">
        <field name="name">x.sale.person.tag.form</field>
        <field name="model">x_sale_person_tag</field>
        <field name="arch" type="xml">
            <form string="Tag">
                <sheet>
                    <group>
                        <field name="x_name"/>
                        <field name="x_color" widget="color"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Tag list View -->
    <record id="view_x_sale_person_tag_list" model="ir.ui.view">
        <field name="name">x.sale.person.tag.list</field>
        <field name="model">x_sale_person_tag</field>
        <field name="arch" type="xml">
            <list string="Tags">
                <field name="x_name"/>
                <field name="x_color" widget="color"/>
            </list>
        </field>
    </record>
</odoo>