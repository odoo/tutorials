<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--This file acts as a view.xml that we define for models-->

    <!-- Action for contact view -->
    <record id="action_x_contact" model="ir.actions.act_window">
        <field name="name">Contacts</field>
        <field name="res_model">x_contact</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- Contact Form View -->
    <record id="view_x_contact_form" model="ir.ui.view">
        <field name="name">x.contact.form</field>
        <field name="model">x_contact</field>
        <field name="arch" type="xml">
            <form string="Contact">
                <sheet>
                    <group>
                        <group>
                            <field name="x_name" placeholder="Contact Name"/>
                            <field name="x_customer_type" placeholder="Customer Type"/>
                        </group>
                        <group>
                            <field name="x_city" placeholder="City"/>
                            <field name="x_area" placeholder="Area"/>
                            <field name="x_pin_code" placeholder="Pin Code"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Contact list View -->
    <record id="view_x_contact_list" model="ir.ui.view">
        <field name="name">x.contact.list</field>
        <field name="model">x_contact</field>
        <field name="arch" type="xml">
            <list string="Contacts">
                <field name="x_name"/>
                <field name="x_customer_type"/>
                <field name="x_city"/>
                <field name="x_area"/>
                <field name="x_pin_code"/>
            </list>
        </field>
    </record>
</odoo>