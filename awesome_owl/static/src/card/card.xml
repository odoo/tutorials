<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">
    <t t-name="awesome_owl.Card">
        <div class="card d-inline-block m-2">
            <div class="card-body">
                <h5 class="card-title">
                    <t t-out="props.title"/>
                    <button class="btn btn-sm border ms-2" t-on-click="toggleContent">
                        <t t-esc="contentToggle.isToggled ? 'Hide' : 'Toggle'"/>
                    </button>
                </h5>
                <t t-if="props.slots and contentToggle.isToggled">                
                    <p class="card-text">
                        <t t-slot="default" />
                    </p>
                </t>
            </div>
        </div>
    </t>
</templates>
