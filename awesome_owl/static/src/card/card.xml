<?xml version="1.0" encoding="utf-8"?>
<templates xml:space="preserve">
    <t t-name="awesome_owl.Card">
        <div class="card d-inline-block m-2" style="width: 18rem;">
            <div class="card-body">
                <h5 class="card-title">
                    <t t-esc="props.title"/>
                    <button class="btn btn-primary" t-on-click="toggle">
                        <t t-if="state.isOpen">Collapse</t>
                        <t t-if="!state.isOpen">Expand</t>
                    </button>
                </h5>
                <p class="card-text"><t t-if="state.isOpen" t-slot="content"/></p>
            </div>
        </div>
    </t>
</templates>
