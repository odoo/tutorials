<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">

    <t t-name="awesome_owl.card">
        <div class="card d-inline-block m-2" style="width: 18rem;">
            <div class="card-body">
                <h5 class="card-title">
                    <t t-esc="props.title"/>
                    
                    <button class="btn bg-success text-white ms-2" t-on-click="this.onClick">
                        <t t-if="!state.isShown">Show content</t>
                        <t t-else="">Hide content</t>
                    </button>
                </h5>

                <t>
                    <p t-if="state.isShown" class="card-slot">
                        <t t-slot="default"/>
                    </p>
                </t>
            </div>
        </div>
    </t>

</templates>
