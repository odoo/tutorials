<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">
    <t t-name="awesome_owl.Card">
        <div class="card d-inline-block m-2 shadow" style="width: 18rem;">
            <div class="card-header d-flex justify-content-center align-items-center">
                <div class="flex-grow-1 text-center" ref="header-div"
                     t-att-class="toggle.value ? 'fw-bolder': ''">
                    <t t-if="props.slots.header">
                        <t t-slot="header"/>
                    </t>
                </div>
                <span class="fa icon ms-2"
                      t-on-click="toggleCard"
                      t-att-class="toggle.value ? 'fa-bars fa-rotate-90' : 'fa-bars'" />
            </div>
            <t t-if="toggle.value">
                <div class="card-body">
                    <t t-if="props.slots.title"><h5 class="card-title text-center "><t t-slot="title"/></h5> </t>
                    <p class="card-text">
                        <t t-slot="default"/>
                    </p>
                </div>
            </t>
        </div>
    </t>
</templates>
