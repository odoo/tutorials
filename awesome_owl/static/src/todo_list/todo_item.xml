<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="awesome_owl.todoitem">
        <div t-att-class="props.todo.isCompleted ? 'text-muted text-decoration-line-through' : ''">
            <input type="checkbox" t-att-checked="props.todo.isCompleted" t-on-change="onToggle" />
            <span>
                <t t-out="props.todo.id"/>: 
                <t t-out="props.todo.description"/>
            </span> 
            <span class="fa fa-remove text-danger ms-2" role="button" t-on-click="onRemove" />
        </div>
    </t>
</templates>
