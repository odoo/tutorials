<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">
    <t t-name="awesome_owl.TodoItem">
        <div t-att-class="props.todo.isCompleted ? 'text-muted text-decoration-line-through border p-3 m-2' : 'border p-3 m-2'">
            <input type="checkbox" t-att-checked="props.todo.isCompleted" t-on-change="onTodoChange"/>
            <t t-esc="props.todo.id"/>
            <span>.</span>
            <t t-esc="props.todo.description"/>
        </div>
    </t>
</templates>
