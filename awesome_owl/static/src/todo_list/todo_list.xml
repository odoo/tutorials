<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="awesome_owl.todo_list">
        <div t-ref="input_focus">
            <input placeholder="Add a todo" type="text" t-on-keyup="addTodo" t-model="state.newTodo"/>
        </div>
        <ul>
            <t t-foreach="todos" t-as="todo" t-key="todo.id">
                <TodoItem id="todo.id" description="todo.description" isCompleted="todo.isCompleted" toggleCompleted="() => this.toggleCompleted(todo.id)" removeTodo="() => this.removeTodo(todo.id)"/>
            </t>
        </ul>
    </t>
</templates>
