<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="awesome_owl.todo_list">
        <div class="p-3 card d-inline-block m-2">
            <input placeholder="Enter a new task" t-on-keyup="addTodo" t-ref="todoInputRef" t-model="currentTodoInput.value"/>
            <t t-foreach="todos" t-as="todo" t-key="todo.id">
                <TodoItem todo="todo" toggleState.bind="toggleState" removeTodo.bind="removeTodo" />
            </t>
        </div>
    </t>
</templates>
