<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">
    <t t-name="awesome_owl.TodoItem">
        <div class="form-check">
            <t t-set="todo" t-value="props.todo"/>
            <input class="form-check-input" type="checkbox" t-att-id="todo.id" t-att-checked="todo.done" t-on-change="todo.toggle"/>
            <label t-att-for="todo.id" t-att-class="todo.done ? 'text-decoration-line-through text-muted' : '' ">
                <t t-esc="todo.id"/>.
                <t t-esc="todo.title"/>
            </label>
            <span role="button" class="fa fa-trash ms-3 text-danger" t-on-click="() => props.onRemove(props.index)"/>
        </div>
    </t>
</templates>
