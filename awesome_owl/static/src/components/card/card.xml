<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">
    <t t-name="awesome_owl.Card">
        <div class="card d-inline-block m-2" style="width: 24rem">
            <div class="card-body" style="background-color: #34eba2">
                <div class="d-flex justify-content-between align-items-center">
                    <h3 class="card-title mb-4"><t t-esc="props.title" /></h3>
                    <button class="btn btn-sm btn-light" t-on-click="toggleContent">
                        <t t-esc="state.isOpen ? 'Hide' : 'Show'"/>
                    </button>
                </div>
                <div class="text-md text-gray-700" t-if="state.isOpen">
                    <t t-slot="default"/>
                </div>
            </div>
        </div>
    </t>
</templates>
