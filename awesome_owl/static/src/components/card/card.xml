<?xml version="1.0" encoding="UTF-8"?>
<templates>
    <t t-name="awsome_owl123.Card">
        <div class="card d-inline-block m-2" style="width: 18rem;">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center">
                    <h5 class="card-title" t-out="props.title"/>
                    <button class="btn btn-sm btn-secondary ms-2" t-on-click="toggle">
                        <t t-if="state.open">–</t>
                        <t t-else="">+</t>
                    </button>
                </div>
                <p class="card-text" t-if="state.open">
                    <t t-slot="default" />
                </p>
            </div>
        </div>
    </t>
</templates>
