<?xml version="1.0"?>
<odoo>
    <record id="action_flights" model="ir.actions.act_window">
        <field name="name">Flights</field>
        <field name="res_model">flights</field>
        <field name="view_mode">tree,form</field>
    </record>

    <record id="view_flight_form" model="ir.ui.view">
        <field name="name">flights.form</field>
        <field name="model">flights</field>
        <field name="arch" type="xml">
            <form string="Flight">
                <sheet>
                    <group>
                        <field name="code"/>
                        <field name="name"/>
                        <field name="airline_id"/>
                        <field name="status" widget="boolean_toggle"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_flight_tree" model="ir.ui.view">
        <field name="name">flights.tree</field>
        <field name="model">flights</field>
        <field name="arch" type="xml">
            <tree string="Flight">
                <field name="code"/>
                <field name="name"/>
                <field name="airline_id"/>
                <field name="status"/>
            </tree>
        </field>
    </record>

    <record id="flights_view_search" model="ir.ui.view">
        <field name="name">flights.search</field>
        <field name="model">flights</field>
        <field name="arch" type="xml">
            <search string="Search">
                <field name="status"/>
                <filter name="active" string="Active" domain="[('status', '=', '1')]"/>
                <filter name="inactive" string="Inactive" domain="[('status', '=', '0')]"/>
                <group>
                    <filter string="Airline" name="airline_category" context="{'group_by': 'airline_id'}"/>
                </group>
            </search>
        </field>
    </record>
</odoo>
