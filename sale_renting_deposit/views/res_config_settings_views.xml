<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.rental.deposit</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="sale_renting.res_config_settings_view_form" />
        <field name="arch" type="xml">
            <xpath
                expr="//app[@name='sale_renting'] //block //setting //div[@class='content-group']"
                position="inside">
                <div class="row mt8">
                    <label for="deposit_product" class="col-lg-3 o_light_label" />
                    <field name="deposit_product" context="{'default_type': 'service'}"
                        placeholder="Set a product to be used for deposit on Sale Order" />
                </div>
            </xpath>
        </field>
    </record>

    <record id="action_rental_config_settings" model="ir.actions.act_window">
        <field name="name">Settings</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_id" ref="res_config_settings_view_form" />
        <field name="view_mode">form</field>
        <field name="context">{'module' : 'sale_renting_deposit', 'bin_size': False}</field>
    </record>

</odoo>