<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Extend the existing form view for the return wizard -->
    <record id="view_stock_return_picking_form_inherit" model="ir.ui.view">
        <field name="name">stock.return.picking.form.inherit</field>
        <field name="model">stock.return.picking</field>
        <field name="inherit_id" ref="stock.view_stock_return_picking_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='product_id']" position="after">
                <field name="picking_id" column_invisible="1"/>
                <field name="sale_order_id" options="{'no_create': True}"
                    required="context.get('active_ids').length > 1  and allowed_sale_order_ids.length >= 1" 
                    column_invisible="context.get('active_ids').length == 1 or not parent.show_so"/>
                <field name="purchase_order_id" options="{'no_create': True}"
                    required="context.get('active_ids').length > 1 and allowed_purchase_order_ids.length >= 1" 
                    column_invisible="context.get('active_ids').length == 1 or not parent.show_po"/>
            </xpath>
        </field>
    </record>

    <record id="action_open_mass_return_wizard" model="ir.actions.act_window">
        <field name="name">Mass Return</field>
        <field name="res_model">stock.return.picking</field>
        <field name="view_mode">form</field>
        <field name="binding_model_id" ref="stock.model_stock_picking"/>
        <field name="target">new</field>
    </record>
</odoo>
