<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="account_move_view_form" model="ir.ui.view">
        <field name="name">account.move.view.form.inherit.pricelist.price</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form" />
        <field name="arch" type="xml">
            <xpath expr="//notebook//page//list//field[@name='quantity']" position="before">
                <field name="book_price" string="Book Price" optional="show" attrs="{'invisible': [('move_id.move_type', '!=', 'out_invoice')]}"/>
            </xpath>
        </field>
    </record>
</odoo>
