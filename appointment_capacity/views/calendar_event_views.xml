<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="calendar_event_view_form_gantt_booking" model="ir.ui.view">
        <field name="name">calendar.event.view.form.gantt.booking.inherit</field>
        <field name="model">calendar.event</field>
        <field name="inherit_id" ref="appointment.calendar_event_view_form_gantt_booking"/>
        <field name="arch" type="xml">
            <xpath expr="//sheet//group//div[@invisible]" position="attributes">
                <attribute name="invisible">appointment_type_capacity_type != 'multiple_seats'</attribute>
            </xpath>
        </field>
    </record>

    <record id="calendar_event_view_form_gantt_booking_resource" model="ir.ui.view">
        <field name="name">calendar.event.view.gantt.gantt.booking.resource.inherit</field>
        <field name="model">calendar.event</field>
        <field name="inherit_id" ref="appointment.calendar_event_view_gantt_booking_resource"/>
        <field name="arch" type="xml">
            <xpath expr="//gantt//templates//li[@t-if='appointment_type_manage_capacity']" position="attributes">
                <attribute name="t-if">appointment_type_capacity_type == 'multiple_seats'</attribute>
            </xpath>
        </field>
    </record>

</odoo>
