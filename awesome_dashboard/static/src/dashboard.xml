<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">

    <!-- Dashboard Template -->
    <t t-name="awesome_dashboard.AwesomeDashboard">
        <Layout display="display" className="'o_dashboard h-100'">

            <t t-set-slot="layout-buttons">
                <button class="btn btn-primary me-2" t-on-click="openCustomers">
                    Customers
                </button>

                <button class="btn btn-secondary me-2" t-on-click="openLeads">
                    Leads
                </button>

                <!-- SETTINGS ICON -->
                <button class="btn btn-outline-secondary" t-on-click="openConfiguration">
                    <i class="fa fa-cog"/>
                </button>
            </t>

            <div class="d-flex flex-wrap">
                <t t-foreach="items" t-as="item" t-key="item.id">
                    <DashboardItem
                        t-if="!state.disabledItems.includes(item.id)"
                        size="item.size || 1"
                    >
                        <t t-set="itemProps"
                           t-value="item.props ? item.props(statistics) : {}"/>

                        <t t-component="item.Component"
                           t-props="itemProps"/>
                    </DashboardItem>
                </t>
            </div>

        </Layout>
    </t>

    <t t-name="awesome_dashboard.ConfigurationDialog">
        <Dialog title="'Dashboard items configuration'">

            <div class="mb-2">
                Which cards do you wish to see?
            </div>

            <t t-foreach="items" t-as="item" t-key="item.id">
                <CheckBox
                    value="item.enabled"
                    onChange="(checked) => this.onChange(checked, item)"
                >
                    <t t-esc="item.description"/>
                </CheckBox>
            </t>

            <t t-set-slot="footer">
                <button class="btn btn-primary" t-on-click="done">
                    Done
                </button>
            </t>

        </Dialog>
    </t>

</templates>
