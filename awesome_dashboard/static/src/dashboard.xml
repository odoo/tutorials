<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

  <t t-name="awesome_dashboard.AwesomeDashboard" owl="1">
    <Layout className="'o_dashboard h-100'" display="{ controlPanel: {} }">

            <!-- Top Buttons -->
      <t t-set-slot="control-panel-create-button">
        <button type="button" class="btn btn-primary o-kanban-button-new" t-on-click="openCustomerView">
          Customer
        </button>
        <button type="button" class="btn btn-primary o-kanban-button-new" t-on-click="openLeadsView">
          Leads
        </button>
      </t>

            <!-- Dashboard Content -->
        <div class="dashboard-items" style="display: flex; flex-wrap: wrap; padding: 1rem;">

                <!-- New Orders -->
          <div style="width: 18rem; background: #fff; border-radius: 0.75rem; box-shadow: 0 2px 8px rgba(0,0,0,0.07); padding: 1rem; margin: 1rem; display: inline-flex; justify-content: center; vertical-align: top; min-height: 3rem;">
            <div class="o_dashboard_stat_block">
              <span class="o_dashboard_stat_label"><strong>New Orders This Month:</strong></span>
              <span class="o_dashboard_stat_value"><t t-esc="state.statistics?.nb_new_orders || 0" /></span>
            </div>
          </div>

                <!-- Total Amount -->
          <div style="width: 18rem; background: #fff; border-radius: 0.75rem; box-shadow: 0 2px 8px rgba(0,0,0,0.07); padding: 1rem; margin: 1rem; display: inline-flex; justify-content: center; vertical-align: top; min-height: 3rem;">
            <div class="o_dashboard_stat_block">
              <span class="o_dashboard_stat_label"><strong>Total Amount This Month:</strong></span>
              <span class="o_dashboard_stat_value"><t t-esc="state.statistics?.total_amount?.toFixed(2) || '0.00'" /></span>
            </div>
          </div>

                <!-- Avg T-Shirts per Order -->
          <div style="width: 18rem; background: #fff; border-radius: 0.75rem; box-shadow: 0 2px 8px rgba(0,0,0,0.07); padding: 1rem; margin: 1rem; display: inline-flex; justify-content: center; vertical-align: top; min-height: 3rem;">
            <div class="o_dashboard_stat_block">
              <span class="o_dashboard_stat_label"><strong>Avg. T-Shirts per Order:</strong></span>
              <span class="o_dashboard_stat_value"><t t-esc="state.statistics?.average_quantity || 0" /></span>
            </div>
          </div>

                <!-- Cancelled Orders -->
          <div style="width: 18rem; background: #fff; border-radius: 0.75rem; box-shadow: 0 2px 8px rgba(0,0,0,0.07); padding: 1rem; margin: 1rem; display: inline-flex; justify-content: center; vertical-align: top; min-height: 3rem;">
            <div class="o_dashboard_stat_block">
              <span class="o_dashboard_stat_label"><strong>Cancelled Orders:</strong></span>
              <span class="o_dashboard_stat_value"><t t-esc="state.statistics?.nb_cancelled_orders || 0" /></span>
            </div>
          </div>

                <!-- Avg Time -->
          <div style="width: 18rem; background: #fff; border-radius: 0.75rem; box-shadow: 0 2px 8px rgba(0,0,0,0.07); padding: 1rem; margin: 1rem; display: inline-flex; justify-content: center; vertical-align: top; min-height: 3rem;">
            <div class="o_dashboard_stat_block">
              <span class="o_dashboard_stat_label"><strong>Average time for an order to go from ‘new’ to ‘sent’ or ‘cancelled’</strong></span>
              <span class="o_dashboard_stat_value"><t t-esc="state.statistics?.average_time || 0" /> hrs</span>
            </div>
          </div>

        </div>
    </Layout>
  </t>

</templates>