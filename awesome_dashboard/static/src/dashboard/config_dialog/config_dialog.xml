<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">
    <t t-name="awesome_dashboard.ConfigDialog">
        <Dialog title="'Dashboard Configuration'">
            <div class="d-flex flex-column gap-2">
                <t t-foreach="props.items" t-as="item" t-key="item.id">
                    <div class="form-check">
                        <input
                            class="form-check-input"
                            type="checkbox"
                            t-att-id="item.id"
                            t-att-checked="!unchecked.has(item.id)"
                            t-on-change="() => toggle(item.id)"
                        />
                        <label class="form-check-label" t-att-for="item.id">
                            <t t-esc="item.description" />
                        </label>
                    </div>
                </t>
            </div>
            <t t-set-slot="footer">
                <button class="btn btn-primary" t-on-click="apply">Apply</button>
                <button class="btn btn-secondary ms-2" t-on-click="props.close">Cancel</button>
            </t>
        </Dialog>
    </t>
</templates>
