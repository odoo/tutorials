<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="awesome_dashboard.AwesomeDashboard">
        <div class="o_action">
            <Layout display="{ controlPanel: {} }">
                <t t-set-slot="control-panel-create-button">
                    <button t-on-click="openCustomers" class="btn btn-primary m-2">Customers</button>
                    <button t-on-click="openLeads" class="btn btn-success m-2">Leads</button>
                    <button t-on-click="openSettings" class="btn btn-light">
                        <i class="fa fa-cog"></i> 
                    </button>
                </t>
                <div class="my_dashboard_content d-flex flex-wrap gap-4">
                    <t t-foreach="getFilteredItems()" t-as="item" t-key="item.id">
                        <DashboardItem t-props="{ size: item.size || 1 }">
                            <t t-set="itemProp" t-value="item.props ? item.props(statistics) : {'data': statistics}"/>
                            <t t-component="item.Component" t-props="itemProp" />
                        </DashboardItem>
                    </t>
                </div>
            </Layout>
        </div>
    </t>

</templates>
