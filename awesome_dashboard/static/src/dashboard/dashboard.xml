<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">

    <t t-name="awesome_dashboard.AwesomeDashboard">
        <Layout className="'o_dashboard h-100'" display="{ controlPanel: {} }" >
            <div class="o_control_panel" >
                <t t-set-slot="control-panel-always-buttons">
                    <button class="btn btn-primary mx-1" t-on-click="openCustomers">Customers</button>
                    <button class="btn btn-secondary mx-1" t-on-click="openLeads">Leads</button>
                </t>
            </div>
            <t t-set-slot="control-panel-additional-actions">
                <button t-on-click="openConfiguration" class="btn p-0 ms-1 border-0">
                    <i class="fa fa-cog"></i>
                </button>
            </t>
            <div class="d-flex flex-wrap justify-content-center align-items-center">
<!--                 <DashboardItem>
                        <h3>New Orders</h3>
                        <p><t t-esc="stats.nb_new_orders"/> orders this month</p>
                </DashboardItem>
                <DashboardItem>
                        <h3>Total Revenue</h3>
                        <p>$<t t-esc="stats.total_amount"/></p>
                </DashboardItem>
                <DashboardItem>
                        <h3>Avg T-Shirts per Order</h3>
                        <p><t t-esc="stats.average_quantity"/></p>
                </DashboardItem>
                <DashboardItem>
                        <h3>Cancelled Orders</h3>
                        <p><t t-esc="stats.nb_cancelled_orders"/></p>
                </DashboardItem>
                <DashboardItem>
                        <h3>Avg Processing Time</h3>
                        <p><t t-esc="stats.average_time"/> days</p>
                </DashboardItem>
 -->
              
              <!-- Pie Chart -->
     <!--            <DashboardItem size="2">
                    <h3>Shirt orders by size</h3>
                    <PieChart t-if="stats" data="stats.orders_by_size" />
                </DashboardItem>
 -->
            <t t-foreach="items" t-as="item" t-key="item.id">
                <DashboardItem size="item.size || 1" t-if="!remove_items.removedItems.includes(item.id)">
                    <t t-set="itemProp" t-value="item.props ? item.props(stats) : {'data': stats}"/>
                    <t t-component="item.Component" t-props="itemProp" />
                </DashboardItem>
            </t>

            </div>


        </Layout>
    </t>

    <!-- Configuration Dialog -->
    <t t-name="awesome_dashboard.ConfigurationDialog">
        <Dialog title="'Dashboard items configuration'">
            Which cards do you wish to see?
            <t t-foreach="items" t-as="item" t-key="item.id">
                <CheckBox value="item.enabled" onChange="(ev) => this.onChange(ev, item)">
                    <t t-esc="item.description"/>
                </CheckBox>
            </t>
            <t t-set-slot="footer">
                <button class="btn btn-primary" t-on-click="onDone">
                    Done
                </button>
            </t>
        </Dialog>
    </t>
</templates>
