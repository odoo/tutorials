<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">

    <t t-name="awesome_dashboard.AwesomeDashboard">
        <Layout display="display" className="'o_dashboard h-100'">
            
            <t t-set-slot="layout-buttons">
                <div class="d-flex flex-column flex-sm-row gap-2">
                    <button class="btn btn-primary w-100 w-sm-auto" t-on-click="CustomerView">
                        Customers
                    </button>
                    <button class="btn btn-primary w-100 w-sm-auto" t-on-click="Leads">
                        Leads
                    </button>
                </div>
            </t>

            <t t-set-slot="control-panel-additional-actions">
                <button t-on-click="openConfiguration" class="btn p-0 ms-1 border-0">
                    <i class="fa fa-cog"></i>
                </button>
            </t>

            <div class="container">
                <div class="row g-3 text-center" t-if="statistics.isReady">
                    <t t-foreach="items" t-as="item" t-key="item.id">
                        <div class="col-12 col-sm-6 col-lg-4">
                            <DashboardItem t-if="!state.disabledItems.includes(item.id)" size="item.size || 1">
                                <t t-set="itemProp" t-value="item.props ? item.props(statistics) : {'data': statistics}"/>
                                <t t-component="item.Component" t-props="itemProp" />
                            </DashboardItem>
                        </div>
                    </t>
                </div>
            </div>
        </Layout>
    </t>

    <t t-name="awesome_dashboard.ConfigurationDialog">
        <Dialog title="'Dashboard items configuration'">
            <p class="text-center">Which cards do you wish to see?</p>
            <div class="d-flex flex-column gap-2">
                <t t-foreach="items" t-as="item" t-key="item.id">
                    <CheckBox value="item.enabled" onChange="(ev) => this.onChange(ev, item)">
                        <t t-esc="item.description"/>
                    </CheckBox>
                </t>
            </div>
            <t t-set-slot="footer">
                <button class="btn btn-primary w-100 w-sm-auto" t-on-click="done">
                    Done
                </button>
            </t>
        </Dialog>
    </t>

</templates>
