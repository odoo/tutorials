<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
	<t t-name="awesome_dashboard.AwesomeDashboard">
		<Layout className="'o_dashboard h-100'" display="{ controlPanel: {} }">
			<t t-set-slot="layout-buttons">
				<button t-on-click="showCustomers" class="btn btn-primary ms-2">
					Customers
				</button>
				<button t-on-click="showLeads" class="btn btn-primary ms-2">
					Leads
				</button>
			</t>
			<t t-set-slot="control-panel-additional-actions">
				<button class="btn ms-1" t-on-click="openDashboardConfiguration">
					<i class="fa fa-cog">
					</i>
				</button>
			</t>
			<div class="d-flex flex-wrap" t-if="result.isReady">
				<t t-foreach="items" t-as="item" t-key="item.id">
					<DashboardItem t-if="!state.disabledItems.includes(item.id)" size="item.size || 1">
						<t t-set="itemProps" t-value="item.props ? item.props(result) : {'data': result}" />
						<t t-component="item.Component" t-props="itemProps" />
					</DashboardItem>
				</t>
			</div>
		</Layout>
	</t>
</templates>
