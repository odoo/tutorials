<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">

    <t t-name="awesome_dashboard.Awesomedashboard">
        <Layout className="'o_dashboard h-100'" display="{ controlPanel: {} }">
            <t t-set-slot="control-panel-create-button">
                <div class="btn-group">
                    <button class="btn btn-primary" t-on-click="openCustomers">
                        Customers
                    </button>
                    <button class="btn btn-primary" t-on-click="openLeads">
                        Leads
                    </button>
                    <button class="btn btn-secondary" t-on-click="openDashboardSettings">
                        <i class="fa fa-cog"/>
                    </button>
                </div>
            </t>
            <div class="d-flex flex-wrap gap-3 p-3">
                <t t-foreach="visibleItems" t-as="item" t-key="item.id">
                    <DashboardItem size="item.size || 1">
                        <t t-set="itemProps"
                            t-value="item.props
                                ? item.props(statistics.data || {})
                                : { 'data': statistics.data || {}}"
                        />
                      <t t-component="item.Component" t-props="itemProps"/>
                    </DashboardItem>
                </t>
            </div>
        </Layout>
    </t>

</templates>
