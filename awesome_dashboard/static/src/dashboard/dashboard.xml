<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">
    <t t-name="awesome_dashboard.AwesomeDashboard">
        <Layout className="'o_dashboard h-100'" display="{controlPanel: {} }">
            <t t-set-slot="control-panel-create-button">
                <button t-on-click="openCustomers" type="button" class="btn btn-primary">Customer</button>
                <button t-on-click="openLeads" type="button" class="btn btn-primary">Leads</button>
            </t>
            <t t-set-slot="control-panel-additional-actions">
                <i class="fa fa-fw fa-cog my-auto" style="cursor: pointer;" t-on-click="openDashboardItemsDialog"></i>
            </t>

            <div t-if="statistics.isReady" class="d-flex flex-wrap gap-3 m-3">
                <t t-foreach="items" t-as="item" t-key="item.id">
                    <DashboardItem t-if="item.isSelected" size="item.size || 1">
                        <t t-set="itemProp" t-value="item.props ? item.props(statistics) : {'data': statistics}"/>
                        <t t-component="item.Component" t-props="itemProp" />
                    </DashboardItem>
                </t>
            </div>
        </Layout>
    </t>
</templates>
