<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">

    <t t-name="awesome_dashboard.AwesomeDashboard">
        <button t-on-click="openCustomers" type="button" class="btn btn-primary m-2">Customers</button>
        <button t-on-click="openCrm" type="button" class="btn btn-primary">Leads</button>
        Dashboard
        <button t-on-click="openConfig" class="btn btn-link">
        <i class="fa fa-cog" />
        </button>
        <Layout className="'o_dashboard h-100'">
            <div class="d-flex w-auto flex-wrap">
                <t t-foreach="items" t-as="item" t-key="item.id">
                    <t t-set="itemProp" t-value="item.props"/>
                    <t t-if="!state.disabledItems.includes(item.id)" t-component="item.Component" t-props="itemProp"/>
                </t>
            </div>
        </Layout>
    </t>
    <t t-name="awesome_dashboard.ConfigurationDialog">
        <Dialog title="'Dashboard items configuration'">
            Which cards do you whish to see ?
            <t t-foreach="items" t-as="item" t-key="item.id">
                <CheckBox value="item.enabled" onChange="(ev) => this.onChange(ev, item)">
                    <t t-esc="item.description"/>
                </CheckBox>
            </t>
            <t t-set-slot="footer">
                <button class="btn btn-primary" t-on-click="done">
                    Done
                </button>
            </t>
        </Dialog>
    </t>
</templates>
