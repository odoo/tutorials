<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_activity_type_form" model="ir.ui.view">
        <field name="name">activity.type.form</field>
        <field name="model">activity.type</field>
        <field name="arch" type="xml">
            <form string="Activity Type">
                <sheet>
                    <group>
                        <field name="code" />
                        <field name="name" />
                        <field name="status" widget="boolean_toggle"
                            options="{'terminology': 'Activity Type'}" />
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_activity_type_tree" model="ir.ui.view">
        <field name="name">activity.type.tree</field>
        <field name="model">activity.type</field>
        <field name="arch" type="xml">
            <tree string="Activity Types">
                <field name="code" />
                <field name="name" />
                <field name="status" widget='boolean_toggle' />
            </tree>
        </field>
    </record>

    <record id="view_activity_type_search" model="ir.ui.view">
        <field name="name">activity.type.search</field>
        <field name="model">activity.type</field>
        <field name="arch" type="xml">
            <search>
                <filter string="Active" name="filter_active" domain="[('status', '=', True)]" />
                <filter string="Inactive" name="filter_inactive" domain="[('status', '=', False)]" />
            </search>
        </field>
    </record>

    <record id="action_activity_types" model="ir.actions.act_window">
        <field name="name">Activity Types</field>
        <field name="res_model">activity.type</field>
        <field name="view_mode">tree,form</field>
        <field name="view_id" ref="view_activity_type_tree" />
        <field name="search_view_id" ref="view_activity_type_search" />
    </record>
</odoo>