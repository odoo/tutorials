<templates xml:space="preserve">

    <t t-name="awesome_clicker.client_action">
        <div class="o_nav_entry">
            Clicks: <ClickerValue/>
        <button class="btn btn-secondary" t-on-click="increment">
            <i class="fa fa-lg fa-plus"></i>
        </button>
    </div>
    <div t-if="this.clicker.level >= 1" class="ms-1 mt-1">
        <h2> Bots </h2>
         <div class="d-flex flex-row">
                <t t-foreach="this.clicker.bots" t-as="bot" t-key="bot">
                    <div t-if="this.clicker.bots[bot].levelRequirment lte this.clicker.level" class="card me-3">
                        <div class="card-header">
                            <t t-esc="this.clicker.bots[bot].have"/>x <t t-esc="bot"/> (<t t-esc="this.clicker.bots[bot].amountIncrease * this.clicker.power"/> clicks/10seconds)
                            <i class="fa fa-android"></i>
                        </div>
                        <div class="card-body">
                            <button t-on-click="() => this.clicker.buyBot(bot)" class="btn btn-primary" t-att-disabled="this.clicker.clicks lt this.clicker.bots[bot].price">
                                Buy <t t-esc="bot"/> (<t t-esc="this.clicker.bots[bot].price"/> clicks)
                            </button>
                        </div>
                    </div>
                </t>
        </div>
    </div>
    <div t-if="this.clicker.level >= 3" class="ms-1 mt-1">
            <h2> Power multiplier </h2>
            <div class="d-flex flex-row">
                <div class="card me-3">
                    <div class="card-header">
                        <t t-esc="this.clicker.power"/>x
                        <i class="fa fa-superpowers"></i>
                    </div>
                    <div class="card-body">
                        <button t-on-click="() => this.clicker.buyMultiplier()" class="btn btn-primary" t-att-disabled="this.clicker.clicks lt 50000">
                            Buy Power Multiplier (50000 clicks)
                        </button>
                    </div>
                </div>
            </div>
        </div>

        <div t-if="this.clicker.level >= 4" class="ms-1 mt-1">
            <h2> Trees </h2>
            <div class="d-flex flex-row">
                <t t-foreach="this.clicker.trees" t-as="tree" t-key="tree">
                    <div t-if="tree_value.level lte this.clicker.level" class="card me-3">
                        <div class="card-header">
                            <t t-esc="tree_value.purchased"/>x <t t-esc="tree"/> (1x <t t-esc="tree_value.produce"/> /30seconds)
                            <i class="fa fa-tree"></i>
                        </div>
                        <div class="card-body">
                            <button t-on-click="() => this.clicker.buyTree(tree)" class="btn btn-primary" t-att-disabled="this.clicker.clicks lt tree_value.price">
                                Buy <t t-esc="tree"/> (<t t-esc="tree_value.price"/> clicks)
                            </button>
                        </div>
                    </div>
                </t>
            </div>

            <h2> Fruits </h2>
            <div class="d-flex flex-row">
                <t t-foreach="this.clicker.fruits" t-as="fruit" t-key="fruit">
                    <div class="card me-3">
                        <div class="card-header">
                            <t t-esc="fruit_value"/>x <t t-esc="fruit"/>
                        </div>
                    </div>
                </t>
            </div>
        </div>
</t>

</templates>